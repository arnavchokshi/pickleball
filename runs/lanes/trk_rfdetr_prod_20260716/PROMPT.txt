You are lane trk_rfdetr_prod_20260716. Your complete binding contract is /Users/arnavchokshi/Desktop/pickleball/runs/lanes/trk_rfdetr_prod_20260716/spec.md — read it first, then the four read-first docs it names. Execute the gates strictly in order (env-fidelity gate BEFORE anything else; STOP typed on gate failure). Lane-local venv only — never touch the repo .venv. CPU/MPS only, no GPU fleet. Write only your lane dir. Deliver report.json + the decision table + POOLDIAG_PHASE1.md (+ FLIP_PROPOSAL.md only on full PASS) per spec.
