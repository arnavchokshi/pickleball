# P0-I ghost-card verdict for tonight (Fable): NOT PASSED — honest negative, no re-run burned

- Card run 1: DECISIVE FAIL (unbound fragments exported as claimed players; root-caused).
- Export-shape fix landed + smoked on the pulled artifacts with the REAL scorer:
  burlington projection 0 spectFP / 4 tracks (would plausibly pass); wolverine projection
  47 TRUE spectator FP / 2 switches / cov4 0.2433 — the BOUND SLOTS themselves are poor on the
  hard clip. Pre-registered hard axes (spectFP=0, switches=0) would fail ⇒ the one-shot re-run
  was NOT burned (no-tuning rule + money discipline; the smoke already answers it).
- REMAINING DEFECT (named, measured headroom): slot binding/recovery quality on Wolverine.
  Design counterfactual says IDF1 0.8519 / cov4 0.6167 / 0 FP is achievable on these same pools;
  the implementation binds far below that. Next session: fix binding/recovery to the design, then
  the one-shot card. Do NOT tune thresholds.
- VERIFIED=0. Ghost removal is NOT yet a user-visible claim.
