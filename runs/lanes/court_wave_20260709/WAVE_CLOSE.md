# COURT WAVE close (2026-07-09 18:00 -> 2026-07-10 ~12:30 PDT, Fable bg manager)

## Verdict
Owner directive "deeply research + build court auto-find" executed as: design round (28-agent
refuted SOTA fanout + gpt-5.6-sol xhigh proposal + 4-agent census) -> data unlock (masked-loss
trainer, 3,921-row licensed real corpus) -> decisive GPU experiment (TRAIN-1 legacy arch,
TRAIN-4 court_unet_v2 both inits). RESULT: real supervision WORKS in-family (2.8px) but
cross-source transfer is the wall; with ~9 dataset-families the 21M net memorizes family
identity (290px held-out) while the tiny CNN generalizes (13.5px). BINDING CONSTRAINT =
independent viewpoint diversity. VERIFIED=0 unchanged. Best-stack delta: NONE (court rows
unchanged: unet_v2 + e4_fusion stay PENDING/DORMANT).

## Honest kills banked this wave (do not re-run)
1. Harvest pseudo-label projection (camera cuts inside all 8 73V rally clips; 2 sources still
   fail solver bars on corrected labels) — court_data1 report.
2. Tennis-transfer lineage (no license; zero-shot = noise on 21 frames) — court_ext1.
3. Roboflow-only court_unet_v2 fine-tune, both inits (this wave's headline negative) — court_train4.
4. TRAIN-1 substitute legacy arms (real signal learned; owner-card transfer no) — court_train1.

## Next-wave queue (evidence-ranked)
1. OWNER O1 (the unlock): +20 diverse calibrated viewpoints per Sol §2.6 strata (~2h owner/
   reviewer time; prelabel+correct tooling exists). Everything else is bounded by this.
2. Capacity-right-sizing probe WITH new data: tiny/mid arch + v2 arch on expanded corpus
   (TRAIN-1-vs-4 contrast says capacity must match family count).
3. A1 build (multi-instance + semantic segments + clip optimizer) per DESIGN_PROPOSAL — start
   once transfer is nonzero on owner cards.
4. Fix booked env/tooling: train_court_model_v2.py DataLoader in_order torch>=2.6 vs fleet
   2.5.1 (default config crashes on fleet); homography-refinement no-op/worse investigation;
   snapshot re-bake (court_external + w7_courtkpingest gt_roots + torch decision).
5. Product seam (independent of model): court trust-chain dead-end after corner taps (w7crit
   finding), profile-matching lib unwired, iOS tap UI absent — these ship guided-confirm value
   regardless of auto-find.

## Owner asks (numbered, safe defaults active)
1. O1 viewpoint acquisition session (~2h): approve + schedule. Default: blocked on you.
2. Net-height truth contract (34in center sag vs 36in template) before the 2cm net gate is
   scored. Default: net stays template-prior, never a measured claim.
3. Commercial rights rulings: CC-BY Roboflow in shipped stack w/ attribution (recommend YES);
   YouTube-harvest promotion-training rights (recommend R&D-only). Default: R&D-only.
4. Auto-coverage product gate (recommend >=80% auto-accept at <=1% false-confident-accept).
   Default: not yet gated.

## Cost
GPU: TRAIN-1 $1-7.7 (1.8h H100) + TRAIN-3 $0 (stockout) + TRAIN-4 $1.4-10.3 (2.4h H100) =
~$2.4-18 total, mid ~$5-9. Codex: 5 lanes + 1 xhigh ideation. Zero preemptions, zero orphans,
fleet 0/4 at close.
