# Deferred Patch Ledger - 2026-07-05

Sweep commands:

```bash
find runs \( -path '*/labels/*' -o -path '*/eval_clips/*' \) -prune -o -name '*.patch' -type f -print | sort
rg -n -i "deferred patch|apply after|wiring patch" runs --glob '!**/labels/**' --glob '!**/eval_clips/**'
```

Patch apply checks were read-only: `git apply --check` and `git apply --reverse --check`. Protected labels/eval paths were pruned from the search.

| patch path | what it does | target files | already applied? | still applies cleanly? | recommendation |
|---|---|---|---|---|---|
| `runs/lanes/ball_failclosed_fixes_20260704/deferred_virtual_world_arc_status_and_raw_ball_flags.patch` | Adds `ball_track_arc_solved.status != "ran"` fail-closed gating in `virtual_world.py` and marks approximate/raw ball world frames render-only/not-for-detection, plus tests. | `threed/racketsport/virtual_world.py`; `tests/racketsport/test_virtual_world.py` | Yes. Current/HEAD has status gate at `threed/racketsport/virtual_world.py:385`, render-only propagation at `threed/racketsport/virtual_world.py:1338-1364`, and tests at `tests/racketsport/test_virtual_world.py:734` and `785-795`. | No. `git apply --check` returns `128`, `error: patch with only garbage at line 4`. | Do not apply. Mark superseded by the later manual integration/`790930ed`; replace with a real diff only if archival cleanliness matters. |
| `runs/lanes/ball_v1_viewer_trail_20260704/deferred_app_wiring.patch` | Wires `BallTrailLayer`, `ImpactMarkers`, `BallHonestyHud`, arc-solved parsing, and auto-bounce candidate manifest parsing into the replay app. | `web/replay/src/App.tsx`; `web/replay/src/viewerData.ts` | Yes. HEAD already imports/renders the trail/honesty modules and parses candidate URLs (`HEAD:web/replay/src/App.tsx` has `BallHonestyHud` and `parseAutoBounceCandidates`; `HEAD:web/replay/src/viewerData.ts` has `auto_bounce_candidates_url`/`ball_bounce_candidates_url`). Working tree has further dirty changes for arc render/display FPS. | No. Forward check fails on `App.tsx:8` and `viewerData.ts:614`; reverse check also fails because files drifted beyond the original patch. | Do not apply. Treat as superseded by `790930ed` plus current dirty viewer work. |
| `runs/lanes/court_autofind_20260705/handoff/court_autofind_wave_a.patch` | Large Court auto-find Wave A handoff: CAL model/eval/train scripts, court detector/proposal modules, synthetic stream, server/replay review UI, tests, and symlink/checklist/package changes. | Many files including `BUILD_CHECKLIST.md`, `scripts/racketsport/evaluate_court_model_v2.py`, `scripts/racketsport/train_court_model_v2.py`, `threed/racketsport/court_*`, `server/*`, `web/replay/*`, tests, and symlink entries. | No as a mainline capability. `MASTER_PLAN.md:53` and `103-108` say worktree-only; current main tree has no Court Wave A source wiring. Some target files such as `BUILD_CHECKLIST.md` and `web/replay/package.json` are already dirty for other work. | No. `git apply --check` fails on `BUILD_CHECKLIST.md`, missing symlinks (`eval_clips/eval_clips`, `models/models`, `third_party/third_party`), and `web/replay/package.json`. | Do not apply blindly in reset. Rebase from `worktree-court-autofind-20260705` or regenerate a clean patch after current dirty docs/web changes settle. |
| `runs/lanes/v1_verify_grounding/i1_new_test_worldhmr_stance_grounding.patch` | Adds a worldhmr stance-grounding test proving R3 placement anchors transitions without speed clamp. | `tests/racketsport/test_worldhmr_stance_grounding.py` | Yes. The test file exists in HEAD and includes `test_stance_aware_grounding_anchors_transitions_to_placement_without_speed_clamp` at `tests/racketsport/test_worldhmr_stance_grounding.py:49`. | No. Forward check fails because the file already exists; reverse check fails because content drifted. | Do not apply. Superseded by current tracked test file. |
| `runs/lanes/v1_verify_grounding/i1_target_diff.patch` | Integrates R3 grounding behavior: disables same-pass placement refine, runs grounding refine z-only when R3 provenance exists, updates foot-pin/worldhmr/virtual_world/orchestrator logic and tests. | `scripts/racketsport/process_video.py`; `tests/racketsport/test_body_grounding_refine.py`; `tests/racketsport/test_foot_pin.py`; `tests/racketsport/test_process_video.py`; `tests/racketsport/test_virtual_world.py`; `threed/racketsport/body_grounding_refine.py`; `threed/racketsport/foot_pin.py`; `threed/racketsport/orchestrator.py`; `threed/racketsport/virtual_world.py`; `threed/racketsport/worldhmr.py` | Partially/largely present in current dirty tree: same-pass placement refine disabled at `scripts/racketsport/process_video.py:1121`; z-only R3 grounding at `scripts/racketsport/process_video.py:2591-2660`; tests at `tests/racketsport/test_process_video.py:1074-1090`; stance test at `tests/racketsport/test_worldhmr_stance_grounding.py:49-77`. | No. Forward and reverse checks fail across all target files due drift. | Do not apply as a patch. Use it only as historical intent and reconcile against the active BODY/placement dirty files. |
| `runs/lanes/racket_6dof_20260705/STATUS.md:138-140` (mention only; no patch file found) | Says pipeline integration of `build_paddle_pose_fused` is deferred until ball/speed `process_video.py` and `virtual_world.py` changes are verified/committed. | Expected future target: `scripts/racketsport/process_video.py` (and maybe manifest/build docs). | No. Current `process_video.py` only reads existing `racket_pose_estimate.json` at `scripts/racketsport/process_video.py:2681-2703`; it does not invoke `build_paddle_pose_fused.py`. | N/A, no patch file exists under the lane. | Create an explicit integration patch after current dirty pipeline files settle. Until then, `paddle_pose_fused` remains built-not-wired. |
| `runs/lanes/racket_6dof_20260705/STATUS.md:179-181` (mention only; no patch file found) | Authorizes a one-line `virtual_world.py` wording patch for `_paddle_estimate_trust_band`. | Expected target: `threed/racketsport/virtual_world.py` | UNPROVEN/apparently not applied as described. Current reason at `threed/racketsport/virtual_world.py:1567-1571` is still generic physics-derived estimate wording for non-wrist estimates. | N/A, no patch file exists under the lane. | If wanted, have the racket lane owner produce a concrete patch after `virtual_world.py` settles; do not infer it from status prose. |
| `runs/lanes/ball_tracking_long_run_STATUS.md:149`, `157`, `235`, `498-499` (mention only; duplicates known patches) | Mentions the ball failclosed `virtual_world` patch and viewer app wiring patch as pending/deferred in the long-run status. | Same targets as the first two BALL patch rows. | Superseded. The referenced behavior is now present at HEAD/current tree as listed above. | N/A for the status file; the underlying patch files do not apply cleanly. | Update/remove stale status references in a docs cleanup lane if needed; no code action from this ledger. |
| `runs/lanes/ball_i1_default_integration_20260705/spec.md:28-33` (spec mention; duplicates known patches) | Owner spec requested applying the two BALL deferred patches during I1. | Same targets as the first two BALL patch rows. | Superseded by later integration. | N/A for the spec file; underlying patch files do not apply cleanly. | No reset action. Keep only as historical task specification. |

## Text Mention Sweep Notes

- Non-log/report/status/spec files with relevant deferred-patch prose were: `runs/lanes/ball_failclosed_fixes_20260704/report.json`, `runs/lanes/ball_v1_viewer_trail_20260704/report.json`, `runs/lanes/ball_v1_viewer_trail_20260704/structured_report.json`, `runs/lanes/ball_tracking_long_run_STATUS.md`, `runs/lanes/ball_i1_default_integration_20260705/spec.md`, `runs/lanes/racket_6dof_20260705/STATUS.md`, plus several lane specs repeating ownership/fence rules.
- Raw logs (`log.txt`/`codex.log`) repeat the same known patch paths and lane fences but did not reveal additional `*.patch` files beyond the five listed above.
