# WIRING-AUDIT Truth Table - 2026-07-05

Ground truth checked on `main` at HEAD `2381ce88c0136bd47ae9ec6fd22f37958a43e8a6`.

Ball arc file mtimes captured because adjacent sessions were live:

| file | mtime | size |
|---|---:|---:|
| `threed/racketsport/ball_arc_solver.py` | `2026-07-05T16:25:19-0700` | 187434 bytes |
| `threed/racketsport/ball_flight_sanity.py` | `2026-07-05T14:51:06-0700` | 26584 bytes |
| `threed/racketsport/ball_arc_chain.py` | `2026-07-05T16:30:46-0700` | 40968 bytes |

Status vocabulary: `DEFAULT-ON`, `OPT-IN flag`, `BUILT-NOT-WIRED`, `WORKTREE-ONLY`, `DEAD-CODE`.

| feature | status | evidence | committed@HEAD? | test that covers the wiring | notes |
|---|---|---|---|---|---|
| Ball 3D chain as default pipeline stage: ball candidates sidecar, auto-bounce anchors, frozen arc solver config, flight-sanity gate, arc render artifact | DEFAULT-ON | `scripts/racketsport/process_video.py:453-459` lists `ball` then `ball_arc`; `scripts/racketsport/process_video.py:1775-1852` runs `run_default_ball_arc_chain` unless `--no-ball-arc`; `threed/racketsport/ball_arc_chain.py:42-72` freezes row-22 configs; `threed/racketsport/ball_arc_chain.py:107-180` writes auto-bounce candidates, arc-solved output, flight sanity, and arc render; `threed/racketsport/ball_arc_chain.py:186-228` records render/net/candidate policy in the manifest. | Yes, landed in `790930ed`; current `process_video.py`, `ball_arc_chain.py`, `ball_arc_solver.py`, `ball_flight_sanity.py`, and tests are dirty. | `tests/racketsport/test_ball_arc_chain.py:23` is the frozen row-22 fixture guard; `tests/racketsport/test_ball_arc_chain.py:181` covers arc render/schema; `tests/racketsport/test_process_video.py:2806-2847` covers process_video arc artifacts. | Default still requires an available `ball_track.json` and `court_calibration.json`; the ball stage itself blocks if no current BALL source/runtime exists (`scripts/racketsport/process_video.py:1531-1543`). |
| Ball viewer trail + honesty KPI + fail-closed `TRUSTED={"ran"}` parsing | DEFAULT-ON | `web/replay/src/components/modules/ballTrail.ts:8-34` defines the trusted-status allowlist; `web/replay/src/components/modules/ballTrail.ts:373-389` suppresses samples/segments unless status is trusted; `web/replay/src/App.tsx:610-619` loads arc-solved + bounce candidates; `web/replay/src/App.tsx:1235-1259` renders impact markers, trail layer, and honesty HUD; `scripts/racketsport/verify_process_video_viewer.py:39-47` mirrors the allowlist; `scripts/racketsport/verify_process_video_viewer.py:113-138` fails if a self-killed solve renders measured. | Yes, landed in `790930ed`; current viewer/verify files are dirty. | `web/replay/src/components/modules/ballTrail.test.ts:109-127`; `tests/racketsport/test_verify_process_video_viewer.py:123-214`; `web/replay/src/shotTrails.test.ts:160-174`. | Viewer loads it when the manifest exposes `ball_arc_solved_url`; missing artifacts are a no-op, not a promotion. |
| `net_plane` consumed by default arc solve | DEFAULT-ON | `scripts/racketsport/process_video.py:1822-1826` passes `_existing_optional_path(self.clip_dir / "net_plane.json")`; `threed/racketsport/ball_arc_chain.py:121` loads/validates it; `threed/racketsport/ball_arc_chain.py:151` passes `net_plane=net_plane_for_solve`; `threed/racketsport/ball_arc_chain.py:169-170` and `217-228` preserve provenance/policy. | Yes, landed in `790930ed`; current files dirty. | `tests/racketsport/test_ball_arc_chain.py:334-388` covers valid/missing net plane behavior; `tests/racketsport/test_process_video.py:2829-2831` captures the passed path. | This is no longer hardcoded `None` in the default chain. |
| BODY slim monoliths default, `--fetch-body-monoliths` restore path, no-force reuse with `skeleton3d+body_full_clip_gate` | DEFAULT-ON | `threed/racketsport/orchestrator.py:644-684` defaults `write_body_monoliths=False`; `threed/racketsport/orchestrator.py:1161-1218` skips `smpl_motion.json` and `body_mesh.json` by default; `threed/racketsport/orchestrator.py:1292-1301` marks monoliths `not_built`; `threed/racketsport/orchestrator.py:1314-1330` produces index/gate artifacts and only inserts monoliths when requested; `scripts/racketsport/process_video.py:2241-2273` reuses completed `skeleton3d.json + body_full_clip_gate.json` when no monolith exists; `scripts/racketsport/process_video.py:2324-2338` constructs the local runner without overriding `write_body_monoliths`. | Yes for slim default/reuse/fetch flag; current BODY files/tests dirty. | `tests/racketsport/test_process_video.py:1913-1957` covers default and `--fetch-body-monoliths`; `tests/racketsport/test_process_video.py:3348-3372` covers no-force skeleton+gate reuse; `tests/racketsport/test_body_serialization_timing.py:140-193` covers slim default/no monoliths. | Restore path is the explicit flag; default review artifacts are the skeleton/gates/index, not monolithic payloads. |
| `body_mesh_index` consumed by viewer, and index built in memory on VM | DEFAULT-ON | `threed/racketsport/body_mesh_index.py:113-145` builds from in-memory payload; `threed/racketsport/body_mesh_index.py:147-172` builds from array-backed records; `threed/racketsport/body_mesh_index.py:244-248` writes the index; `threed/racketsport/orchestrator.py:1219-1230` builds an index while monoliths are skipped; `scripts/racketsport/process_video.py:2841-2868` finds root or nested `body_mesh_index`; `web/replay/src/viewerData.ts:64-72` parses `body_mesh_index_url`; `web/replay/src/App.tsx:585-606` fetches the index/faces. | Partly yes; current orchestrator/viewer are dirty. Array-native source file is untracked, but the legacy in-memory payload/index path is tracked. | `tests/racketsport/test_replay_viewer_manifest.py:96-133`; `tests/racketsport/test_process_video.py:1265-1303`; `web/replay/src/viewerData.test.ts:1226-1480`. | Viewer consumption is default when manifest carries the index URL. Array-backed builder exists but depends on the untracked `body_array_native.py` when using the experimental array-native path. |
| Binary `.npy` chunk subprocess handoff default (`racketsport_sam3dbody_batch_binary_chunk` v1) plus legacy fallback | OPT-IN flag | Direct runner supports binary at `scripts/racketsport/run_sam3dbody_batch.py:67-72` and writes binary chunks at `scripts/racketsport/run_sam3dbody_batch.py:995-1015`; CLI default is `pickle` at `scripts/racketsport/run_sam3dbody_batch.py:315-323`; orchestrator wrapper explicitly passes `--chunk-format pickle` at `threed/racketsport/orchestrator.py:1768-1794`; loader handles binary and pickle at `scripts/racketsport/run_sam3dbody_batch.py:742-773`; fallback path is `threed/racketsport/orchestrator.py:1840-1845`. | Yes for current pickle-default behavior; files dirty. | `tests/racketsport/test_run_sam3dbody_batch.py:611-680` covers binary roundtrip; `tests/racketsport/test_run_sam3dbody_batch.py:682-941` covers pickle/default behavior. | Product default is not binary. There is a wording bug: `threed/racketsport/orchestrator.py:1854-1855` still reports `binary_sidecar_v1` even after running pickle chunks. |
| `--body-schedule=overlap` | OPT-IN flag | `scripts/racketsport/process_video.py:432-437` selects overlap only when options say `overlap`; `scripts/racketsport/process_video.py:4680-4689` CLI default is `serial`; overlap blocks only ball/arc/events/fill before the hard join. | No; dirty-only, absent from `HEAD:scripts/racketsport/process_video.py`. | `tests/racketsport/test_process_video.py:3798-3808`; `tests/racketsport/test_process_video.py:3981-4170`. | Serial default is intended byte-identical. |
| `body_array_native.py` gate-feed path / payload collapse | WORKTREE-ONLY | `threed/racketsport/body_array_native.py:24-42` defines the builder; `threed/racketsport/orchestrator.py:29-33` imports the untracked module; `threed/racketsport/orchestrator.py:672` defaults `experimental_body_array_native=False`; `threed/racketsport/orchestrator.py:1110-1148` uses array-native only when `samples and self.experimental_body_array_native`; `threed/racketsport/body_array_native.py:122-190` builds visual smoothing/grounding/index inputs without materializing monoliths. | No; `threed/racketsport/body_array_native.py` is untracked and orchestrator is dirty. | `tests/racketsport/test_body_serialization_timing.py:140-193` proves default stays legacy; `tests/racketsport/test_body_serialization_timing.py:209-261` covers explicit array-native opt-in. | Runtime behavior is opt-in in the current file, but the import is unconditional. A clean checkout at HEAD lacks the module. |
| Contact-dense mesh scheduling `ball_aware` mode, hitter dense in +/-0.5s contact windows | WORKTREE-ONLY | `scripts/racketsport/process_video.py:4630-4675` defaults mesh coverage to `ball_aware` and exposes optional ball-aware artifacts; `scripts/racketsport/process_video.py:2022-2064` loads `events_selected.json` and `ball_track_arc_solved.json` only if present; `threed/racketsport/body_compute.py:26-37` defines contact-dense profile/reasons; `threed/racketsport/body_compute.py:273-288` activates it from frame plan policy `mode=="ball_aware"`; `threed/racketsport/body_compute.py:432-449` records contact-dense schedule metrics; `threed/racketsport/frame_rating.py:27-50` documents the required ball-aware inputs. | No for contact-dense body scheduling; `HEAD:threed/racketsport/body_compute.py` has no `contact_dense`. Some ball-aware planner plumbing predates this dirty diff. | `tests/racketsport/test_body_compute.py:597-631`; `tests/racketsport/test_frame_rating.py:634-784`; `tests/racketsport/test_process_video.py:1810-1890`. | Missing for a default fresh run: current ball runtime/source may be absent (`process_video.py:1531-1543`), and the default chain writes `ball_track_arc_solved.json` but not `events_selected.json` (`threed/racketsport/ball_arc_chain.py:158`). Therefore physical-contact triggers are unavailable unless an external/legacy events artifact is supplied; proximity/swing can still trigger when their inputs exist. |
| Stance-aware temporal smoothing + VP-A placement redistribution + VP-A2 reset carry | WORKTREE-ONLY | `threed/racketsport/worldhmr.py:44-59` defines R3/stance/reset constants; `threed/racketsport/worldhmr.py:411-430` applies refined stance phase lock and visual smoothing in the BODY artifact builder; `threed/racketsport/worldhmr.py:551-650` performs refined stance phase lock/pin; `threed/racketsport/worldhmr.py:875-922` carries/resets sparse gaps; `threed/racketsport/worldhmr.py:1127-1200` applies visual joint smoothing; `threed/racketsport/worldhmr.py:1833-2043` does stance-aware smoothing; `threed/racketsport/placement.py:54-90` includes `visual_max_root_step_m`; `threed/racketsport/placement.py:804-930` handles segmented gaps; `threed/racketsport/placement.py:1116-1222` enforces visual root step limits. | Partial: earlier stance-aware pieces exist at HEAD; VP-A/VP-A2/visual smoothing files are dirty-only. | `tests/racketsport/test_worldhmr_stance_grounding.py:49-77`; grounding/placement tests are in dirty test files. | Default-on only inside the current dirty BODY path when R3 placement/stance provenance is present. Not a clean HEAD capability. |
| Viewer VP-C2 mesh-root fix + 2x-FPS interpolation button + flicker/presence hold | WORKTREE-ONLY | `web/replay/src/viewerData.ts:1186-1210` aligns solid body mesh frames to world skeleton roots; `web/replay/src/viewerData.ts:2028-2102` defines interpolation/hold windows; `web/replay/src/viewerData.ts:2258-2279` applies mesh-root render translation with floor guard; `web/replay/src/viewerData.ts:2354-2399` builds/readouts 2x FPS data; `web/replay/src/App.tsx:755-808` computes display-FPS data; `web/replay/src/App.tsx:1398-1475` renders the 2x-FPS button; `web/replay/src/App.tsx:2212-2219` and `2325-2369` pass presence opacity/render translation to meshes. | No; current viewer files are dirty-only for VP-C2/display-FPS/root alignment. | `web/replay/src/viewerData.test.ts:754-840`; `web/replay/src/viewerData.test.ts:906-1030`; `web/replay/src/viewerData.test.ts:1141-1221`; `web/replay/src/viewerData.test.ts:1434-1505`. | Mesh-root fix and hold are active when solid meshes render in the dirty viewer. The 2x-FPS path is user opt-in via the button. |
| `paddle_pose_fused` racket estimator | BUILT-NOT-WIRED | `scripts/racketsport/build_paddle_pose_fused.py:1-8` says it emits the same `racket_pose_estimate.json` contract; CLI args are at `scripts/racketsport/build_paddle_pose_fused.py:52-90`; `scripts/racketsport/process_video.py:2681-2703` only reads an existing `racket_pose_estimate.json`; `scripts/racketsport/process_video.py:2748-2756` confidence gate also only reads it; `threed/racketsport/virtual_world.py:201-242` and `1412-1453` consume an estimate but never build it. | Built code is tracked but dirty; no process_video integration at HEAD or in working tree. | `tests/racketsport/test_paddle_pose_fused.py` covers the estimator/CLI; no process_video wiring test found. | Rendering works only when `racket_pose_estimate.json` is manually produced/provided. |
| `grounding_refine` | BUILT-NOT-WIRED | `scripts/racketsport/process_video.py:461-468` includes `grounding_refine` in suffix stages; `scripts/racketsport/process_video.py:2519-2546` skips unless `foot_contact_phases.json`, calibration, and tracks exist; `scripts/racketsport/process_video.py:2575-2664` refines only with present phases; `threed/racketsport/body_grounding_refine.py:55-82` consumes phases; `scripts/racketsport/run_physics_footlock.py:92-94` can write `foot_contact_phases.json`, but no default process_video stage invokes that CLI. | Default stage exists at HEAD; current grounding/R3 files dirty. | `tests/racketsport/test_process_video.py:996-1108`; `tests/racketsport/test_body_grounding_refine.py:161-167`. | Not dead code: a standalone producer exists. But default process_video does not produce the required phases, so the stage usually skips. |
| Court auto-find Wave A | WORKTREE-ONLY | `MASTER_PLAN.md:53` says Wave A patch is worktree-only and not mainline; `MASTER_PLAN.md:103-108` points to worktree branch/handoff patch; `BUILD_CHECKLIST.md:306` says pending worktree branch; `find runs/lanes/court_autofind_20260705/handoff` shows handoff artifacts only; `git diff --name-only HEAD -- scripts threed tests web ios docs configs | rg -i "court|cal|auto|line|proposal"` produced no main-tree source change hits from this lane. | No mainline source integration; handoff/lane artifacts are not tracked in main. | No default pipeline wiring test on main. | Main carries reports/handoff references, not the Wave A implementation. |
| `monitor_process_resources.py` direct-CLI reference test / scaffold gap | BUILT-NOT-WIRED | `tests/racketsport/test_monitor_process_resources.py:1-45` imports helper functions only; `tests/racketsport/test_scaffold_tool_index.py:10` expects no missing direct CLI tests; `tests/racketsport/test_scaffold_tool_index.py:169-197` asserts missing direct references are zero; live `list_scaffold_tools.py` reports `scripts/racketsport/monitor_process_resources.py` with `direct_cli_reference_test=None` and `missing_direct=1`. | No; monitor file/test are clean, gap still present. | The existing scaffold test itself would catch the gap; there is no direct CLI reference test for monitor. | This was not fixed. It is the known unowned failure in multiple lane reports. |
| `confidence_gate` approx-ball fix, bounce-lift review-only markers, `ball_inflections` no `(0,0,0)` | DEFAULT-ON | `threed/racketsport/confidence_gate.py:266-270` bands approx world frames as low/predicted and marks review-only; `threed/racketsport/ball_physics_fill.py:339-349`, `905-912`, and `1023-1028` write render-only/not-for-detection markers for filled/lifted samples; `threed/racketsport/ball_inflections.py:191-197` emits `ball_world_xyz=None` instead of fabricated origin when raw world coordinates are absent; `threed/racketsport/virtual_world.py:1338-1364` preserves review-only markers in world ball frames. | Yes via `790930ed` for confidence/inflections/virtual_world behavior; `virtual_world.py` is currently dirty but the cited logic is at HEAD. | `tests/racketsport/test_confidence_gate.py:167-206`; `tests/racketsport/test_ball_inflections.py:107-133`; `tests/racketsport/test_virtual_world.py:717-795`; `tests/racketsport/test_process_video.py:1346-1384`. | These are wired into default consumers where the artifacts exist; they remain review-only markers, not promotions. |

## Unproven / Negative Findings

- I found no current default invocation of `build_paddle_pose_fused.py` from `process_video.py` or `virtual_world.py`.
- I found no default `process_video.py` producer for `foot_contact_phases.json`; `run_physics_footlock.py` is a separate CLI.
- I found no mainline Court auto-find Wave A source wiring; the available artifact is the handoff patch under the lane directory.
- I found no direct CLI reference test for `monitor_process_resources.py`.
