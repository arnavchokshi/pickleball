# ROUND-3 FOCUSED CONFIRM of court_c0_ingest_20260721 fix2 (read-only) — DECIDABLE THIS ROUND

Scope: ONLY the four R2 blocking fixes (review_r2.json) as claimed closed in report_fix2.json.
Re-run your own probes: (1) both verbatim sliver-geometry configs + one new variant must be
rejected; (2) load 60 rows through the REAL C1 trainer code path with CWD = repo root on a
relocated corpus copy; (3) the real pinned manifest (shards key) must be accepted by the
production CLI today; (4) the PPA alias map is hash-pinned and family counts (66/18, 8 frozen
holdout source groups) recompute. Suite: confirm failures are exactly the known set.
VERDICT in final JSON: ACCEPT or REJECT (name the failing probe). No new scope.
