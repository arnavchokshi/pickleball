# ROUND-2 RE-REVIEW of data_steward_ledger_20260721 after FIX round (read-only)

Prior review: runs/lanes/data_steward_ledger_20260721_review/review.json (REJECT: dispatch gate
trusted caller declarations). Fix claim: report_fix1.json. Target files per spec.md FILE OWNERSHIP.

Re-run your EIGHT original probes as negative-input validation against the fixed audit CLI —
each must refuse based on the ACTUAL command argv and ledger state, not caller declarations:
protected event seed GPU dispatch; eval_clips protected; NC-licensed source; compare-only gallery;
IYnbdRs1Jdk-derived court pack; state=REJECTED tt_sounds; zero-decoded with empty asset_ids;
argv referencing protected data under a benign contract. Then attempt TWO new bypass shapes of
your choosing (e.g., relative/normalized path aliasing of a protected asset in argv; a consumer
declaring a subset selection it does not enforce). Verify: ledger schema v2 round-trip determinism;
25-asset snapshot integrity; the mixed-identity refusal posture (5 assets refuse training absent a
proven immutable clean selector — is that fail-closed behavior real?).
VERDICT in final JSON: ACCEPT | ACCEPT_WITH_FIXES | REJECT.
