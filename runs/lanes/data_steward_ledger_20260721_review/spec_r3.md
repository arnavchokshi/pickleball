# ROUND-3 FOCUSED CONFIRM of data_steward_ledger_20260721 fix2 (read-only) — DECIDABLE THIS ROUND

Scope: ONLY the four round-2 findings (review_r2b.json) as claimed closed in report_fix2.json.
Re-check with the CLI on real inputs: (1) concatenated (-dPATH) and equals-form option scanning —
your original -druns/... case must refuse, and an unsupported dash form must refuse as ambiguous;
(2) selector references in output/save/log/config roles must not grant access; a data-bearing
input selector must be required consistently; (3) the ledger's protected-PERSON row now binds all
four person_ground_truth.json files by hash with 11,459 boxes and clean byte accounting;
(4) the immutable 15,312-row PERSON selector exists, is hash-bound, and the BALL vs PERSON
consumer records are truthful. Also confirm the corrected snapshot (27 assets, never-queued 0) is
internally consistent. The 2 extra suite failures are ball-arc timing-budget items previously
classified as load-sensitive flakes — confirm they are unrelated to the owned files.
VERDICT in final JSON: ACCEPT or REJECT (name the failing check).
