# LANE w3_phasefix_20260707 — foot-contact PHASE QUALITY fix: per-foot confidence-bearing phases + weak-bilateral demotion + gate-stream instrumentation (wave-3 #1+#2 composed FIX)

## HARD RULES (violating any = lane rejected)
- You are Codex lane `w3_phasefix_20260707`. Work ONLY inside /Users/arnavchokshi/Desktop/pickleball.
- No git branches/commit/push. Write runs/lanes/w3_phasefix_20260707/commit_manifest.md for the manager.
- OWNED files (edit only these + new tests + runs/lanes/w3_phasefix_20260707/):
  `threed/racketsport/foot_contact.py`, `threed/racketsport/placement.py` (phase-producer + lock-eligibility paths ONLY — root/anchor placement computation UNTOUCHED), `threed/racketsport/foot_lock_solver.py`, `threed/racketsport/footlock.py`, `threed/racketsport/foot_pin.py` (only if genuinely needed), `threed/racketsport/body_grounding_refine.py`, `threed/racketsport/worldhmr.py` + `threed/racketsport/body_grounding_quality.py` (gate-stream INSTRUMENTATION ONLY — the 0.03 threshold, review floors, and guard constants are FROZEN), `threed/racketsport/pose_temporal.py` ONLY for phase-producer code paths (NEVER the MAD bone-length / smoothing weights — those are default-ON per a wave-2 ruling and adversarially cleared), and the corresponding tests/racketsport/test_*.py files.
- FENCED (do NOT edit; deferred patches under runs/lanes/w3_phasefix_20260707/deferred_patches/ if genuinely required): `scripts/racketsport/process_video.py`, `threed/racketsport/camera_motion.py`, `scripts/racketsport/estimate_camera_motion.py`, `scripts/racketsport/remote_body_dispatch.py`, `scripts/fleet/*`, `threed/racketsport/roboflow_corpus.py`, `threed/racketsport/ball_tracknet_cvat_dataset.py`, `threed/racketsport/frame_rating.py`.
- THRESHOLDS FROZEN: any change to foot_slide_gate 0.03, p95 bars, root-jump review floor 10.0, guard constants, or refine's better/worse predicate CONSTANTS = auto-reject. (Changing what refine CONSUMES is the assignment; changing how strictly it judges itself is not.)
- Protected data: 4 eval clips EVAL-ONLY (artifacts fine; Outdoor/Indoor labels never). Held-out harvest videos (IDs in runs/manager/heldout_eval_ledger.md) appear nowhere.
- Honest reporting; WIDE suite `MPLBACKEND=Agg .venv/bin/python -m pytest tests/racketsport -q --ignore=tests/racketsport/court_finding_technology_benchmark.py` + focused suites; classify residual failures REAL / PRE-EXISTING (prove at HEAD) / SANDBOX-SUSPECT / CROSS-LANE-SUSPECT (several lanes run concurrently — name suspected lane).
- importorskip("torch"); no new CLI expected (if you add one: direct-CLI reference test same-lane); no new root .md. `.venv/bin/python` always.
- Read first: BUILD_CHECKLIST.md last ~15 bullets; then the two diagnosis reports that DEFINE this lane:
  `runs/lanes/w3_slidediag_20260707/REPORT.md` (esp. §6 fix design + instrumentation request list) and
  `runs/lanes/w3_groundref_diag_20260707/REPORT.md` (esp. §5 recommendation + acceptance numbers).

## CONTEXT — the converged root cause (two independent diagnoses, one mechanism)
1. Foot-slide MAX gate fails burlington 40.6mm / outdoor 56.0mm vs `grounding_metrics.max_foot_lock_slide_m` ≤ 0.03 (p95 under bar everywhere → outlier phases). The slidediag lane REFUTED guard-fire as the mechanism (same-frame overlap 11-14%, wolverine 0%) and localized the outdoor max INSIDE a weak `bilateral_from_player_stance` phase with `source_phase_foot: unknown` (P3 right 791-804 @ 56.002mm, activated post-placement-fix); burlington's near-gate phase (P2 right 169-205 @ 40.067mm) is p06-identical (pre-existing weak phase).
2. grounding_refine self-kills 4/4 because 100% of consumed phases are bilateral-from-stance with 0 confidence keys and exact-foot BODY agreement only 0.363-0.651; its self-kill predicate (`mean_abs(after_foot) > mean_abs(before_foot) + 1e-12 OR track worse`) is honest — the INPUT phases are the problem. Note: track delta is 0.000 because xy_translation_enabled=false (leave that flag's default alone this lane).
- The official gated max is computed GPU-side (worldhmr/body_grounding_quality path) on samples not persisted today — hence the instrumentation deliverable. Offline you can only validate on persisted surrogates; the wave-end fresh GPU run is the gate proof. DO NOT claim gate clearance from offline replays.

## THE DESIGN (manager-ruled; both diagnosis reports bind the details)
A. **Foot-specific, confidence-bearing phases at the source**: the phase producer emits per-foot contact phases with per-phase confidence/quality fields (at minimum: `min_confidence`, `max_height_m`, `max_speed_mps`, source thresholds/provenance, foot assignment + assignment evidence). `bilateral_from_player_stance` with unknown foot is emitted ONLY as explicitly-weak (or split into per-foot phases when per-foot keypoint support allows); when no confident per-foot phase exists → honest absence, never a bilateral placeholder.
B. **Lock-eligibility demotion**: foot-lock scoring treats weak/unknown-foot phases as weak evidence — split or demote when anchor streams (smoothed/fused/original) show local inflection within the phase or per-foot keypoint support disagrees with the selected foot (slidediag §6). Root/anchor placement computation is UNTOUCHED (root-jump fix must not regress).
C. **Refine consumes only confident per-foot phases**: grounding_refine filters to confident per-foot phases; with zero such phases it no-ops honestly (and says so in its report artifact) instead of consuming bilateral placeholders.
D. **Gate-stream instrumentation** (worldhmr/body_grounding_quality): persist the exact GPU-side stream behind `max_foot_lock_slide_m` per the slidediag instrumentation request list: per-phase rows (clip, player, foot, phase_id, frames, slide_m, max-contributing frame, anchor xyz, contact source, foot assignment, weak/demoted/split status), per-frame rows inside phases (frame_idx, contact state, selected foot, lock anchor, raw/fused/smoothed/original XY, BODY root world, output_source, divergence/speed-cap flags, residuals, bbox margin, foot-pin correction magnitude), and a summary (top-20 phases by slide_m, all phases > 0.03, rejection reason code for every weak-filtered phase). BOUND the artifact size (summarize/stride if a clip would exceed ~20MB; state the policy).

## ACCEPTANCE (offline; all must hold)
- Unit tests: per-foot phase emission w/ confidence fields; weak-bilateral demotion/split on synthetic inflection + foot-disagreement cases; refine's confident-phase filter + honest no-op path; instrumentation rows schema-validated.
- Offline candidate replays on the SAVED wave-2 artifacts (skeleton/tracks/phases from runs/lanes/wave2_freshworlds_20260707/) reproduce, with new code: outdoor P3 791-804 weak phase demoted/split (persisted-surrogate max phase eliminated); burlington P2 169-205 neutral-or-lower; wolverine phases shrink-or-equal (18.4mm surrogate not worsened); img1605 no regression. Report before/after surrogate tables per clip.
- Phase artifact on all 4 clips: 0 `bilateral_from_player_stance` phases CONSUMED by refine; 100% of consumed phases carry the confidence fields; per-foot BODY-detector agreement ≥0.90 for every EMITTED per-foot phase on each clip OR the phase is not emitted (groundref acceptance numbers).
- Refine offline candidate reports: foot-plane before/after delta direction improves (after ≤ before) on ≥3 of 4 clips using saved inputs; full un-kill claim explicitly DEFERRED to the fresh GPU run.
- Root-placement isolation: show (diff scope + a targeted test) that root/anchor placement outputs are byte-identical on a replayed clip — only phase/lock/refine/instrumentation behavior changed.
- WIDE suite green per HARD RULES.
- Report the GPU-proof criteria block verbatim for the wave-end run: burlington+outdoor `max_foot_lock_slide_m` ≤ 0.03; p95 < 0.03 all clips; root jumps outdoor 0 / burlington ≤1 / others 0; wolverine still passing; refine `kill_recommended=false` + `residual_family_worse.foot_plane=false` on every clip with nonzero consumed phases; world NOT restored by the sanity gate; consumed counts match the phase artifact.

## SELF-ITERATION + BOUNDED FIX AUTHORITY
Iterate offline until every acceptance item passes or a genuine blocker (report with evidence). You own the listed threed/ files fully within the stated scope limits. process_video.py consumption wiring, if needed → deferred patch + report.

## STRUCTURED REPORT
objective_result vs acceptance; acceptance table incl. per-clip before/after surrogate values; changes file:line; full_suite + classification; instrumentation artifact size policy; HONEST ISSUES (esp. anything only provable on GPU); NEXT; commit_manifest path; BUILD_CHECKLIST bullet DRAFT in the report.
