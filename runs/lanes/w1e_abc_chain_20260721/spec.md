# w1e_abc_chain_20260721 — the LAST A/B/C blocker: executable, race-safe evaluation chain

Codex gpt-5.6-sol xhigh. Gate round-3 (runs/lanes/ultra_review_abc_gate3_20260721/log.txt) fixed 3/4; blocker 4 remains, four sub-items — fix EXACTLY (fixtures only, no judges, focused tests only):
1. PRODUCER HASHES: build_audio_onsets_v2.py + the ball_inflections payload must emit media/PTS sha256 identity fields (media_sha256 + pts_source identity) so the materializer's mandatory-hash requirement is satisfiable by the real producers. Additive payload fields; update their writers + tests.
2. RUNBOOK OVERWRITE BUG: build_abc_arm_manifests.py:32/:841 imports and re-runs the stale staging generator which overwrites the updated VM_ABC_RUN.md — decouple (materializer must never write the runbook; remove the import/write path); test that running the materializer leaves VM_ABC_RUN.md untouched.
3. OWNER-41 EVALUATOR MODE: eval_event_head.py needs an explicit owner-41 mode (--mode owner-val consuming the owner_102_manifest val split) emitting the fields the decision gate requires per seed/arm: macro-F1@±2 per class, negFP count, timing-p90, full-video firing rate. Deterministic; fixture-tested with synthetic predictions.
4. ONE-TOUCH GUARD RACE: VM_ABC_RUN.md:167/:243 — the rg exit-code logic is inverted (exit 2 proceeds) and the ledger-check + O_EXCL claim are not atomic. Rewrite as a single atomic claim: mkdir-based lock (atomic) + explicit grep exit-code handling (0=token-present=REFUSE; 1=absent=proceed; >=2=ERROR=refuse); test the three branches in a shell test or python equivalent.
FENCE: scripts/racketsport/{build_audio_onsets_v2.py,build_abc_arm_manifests.py,eval_event_head.py}, threed/racketsport/{audio_onsets_v2.py,ball_inflections.py} (additive payload fields only), runs/lanes/w1b_abc_loader_20260721/VM_ABC_RUN.md, their tests, lane dir. NO commits. Report each sub-item FIXED + hunk.
