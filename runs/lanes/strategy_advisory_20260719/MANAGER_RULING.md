# Manager ruling on the ULTRA advisory (Fable, 2026-07-20)

Per owner policy 2026-07-19, next-step decisions = Fable thinking + a gpt-5.6-sol ULTRA advisory,
manager rules. I re-derived each decision from the cited evidence and ADOPT all four, notes below.
VERIFIED=0 unchanged; nothing promotes a stack, default, or authority band.

## D1 COURT — ADOPT (advisory 0.87)
Finish static_cal_firstlock unchanged (in flight). Then a bounded classical line-front-end
hardening lane (frozen baseline -> ROI/lookalike-line rejection -> line-over-point weighting ->
shadow arm only if a shadow stratum measurably fails). Guided framing -> NS-03.LIVE, folded into
the existing phone/real-game session. TASKS 88-91 DEFERRED, re-scoped as frozen source-grouped
eval fuel (not a v1 calibration lever, not learned-finder training). Research: auto line detection
costs 15-24 pts vs solver; capture discipline is pb.vision's entire <1px edge; no system solves
~5px auto single-camera. Follow-on lane, NOT tonight's critical path.

## D2 BALL — ADOPT (queue #5 does NOT move)
Factorized: finish scaled RGB pretrain (T8) -> freeze -> repair fine-tune context contract
(finetune_event_head.py defaults to 9f, doesn't stamp window_frames; eval decodes +/-1.0s not exact
frame count) -> H0 RGB-only control + H1 wrist arm FROM THE SAME pretrain (never H0->H1; missing
wrist reproduces H0) -> default-off DP on DENSE public rallies only -> typed-only plane BOUNCE
scoped reopen AFTER CAL planes. DP requires plausible ~0.3-1.0 ev/s raw rate first. One
candidate-attributable physics violation kills the anchor arm. Plane-BOUNCE = scoped bounce-only
reopen of killed TT3D whole-chain geometry; whole-flight rejection stays binding.

## D3 PB.VISION — ADOPT (advisory 0.94) — CORRECTS MY EARLIER FRAMING
NO PB gallery predictions in event-head training now. All 13 source games (12 gallery + Demo Vid)
+ derivatives QUARANTINED compare-only (ID/title/SHA table in advisory). PB ToS/API restrict
automated/commercial/competing use; RD_ONLY is not a license; training on the gallery destroys the
head-to-head benchmark; PB + its own outputs = one teacher not independent votes. Reopen ONLY with
written training rights + frozen source-disjoint control + >=47/50 audited pseudo-label precision +
A/B/C lift test. CORRECTION I OWE THE OWNER: earlier I framed the gallery as a likely pseudo-label
multiplier for the 102 rows. Premature. Governance says no without written rights and it would
wreck our only honest benchmark. Real value stands: 12x COMPARE-ONLY benchmark (~490 rallies,
diverse venues) + confirms PB tracks no pose (our wrist advantage is unique). Pseudo-labels, if
ever, from rights-cleared public/owned pixels + our own predictors, never PB.

## D4 OWNER LABELS — ADOPT (advisory 0.80)
Use existing source split NOW: 61 train / 41 source-held validation (NOT all 102). Protected 50
stays one-shot final judge. Do NOT ask for the remaining 198 today. Reconcile ingest bookkeeping
(46/57 legacy maps vs 60 coords) before training, no owner relabel. Ask for ONE 50-row
uncertainty round only if median G_val >= +0.10 AND 3 seeds non-negative AND abs val macro-F1 <
0.80 AND val neg-FP <= 2/22 AND rate 0.3-1.0/s. Abs >= 0.80 -> stop asks, go wrist/DP. G_val <
+0.10 -> stop asks, diagnose. Fresh owner-shot footage outranks another old-pack round; RECORD A
REAL GAME stays the #1 ask.

## Net board changes
- T11: add fine-tune-context-repair preflight + 61/41 split + bookkeeping reconcile + G_val gate;
  H0 RGB control + H1 wrist arm.
- T12 DP: gated on T8 plausible rate; dense public rallies; default-off.
- NEW deferred lane: court classical line-front-end hardening (D1) - follow-on, not tonight.
- pb.vision gallery: compare-only quarantine; NOT a training source.
- North Star SS5 + owner-asks + OWNER_CHECKIN updated to match.
