# 2026-07-15 regression fix

`time_for_frame` now refuses silent CFR fallback only when the resolved legacy frame-time lookup is non-empty and omits the requested frame. An empty mapping carries no timing information and again follows the explicit-fps path, returning `time_basis=constant_fps_assumed`, `provenance=explicit_fps_argument`, and `fallback_used=true`; a non-empty partial mapping still raises. This corrects the four `ball_physics_fill` failures caused by the adopted tbwire semantic change without editing `ball_physics_fill.py`. Verification used direct pytest exit-code capture with no pipeline: `tests/racketsport/test_ball_physics_fill.py -q` produced 11 passed in 0.28s with literal exit code 0, and the focused `test_io_decode.py test_timebase_contract.py test_process_video.py test_audio_onsets.py -q` command produced 217 passed in 21.56s with literal exit code 0. VERIFIED=0 remains binding; this is a scoped regression pass with (c) no stack delta.
