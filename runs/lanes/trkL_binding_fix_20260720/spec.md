# trkL_binding_fix_20260720 — fix slot-binding/recovery quality to the design counterfactual (ULTRA)

Codex gpt-5.6-sol ULTRA (genuinely hard, gate-adjacent). The committed selection layer prevents fabrication but BINDS BADLY on wolverine: the card smoke (runs/lanes/trkL_unbound_export_fix_20260720/report.json + runs/lanes/p0i_scorecard_20260720/{vm_pull,CARD_VERDICT_20260720.md}) shows the 4 bound slots contain spectator content (47 true spectFP, 2 switches, cov4 0.2433) while the GT-informed counterfactual (runs/lanes/trk_rfdetr_prod_20260716/counterfactuals.py + the CF rows: IDF1 0.8519, 0 switches, 0 FP, honest cov4 0.6167) proves the SAME frozen pools support near-perfect selection. Close that gap WITHOUT GT and WITHOUT touching the registered thresholds (accept 0.35/reject 0.42/2.5m/12f/weights/S0.5 stay). NO commits. VERIFIED=0.

DO: study counterfactuals.py's decision pattern (what it stitches/refuses/binds using GT) and re-derive the equivalent GT-FREE decisions from the registered evidence classes (appearance embeddings, court presence, persistence, temporal consistency) inside player_selection.py's enrollment/binding/Layer-C recovery. Likely defects to hunt (from the smoke): slots binding to spectator fragments during enrollment windows; recovery failing to extend bound slots through gaps (cov4 0.24 vs CF 0.62); re-bind not preferring the enrolled owner.
FENCE: threed/racketsport/player_selection.py + tests/racketsport/test_player_selection.py + lane dir ONLY. OFF byte-identical stays (golden test). Registered thresholds untouched (assert in report — diff shows none).
ACCEPTANCE (local smoke on the PULLED frozen artifacts with the REAL scorer, honestly labeled projection-not-card): wolverine bound-slot spectFP → 0-5 (from 47), switches ≤1, cov4 ≥ 0.50 (toward CF 0.6167); burlington stays clean (0 FP, 4 slots); f44<->f87 still refused; all prior tests green. Then the manager runs the one-shot GPU card. Misses are misses — report honestly.
