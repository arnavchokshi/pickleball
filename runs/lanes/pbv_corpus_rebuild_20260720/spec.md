# pbv_corpus_rebuild_20260720 — rebuild the pb.vision corpus per the exact ultra repair gate

Codex gpt-5.6-sol xhigh. The corpus review (runs/lanes/ultra_review_pbvcorpus_20260720/log.txt, DO_NOT_USE) found a CRITICAL timebase bug (export events are in a 30fps timebase, sources are 60fps → all frames ~2x off) + holdout leakage in a derivative + confidence-stripping. Its "Exact repair gate" is your spec — implement it VERBATIM. CROSS-SIGNAL ROW: consumes pb.vision teacher events + our media timing (PTS); feeds the A/B/C hit-detection arms → typed anchors → ball 3D. NO commits; ultra re-review before training use. VERIFIED=0.

## THE REPAIR GATE (from the review, verbatim requirements)
1. TIMEBASE: convert export event frames through teacher TIMESTAMPS to the nearest encoded source PTS (never assume fps equality); derive fps/frame count FROM MEDIA metadata (frame_times where available, e.g. runs/lanes/pbv_replay_20260720/vm_pull/.../frame_times.json for xkadsq9bli3h; for videos without local media, compute from provenance-declared fps with an explicit timebase-conversion field and mark needs_pts_verify=true for the VM staging step). The review's worked example MUST verify: the 2.233s event on xkadsq9bli3h maps to source frame ~134 (60fps), not 67 — make this an explicit test.
2. HOLDOUTS: permanently denylist the 3 compare-only ids across EVERY derivative (incl. teacher_event_context.jsonl — segregate their rows into a separate compare_only file or mark training_eligible=false); test that no derivative row for a holdout is training-eligible.
3. SPLITS: assign by immutable source lineage (content hash from MANIFEST.json shas + video id); carry content hash + parent identity per row.
4. PROVENANCE: emit source sha256, export/CV version (get_cv_version.json), builder identity (script sha), filtering-policy identity into the manifest header.
5. CONFIDENCE + UNKNOWN MASKS: RETAIN teacher confidence per event; emit per-frame UNKNOWN masks so rejected/low-conf teacher events become ignored-frames (masked from loss), NEVER silently background. Respect datasets.py loss_validity_mask semantics or extend the row schema additively for a per-frame unknown mask the loader honors (coordinate: finetune_event_head.py is owned by the concurrent finetune_bypass_fix lane — do NOT edit it; if the loader needs a change, emit the required hunk in your report for the manager).
6. FROZEN A/B/C WEIGHTING (adopt the review's policy verbatim into the manifest fields): pseudo weight 0 / 0.25 / 0.5 for zero / one / >=2 independent audio-or-wrist agreements; PB confidence alone earns ZERO weight; uncertain frames ignored-not-background; normalized aggregate pseudo loss capped at human loss; selection only on owner validation. Emit the agreement-count field per event (the agreement computation itself runs at VM time when our audio/wrist artifacts exist per clip — emit needs_agreement_pass=true placeholders + the exact procedure in ABC_STAGE.md).
7. Real integration tests: a decode-verified test on the LOCAL xkadsq9bli3h media (data/pbv_replay_20260720/xkadsq9bli3h/max.mp4 is local) proving a corrected event frame decodes at the right timestamp; determinism; builder totals.

FENCE: scripts/racketsport/build_pbvision_event_corpus.py + tests/racketsport/test_pbvision_event_corpus.py + lane dir + regenerated corpus under runs/lanes/pbv_corpus_rebuild_20260720/. Do NOT touch finetune_event_head.py, datasets.py (hunks in report if needed), player_selection, court files.
ACCEPTANCE: every review FAIL item repaired with a test; the worked example passes; holdout-derivative test passes; rebuild deterministic; honest report.
