# w1d_abc_final_20260721 — the 4 remaining A/B/C launch blockers (gate round-2 list, verbatim)

Codex gpt-5.6-sol xhigh. From runs/lanes/ultra_review_abc_gate2_20260721/log.txt fix EXACTLY (nothing else; NO judge peeking — fixtures/synthetic only; do NOT run the wide suite, run ONLY your focused tests + the specific test files you touch, to honor the categorical no-judge rule the reviewer flagged):
1. DECISION GATE COMPLETE: abc_decision_gate.py must implement the FULL registered causal gate: median(B−A)≥+0.10 macro-F1@±2 AND paired-bootstrap 95% lower bound >0 AND all seeds non-negative AND B>C AND per-class regression ≤0.03 AND negFP ≤2/22 AND negFP(B) ≤ negFP(A)+1 AND timing-p90 non-worse AND rate 0.3-1.0/s AND equal final steps across arms/seeds. Kill the reviewer's counterexample: add it as a test (B with +2 negFP over A and 1 training step must FAIL).
2. EQUAL-STEP/STALE MANIFEST: interrupted fine-tune runs must not leave a stale finetune_manifest.json (finetune_event_head.py:993/:1081) — write manifest atomically LAST or delete on interrupt; test.
3. C-PLACEBO EXPOSURE PARITY: build_abc_arm_manifests.py:537/:625 — C must have IDENTICAL loss-valid frame counts to B (shuffle event times WITHOUT changing mask cardinality: the shuffled frame gets B's mask treatment, the vacated frame keeps validity as background — total loss-valid frames equal). Parity guard must count masks; add the 64-vs-63 probe as a failing-then-fixed test.
4. MANDATORY SHA CHAIN + RUNBOOK: audio/ball dependency hashes REQUIRED (not optional) at :356 — refuse manifests whose agreement inputs lack media/PTS-bound identity. Complete VM_ABC_RUN.md: derivative-to-media SHA binding at every hop, the protected-50 ONE-TOUCH sequence (ledger row + refusal wiring: gate computes on owner-41 only; protected-50 touched once at the very end on frozen arms; any second touch = hard refusal), and the final frozen evaluation sequence.
FENCE: scripts/racketsport/abc_decision_gate.py, scripts/racketsport/finetune_event_head.py, scripts/racketsport/build_abc_arm_manifests.py, runs/lanes/w1b_abc_loader_20260721/VM_ABC_RUN.md (update in place), their tests, lane dir. NO commits. Report each blocker FIXED + hunk.
