# ultra_review_abc_gate2_20260721 — A/B/C launch gate, round 2 (the GPU go/no-go)

REVIEW-ONLY, gpt-5.6-sol ultra. Round-1 gate (runs/lanes/ultra_review_abc_gate_20260720/log.txt) said DO_NOT_LAUNCH with 5 blockers. Three lanes claim they're fixed: w1a_abc_traintruth_20260721 (protected-window leak via window-overlap exclusion + rebuilt owner manifest; pseudo-loss cap POST class/frame weighting w/ adversarial 83%→≤50% test; executable abc_decision_gate.py; equal-step guarantee), w1b_abc_loader_20260721 (datasets.py schema-v2 per-frame UNKNOWN mask, ignored-not-background; build_abc_arm_manifests.py B/C materializers w/ frozen 0/0.25/0.5 agreement weights + SHA-bound consumed inputs; PTS sha-binding; VM_ABC_RUN.md), w1c_unknown_bridge_20260721 (finetune consumes frame_loss_mask, masked frames = zero loss, composition with pseudo-cap unchanged).

VERIFY each round-1 blocker is TRULY resolved in the live tree (re-check the 2 named leak windows are gone from the rebuilt owner_102_manifest; re-run/verify the adversarial pseudo-influence probe logic; UNKNOWN frames provably excluded end-to-end through finetune loss; the C-arm placebo parity claim; the decision gate computes the EXACT registered criteria; the VM_ABC_RUN sequence has SHA checks at each hop and the protected-50 one-touch rule). Confirm NO judge-peeking occurred in any of the three lanes' logs (spot-check for scorer invocations during development). Cross-lane composition: w1a's pseudo-cap + w1c's mask + w1b's weights interact in ONE loss path — verify the composition math once, end-to-end, on a synthetic batch.

OUTPUT: verdict LAUNCH_OK / LAUNCH_WITH_FIXES (exact) / DO_NOT_LAUNCH + file:line; one-line GPU go/no-go.
