# Owner check-in — THE single always-current file (updated 2026-07-20 END OF DAY)

⭐ HEADLINE: **THE PRODUCT CHAIN WORKS ON A FRESH CLIP.** The clip that showed "Players: 0"
yesterday now tracks its players end-to-end with ZERO human input (browser-verified screenshot in
~/Desktop/visual_evidence_20260720/). Ball event-head went 0 → F1 0.215 (frozen gate). The court
gallery you asked for is on your Desktop. The A/B/C hit-detection experiment is fully built but
held at DO_NOT_LAUNCH by ultra review (it caught a protected-eval leak + a loss-weighting bug that
would have invalidated the result) — first wave tomorrow. VERIFIED=0 everywhere; ~$27 of $35 spent;
fleet EMPTY confirmed.

## TODAY'S MEASURED RESULTS
1. COURT/PRODUCT ✅: fresh-clip proof — pooled calibration live: far_centerline 68 support/0.36px,
   gate TRUE, tracking RAN (players 51%/46% coverage), placement+world ran, viewer verified.
   (Honest: 0 full 3D meshes — our VM's EXCLUSIVE_PROCESS mode blocked the BODY subprocess, queued
   fix; skeleton fallback 5,984 frames; coaching_facts bug exposed + queued.)
2. BALL ✅: event head 0 → 510 preds / 70 TP@±2f / F1 0.215 on the frozen 50-clip gate (weighted
   loss broke the collapse). Corpus rebuilt with the 2x-timebase fix (all 8 checks PASS).
3. PEOPLE ✅/⚖️: fabrication-prevention + honest export COMMITTED (3 review rounds). Binding fix
   NOT committed — review proved scorer-guided development (judge contamination) + a veto bypass;
   work preserved as patch; next evaluation must use the UNTOUCHED holdout pair (prereg).
4. Court gallery (full-video calibrations, 5 venues) + evidence pack on your Desktop.

## TOMORROW'S FIRST WAVE (exact, from the ultra gate's blocker list)
1. Fix the protected-window leak (window-overlap exclusion, not center-distance —
   build_owner_event_manifest.py:111; 2 leaking windows identified).
2. Fix pseudo-loss normalization (cap AFTER class/frame weighting — measured 83.3% pseudo influence).
3. Apply the loader schema-v2/UNKNOWN-mask diff (staged at pbv_corpus_rebuild/LOADER_CHANGE_REQUIRED.diff).
4. Build the agreement/B/C materializers + SHA-bound PTS verify (VM staging step).
5. Wire the decision gate as EXECUTABLE code (median B−A ≥ +0.10, all-seeds, B>C, negFP, rate band).
=> re-review => LAUNCH A/B/C (the pb.vision hit-detection verdict).
Then: people untouched-holdout evaluation; EXCLUSIVE_PROCESS/BODY fix + coaching_facts field fix;
RF-DETR repro.

## MONEY: ~$27/$35 today. Fleet EMPTY (list-confirmed). Every VM torn down after its run.
