# Pickleball/Tennis CV Pipeline - Build Checklist & Agent Coordination

**This is the operational source of truth for the pickleball/tennis CV pipeline.** The lead build agent (Codex) and every subagent it spawns coordinate **here** - it is how everyone knows who is doing what, what is done, and what remains. Agents cannot talk to each other directly; **this file + git commit messages are the only communication channel.**

**Role:** operational task status, row ownership, and handoff checklist. Use `README.md` for canonical read order; each task below names its phase — go there for full detail and do not re-derive it.

**Model variants are benchmarked + human-approved, not assumed:** `TECH_STACK.md §2.3` lists each stage's default + candidates; **all H100/on-device speeds in this repo are estimates.** Task **EVAL-0** benchmarks the candidates on real clips, **renders side-by-side comparison videos, and requires human approval before locking the variant (§1.6) — unless one option is obviously better, in which case it may auto-finalize and log it.** The approved choice is locked in `models/MANIFEST.json`: ON-DEVICE LIVE uses the lightest iPhone ANE variant that gives usable guidance, while SERVER OFFLINE recomputes the accurate async result. Benchmark **Fast SAM-3D-Body first** for server-deep sizing, but do not let that displace the on-device live priority.

**Product scope reminder:** **single static camera** is the product focus. Multi-camera is FUTURE; multi-view is a TRAINING-TIME-ONLY technique that ships a single-camera model. Native **iOS Swift** client + GPU **server**. The canonical live/server split lives in `CAPABILITIES.md` section "Canonical Tier Split"; any conflicting tier wording in this checklist is a bug.

## Current truth snapshot (2026-06-30 audit continuation)

This audit cross-checked the checklist, implementation phases, current code, recent commits, local dirty tree, ignored run artifacts, and four parallel subsystem reviews. It is intentionally stricter than the prior status reset: `DONE` means code complete and unit/self-test green for that row, while `VERIFIED` still requires the documented real-clip acceptance gate. A scaffold, parser, fake runtime, precomputed adapter, review helper, candidate benchmark, or static artifact does not count as verified production capability.

Confirmed manager preflight from the 2026-06-30 docs/status pass: `body4d-gcp-prod` H100 is absent, `body4d-waker-ctrl` is running, and `pickleball-a100-spot-ase1a` is running with one idle A100-SXM4-40GB. The A100 VM has `/home/arnavchokshi/pickleball_git` and `/home/arnavchokshi/pickleball/.venv`, but it is missing the H100/pod-agent paths `/workspace`, `/workspace/pickleball`, and `/opt/conda/envs/fast_sam_3d_body/bin/python`. Current work is A100-first unless a row is formally rewritten to name A100 evidence; old H100-specific acceptance rows remain unpassed, not automatically superseded.

Status counts after this audit:

| status | count |
|---|---:|
| DONE | 10 |
| SCAFFOLD | 30 |
| PROTOTYPE-GATE | 2 |
| IN-PROGRESS | 2 |
| VERIFIED | 0 |

Canonical build priority from the tier split:

| priority | tier | work | current status rule |
|---|---|---|---|
| 1 | ON-DEVICE LIVE / fast tier | Court setup seed/cache from ARKit intrinsics+pose+floor plane; capture-quality guidance | IOS/CAL scaffold until device capture and sidecar gates pass |
| 2 | ON-DEVICE LIVE / fast tier | Person detect + track + N-lock with CoreML YOLO26n/s, lightweight ByteTrack/BoT-SORT, and court-polygon filter | IOS-3/TRK stays unverified until on-device latency and labeled ID gates pass |
| 3 | ON-DEVICE LIVE / fast tier | 2D pose/joints via Apple Vision or CoreML RTMPose-m | IOS-3 scaffold until physical-device FPS/latency gates pass |
| 4 | ON-DEVICE LIVE / fast tier | Distilled/quantized CoreML heatmap ball tracker at ~288p | **Top technical risk**; add an explicit spike before spending more on server replay polish |
| 5 | ON-DEVICE LIVE / fast tier | Ball IN/OUT + net-crossing line calls from homography + bounce on cached court, and contact timing from mic onset + wrist-velocity peak | scaffold until ball F1, timing, and device gates pass |
| 6 | SERVER OFFLINE / deep tier | 3D mesh, world grounding, foot-lock/physics, paddle 6DoF, full biomech, replay render, LLM copy, week-over-week | async deep track; keep SCAFFOLD/IN-PROGRESS until documented gates pass |

Borderline rule: camera-space mesh preview is `server-fast`, not phone-real-time. LiDAR is a near-field (~5 m) bonus only.

Historical commit-window note:

- Strict "past 12 hours" from the original audit time had **0 commits**. The relevant work burst was the wider prior window: 43 commits from 2026-06-26 23:00 PDT through `22fba00` on 2026-06-27 19:04 PDT.
- Do not treat those commit IDs or branch-ahead notes as live branch state. This checklist records capability truth, not the current Git pointer; agents must run `git status --short --branch` and `git log --oneline --decorate -5` at the start of a pass.
- Treat pushed code, local dirty changes, and ignored `runs/` evidence as three different evidence classes. Do not claim a capability from ignored run artifacts unless the row also has code, a reproducible command, and the relevant gate result.
- Accepted-four artifacts under `runs/eval0/prototype_gate_h100_v2` are prototype packet evidence, not populated `DATA-1` test clips.

Accepted-four artifact truth after the 2026-06-28 four-lane pass:

| lane | current truthful state | blocker before promotion |
|---|---|---|
| TRK/player coverage | canonical `tracks.json` coverage is too sparse for BODY scheduling: Burlington 2/180 four-player frames, Wolverine 1/91, Outdoor 1/1786, Indoor 0/900. H100 YOLO26m source-30 strict candidates improved Outdoor to 864/1800 and Indoor to 424/900 four-player frames and audit as `canonical_candidate_not_gate_verified`; widened-margin diagnostics audit as `diagnostic_only` because they include off-court frames. The latest A100 tiled candidate covers Outdoor 0-1150 but still fails promotion; blind repair/pruning, seeded source gap-fill, and global source-only selection are also diagnostic-only. Source-only ReID diagnostics first found no persisted embeddings; the follow-up learned embedding exporter then wrote 6,918 YOLO26n crop embeddings from the raw source pool using the recovered A100 checkpoint sha256 `511fcd11a37bae0f9f20bc66b02876b6a3e1e7e678fa24076be157b7c144e11f`. The selector/CLI now consumes that export as a weak diagnostic appearance cost and scores `runs/phase2/trk_embedding_source_selection_20260630T223103Z/`; best IDF1 is still only 0.4314 with 115 switches, 1379 spectator/background FP, and 0.5209 four-player coverage. | improve embedding-aware repair or retrain detector/tracker over the same source pool, then rerun labeled IDF1/spectator/ID-switch/throughput gates before TRK promotion |
| BODY | Fresh A100 full-track BODY smokes at `runs/body_joint_goal_smoke_20260630T001407/` prove the Fast SAM-3D-Body runtime can submit real videos and emit scheduled world-joint/mesh outputs when fed BODY-safe player-frames. The runner now calls Fast SAM once per tracked player bbox, scales track bboxes into the actual materialized BODY frame size before cropping, caps grounded root motion at 8 m/s by default, preserves the full-track submit schedule after BODY review helpers run, and resets temporal smoothing when a prior smoothed state would drift more than 0.75 m from the current track anchor. The latest synced reset-bound diagnostics are Burlington `a100_body_video_smoke_burlington_bboxscaled_resetcap075_runtime_20260701T001500Z` and Wolverine `a100_body_video_smoke_wolverine_bboxscaled_resetcap075_runtime_20260701T002500Z`: Burlington ran 149 scheduled frames / 265 player-frames with full-clip coverage 0.974265, and Wolverine ran 80 scheduled frames / 163 player-frames with full-clip coverage 0.964497. Both `body_full_clip_gate.json` files pass structurally, both `body_joint_quality.json` files have empty quality blockers/warnings, and both remain `quality_checked_needs_accuracy_gate` with `missing_world_mpjpe_gate`. The reset-bound runs cut max track-anchor residual to 0.748754 m / 0.721875 m after 41 / 24 smoothing resets, and the review bundles render selected overlays with 0 selected overlay alignment failures (Burlington 9 warnings over 27, Wolverine 2 warnings over 20). The compact report `runs/body_joint_goal_smoke_20260630T001407/body_gate_report_resetcap075.json` now keeps mesh/full-clip BODY passing, can score finalized labels from `body_world_label_packet.json` without local huge motion-file copies, and reports BODY label review `blocked_finalization`: 27 Burlington / 20 Wolverine selected samples are still at 0 accepted template samples, `selected_samples_have_overlay_warnings` is present, and `body_world_label_finalization_blocked` joins `body_joint_overlay_warning_review_required` as a promotion blocker while selected overlay warnings remain. `body_joint_quality_from_packet.json` sidecars show compact packet quality has no blockers for all 265 / 163 packet samples. This is runtime/coverage/review evidence, not BODY verification; Outdoor still has 0 trusted BODY output. | repair/promote TRK continuity/coverage for the broader clip set, fill representative reviewed/equivalent BODY world-joint labels, run the bundle `finalize_command`, resolve selected overlay warnings, and rerun the world-MPJPE/full-clip BODY gates before promotion |
| BALL/contact | `contact_timing_review_alignment.json` matches the 34 saved human-review contacts to promoted `contact_windows.json` at 0-frame delta, but `ball_verified=false`; this proves review-promotion alignment only, not machine BALL detection. The official TrackNetV3 checkpoint archive is hash-verified. A leased A100 full-CVAT run covers all 1,524 reviewed ball labels across Burlington/Wolverine/Outdoor, including Outdoor frames 0-1150. Raw TrackNetV3 A100 fails BALL quality at F1@20 0.554, recall@20 0.610, precision@20 0.508, hidden-FP rate 0.607, and 61 teleports. The dense hidden-negative TrackNetV3 CVAT dataset has now been materialized locally and fine-tuned on A100 from the official checkpoint; the dense candidate improves to F1@20 0.626, recall@20 0.647, precision@20 0.606, hidden-FP rate 0.342, and 2 teleports, but it still fails BALL verification and contact-cue coverage remains incomplete at 12/24 reviewed contacts. The hard-negative materializer now consumes `ball_hard_negative_iteration_plan.json` with Outdoor held out, but the fresh A100 epoch-20 hard-negative candidate regresses to F1@20 0.569, recall@20 0.583, precision@20 0.555, hidden-FP rate 0.342, and 1 teleport; held-out Outdoor drops to F1@20 0.296, so this iteration is diagnostic-only and rejected. The CPU local-search postprocess is diagnostic-only and rejected; at `runs/ball_local_search_postprocess_20260630T225735Z/`, dense local-search postprocess regresses to F1@20 0.487 / precision 0.429 / recall 0.563 / hidden-FP 0.970 / 10 teleports, and hard-negative local-search postprocess regresses to F1@20 0.502 / precision 0.490 / recall 0.514 / hidden-FP 0.342 / 18 teleports. PB-MAT runtime/checkpoints remain absent. | reject the hard-negative-only resume and local-search postprocess, target Outdoor localization/recall or a different model-side candidate, then rerun reviewed ball F1, hidden-FP, teleport, cue coverage, audio/wrist/ball fusion, and +/-2-frame contact timing gates |
| RKT/paddle | `paddle_true_corner_review.json` and crop sheets exist for all four clips, but every accepted-four candidate is box-derived: Burlington 42, Wolverine 36, Outdoor 397, Indoor 280. The 2026-06-30 CVAT package adds 2211 reviewed `paddle` rectangles across the three active clips and they now convert into review-only `racket_candidates.json`; they are still box-derived labels, not true face corners. `RacketStageRunner` rejects box-derived sources before canonical `racket_pose.json`; promotion audits are `safe_preview_only`. | label true paddle-face corners or provide CAD/reference/ArUco GT; then run RKT face-angle/contact gates |
| RPL/replay | `replay_readiness_report.json` is `blocked`: all four clips have review visuals and two valid static review GLBs each, but those are classified as `review_static_glb_export`, not production replay. Production replay and metrics are blocked by missing skeletal animation/skinning/production compression, BODY accuracy, preview-only paddle pose, approximate ball projection, missing DATA-1 labels, and Outdoor missing BODY mesh. | build animated GLB/USDZ and real viewer only after upstream BODY/BALL/RKT artifacts are trustworthy |

Current CVAT reviewed-label package (2026-06-30 local artifacts):

- Active imported clips are Burlington, Wolverine, and Outdoor only. The Indoor source video exists locally, but the required Indoor CVAT `for video 1.1` export is still missing.
- Outdoor is capped at source frames `0..1150`; frame 1151 onward is intentionally discarded from the trimmed video, imported annotations, and all generated datasets.
- Canonical manifest: `runs/cvat_imports/2026_06_30/manifest.json`.
- Imported labels now include CVAT `ball` ellipses converted to detector boxes: 8080 player boxes, 2211 paddle boxes, and 1524 ball boxes across the three active clips. The paddle boxes are useful for detector training and RKT crop/candidate review only; they are not true paddle-face corner labels.
- Generated CPU-ready YOLO datasets exist for `player`, `paddle`, `ball`, and `combined` under `runs/cvat_imports/2026_06_30/yolo_datasets/`, split by clip with Outdoor as validation. These are dataset/training inputs only; they do not mark TRK, BALL, RKT, BODY, or replay gates as verified.

2026-06-30 overnight manager evidence:

- A100 runtime diagnostic is recorded at `runs/gpu_diagnostics/a100_runtime_20260630_codex.json`: CUDA/PyTorch can use the idle A100, but the VM is still diagnostic-only for rows that name H100 gates. A bounded A100 substrate run at `runs/a100_substrate_20260630T0809Z/` confirms the practical A100 path can run full Outdoor 0-1150 TRK/player replay through `/home/arnavchokshi/pickleball/.venv` under the GPU lease, but the candidate is not promotable: IDF1 0.3230, MOTA 0.2527, precision 0.6451, recall 0.6629, and 204 ID switches over 1151 processed frames.
- A100 player-detector smoke is recorded at `runs/a100_detector_eval_20260630/summary.json`: YOLO26n trained for 2 epochs on the CVAT player dataset (900 train images, 1151 Outdoor validation images) and measured precision 0.765, recall 0.554, mAP50 0.670, and mAP50-95 0.228. This is useful data feedback only, not TRK promotion.
- A100 tiled/player-trained candidate scoring is recorded at `runs/a100_trk_candidate_score_20260630T081736Z/scoring/person_track_gt_scoring_report.json`: the synced remote candidate covers Outdoor frames 0-1150 and scores IDF1 0.4482, MOTA 0.1491, precision 0.6150, recall 0.5092, 185 ID switches, 1428 FP, 2199 FN, and 592/1027 exact four-player frames. It improves over the quick full-frame A100 substrate replay but remains `diagnostic_only_do_not_promote`; blind repair has now been tried and the next action is source-only four-player selection/ReID from the raw `tracked_detections.json` pool, then rescore.
- A100 tiled repair/pruning is recorded at `runs/phase2/trk_a100_tiled_repair_20260630T082849Z/`: the best non-GT repair attempt (`repair_tracks_default`) improves switches 185 -> 76, FP 1428 -> 1094, precision 0.6150 -> 0.6537, and MOTA 0.1491 -> 0.1998, but IDF1 only moves 0.4482 -> 0.4485 while recall drops 0.5092 -> 0.4609 and exact four-player frames fall 592/1027 -> 229/1027. It remains `diagnostic_only_do_not_promote`. A CVAT-label-informed ceiling reaches IDF1 0.8596 with zero FP/switches from the raw tracked-detections pool, but it uses labels and is review-only; the actionable next path is a tested source-only four-player selection/ReID repair from `tracked_detections.json`.
- Source-only seeded gap-fill from `tracked_detections.json` is implemented and tested in `threed/racketsport/player_source_selection.py` and `scripts/racketsport/select_source_person_tracks.py`; results are recorded at `runs/phase2/trk_source_selection_20260630T084549Z/`. The best source-only variant nudges IDF1 0.4482 -> 0.4515 and MOTA 0.1491 -> 0.1527, with recall 0.5092 -> 0.5132, but switches remain 185, exact four-player frames remain 592/1027, FP rises 1428 -> 1430, and off-court FP rises 0 -> 2. `promote_trk=false`; do not feed BODY from this candidate.
- Global source-only four-player selection from `tracked_detections.json` is implemented and tested in the same module/CLI; results are recorded at `runs/phase2/trk_global_source_selection_20260630T085520Z/`. The best fail-closed global variant scores worse than baseline (IDF1 0.4141, MOTA 0.1426, 230 switches, 535/1027 exact-four frames). The best cardinality variant improves exact-four frames to 703/1027 but adds 109 off-court FP and 293 switches. `promote_trk=false`; the next TRK step is a stronger embedding-aware selector/repair or detector/tracker retraining, not feeding BODY from these tracks.
- Source-only appearance/ReID diagnostics are recorded at `runs/phase2/trk_reid_diagnostics_20260630T090857Z/`: no crop folders, image files, embedding files, or embedding-like detection keys were found in the A100 candidate pool. The diagnostic exported 225 sampled source crops, eight crop sheets, and CPU color-histogram pairwise distances, but it is `source_only_appearance_diagnostic` and `promote_trk=false`; it proves the next promotable ReID step needs real embedding generation/persistence or retraining, not another geometry-only selector.
- The TRK/ReID next-step inventory at `runs/phase2/trk_reid_next_step_20260630T092540Z/` confirmed the A100 raw pool has 6918 detections across 9 source track IDs and exported 115 CPU crop/color samples plus 8 nonblank crop sheets. It still found 0 persisted embedding-like keys. The exact A100 detector checkpoint was recovered locally at `runs/detect/runs/gpu/train_player_yolo26/a100_player_yolo26n_20260630_044525/weights/best.pt` with sha256 `511fcd11a37bae0f9f20bc66b02876b6a3e1e7e678fa24076be157b7c144e11f`.
- A bounded learned ReID export is recorded at `runs/phase2/trk_reid_embedding_export_20260630T171133Z/`: `person_reid_embeddings.json` contains 6,918 detections, 384D L2-normalized YOLO26n crop embeddings, the same checkpoint sha256 `511fcd11a37bae0f9f20bc66b02876b6a3e1e7e678fa24076be157b7c144e11f`, and `promote_trk=false`. The selector/CLI now consumes this export as a weak diagnostic appearance cost, joined by frame/source-track/detection index with raw-coordinate bbox sanity via `embedding_bbox_scale=2.0` for the existing scaled calibration. The scored sweep at `runs/phase2/trk_embedding_source_selection_20260630T223103Z/` does not promote TRK: the best `source_global_four_player_embedding_w10_seed_prior_heavy` variant improves the prior global seed-prior baseline from IDF1 0.4141 / 230 switches to IDF1 0.4314 / 115 switches, but four-player coverage stays 535/1027 (0.5209), spectator/background FP remains 1379, and required Burlington/Wolverine clips are missing from the candidate set.
- TRK embedding analysis is recorded at `runs/phase2/trk_reid_embedding_analysis_20260630T_goal_continuation/`, with the implementation guardrail handoff at `runs/phase2/trk_reid_embedding_consumption_plan_20260630_goal_continuation/`: the full export's track-centroid cosine min/mean/max is 0.963704/0.986169/0.999300, so embedding-only repair is unsafe. The current consumer keeps court/source/same-frame/overlap/cardinality constraints in force and is scored diagnostic-only; the next TRK step is stronger appearance-aware repair or detector/tracker training over the same source pool, followed by full labeled scoring before promotion is even considered.
- CVAT import sanity is recorded at `runs/cvat_imports/2026_06_30/coverage_sanity_report.json`: source zips, videos, manifests, imported labels, validation counts, and YOLO exports agree for the three active clips. It still reports Indoor pending, 74 outside shapes, 12 Wolverine out-of-image visible ball boxes, and Outdoor capped at frame 1150. The DATA-1 substitute report at `runs/cvat_imports/2026_06_30/data1_substitute/manifest.json` remains `blocked_missing_data1_inputs`: source videos are 4/4 present, CVAT exports and reviewed-box imports are 3/4 present, Indoor is missing `cvat_upload/exports/04_indoor_doubles_fwuks_0500_long_mid_baseline_cvat_for_video_1.1.zip`, and 43 inputs are missing. Its 36 generated label skeletons are all marked `not_ground_truth=true`. The Indoor recovery report at `runs/cvat_imports/2026_06_30/data1_substitute/indoor_missing_export_report.json` inspected 52 deduped zip files across the repo, run directory, `~/Downloads`, and `~/Desktop`; the only close CVAT-for-video matches were Burlington, Wolverine, and Outdoor, including the generic `task_2380788` download that proved to be Outdoor.
- The DATA-1 next-step inventories at `runs/cvat_imports/2026_06_30/data1_next_step_20260630T092447Z/` and `runs/cvat_imports/2026_06_30/data1_next_step_20260630T170631Z/` narrow the blocker: `cvat_upload/04_indoor_doubles_fwuks_0500_long_mid_baseline_30s.mp4` exists (900 frames at `30000/1001` fps), but the expected Indoor export zip is absent. A later search found 58 ZIP files total and exactly 6 CVAT `annotations.xml` ZIPs, all duplicate copies of the three existing exports with task sizes 300, 600, and 1800. The goal-continuation recheck at `runs/cvat_imports/2026_06_30/data1_indoor_export_recheck_20260630_goal_continuation/` inspected 56 ZIPs, found the same 6 CVAT XML ZIPs, and found 0 exact/equivalent Indoor matches. DATA-1 remains blocked on the missing 900-frame Indoor CVAT-for-video export and the 43 exact inputs.
- TRK diagnostic-only scoring is recorded under `runs/phase2/person_track_gt_scoring_20260630/`: before/after reports now include identity-switch event extraction, transition summaries, and temporal coverage diagnostics. `TRK_OUTDOOR_PLAYER_ID_DIAGNOSTICS_20260630.json/md` keeps status `diagnostic_only_no_trk_promotion`: the best full required-clip source has mean IDF1 0.6397, worst IDF1 0.4983, 82 ID switches, 1222 spectator/background false positives, 877 off-court false-positive frames, and mean/worst four-player coverage 0.7964/0.5842. Outdoor's best-IDF1 source predicts frames 0-599 while CVAT spans 0-1150, leaving 551 GT frames and 2080 GT player boxes after the last prediction; no TRK promotion.
- TRK review-only coverage proxy is recorded at `runs/phase2/trk_outdoor_cvat_review_proxy_20260630/`: CVAT-derived proxy outputs cover Burlington 600/600 labeled frames, Wolverine 300/300, and Outdoor 1151/1151 with 4480 proxy detections. Outdoor has 1027 expected-four-player frames and 124 frames missing the expected four-player count. This proxy is `review_only=true`, `not_ground_truth=true`, `model_prediction=false`, `not_gate_verified=true`, and `promote_trk=false`; it exists only to make the full-horizon label coverage/blocker explicit.
- BALL CVAT benchmark history is recorded at `runs/eval0/prototype_gate_h100_v2/ball_tracker_benchmark/cvat_ball_horizon_candidate_benchmark_20260630.json`: the earlier best no-click candidate had F1@20 0.558 and 0 teleports, but evaluated only 1304/1524 visible ball labels because Outdoor labels after frame 599 were outside the candidate horizon.
- BALL A100 full-CVAT evidence is recorded at `runs/ball_a100_runtime_20260630T090538Z/` and `runs/ball_a100_full_cvat_20260630T0925Z/`: the A100 is usable for CUDA, upstream TrackNetV3 is cloned at commit `77c123ad4dd449b7d275f16cc43f316ba5b54042`, and the official checkpoint archive is hash-verified (`TrackNet_best.pt` sha256 `df867641a02712b021f04548ff4b1208ddfdb47f629ab2094ceb978667e83b1a`, `InpaintNet_best.pt` sha256 `5749b66b8002f3ad9e0af841604004706fc796df30599e6bf01952696009688c`). The full-CVAT TrackNetV3 run processed Burlington 600 frames in 106.09 s, Wolverine 300 frames in 54.62 s, and Outdoor 1151 frames in 198.08 s, then benchmarked all 1524 visible CVAT labels. Raw A100 TrackNet scores F1@20 0.554, precision@20 0.508, recall@20 0.610, hidden-FP rate 0.607, and 61 teleports. Label-free post-processing at `BALL_A100_FULL_CVAT_POSTPROCESS_REPORT.md` does not fix the gate: the best FP-suppressed temporal-path variant drops hidden-FP rate to 0.025 but recall@20 to 0.093/F1@20 0.157; localtraj/ballistic variants remove teleports but collapse recall. This is runtime/evaluation proof only, not BALL verification.
- BALL dense TrackNetV3 CVAT label preparation is recorded first at `runs/ball_tracknet_cvat_dataset_20260630T171321Z/` and then materialized at `runs/ball_tracknet_cvat_dataset_20260630T175124Z_materialized/`: the active three clips have 2,051 reviewed frames, 1,524 visible ball labels, and 527 explicit hidden-negative frames, split Burlington+Wolverine train vs Outdoor validation, with frame PNGs and medians present for TrackNetV3 training. A bounded A100 resume fine-tune from the official TrackNet checkpoint wrote remote `TrackNet_best.pt` sha256 `fe6701fb0f1a63be4b8e9463cd5c39dfa78ea5c4e8001daf469383c2b9dc6cba`; local converted predictions and the CVAT benchmark live at `runs/ball_tracknet_finetune_20260630T1812Z/`. The after metric improves but is not a gate pass: F1@20 0.626, precision@20 0.606, recall@20 0.647, hidden-FP rate 0.342, and 2 teleports over all 1,524 visible labels.
- BALL fine-tune failure shape from the same benchmark is explicit at `runs/ball_tracknet_finetune_20260630T1812Z/error_analysis_20260630_goal_continuation/`: Burlington reaches F1@20 0.867 but fires on 77/93 hidden frames and has one teleport; Wolverine reaches F1@20 0.599, fires on 53/53 hidden frames, has one teleport, and has p95 spatial error 250.7 px; Outdoor validation is the highest-leverage quality failure with F1@20 0.465, 410 FN, 418 FP, p95 spatial error 214.9 px, and no teleports. The directory includes annotated `review_sheets/` PNGs plus `hard_negative_plan/`. The materialized hard-negative dataset at `runs/ball_tracknet_cvat_dataset_20260630T211745Z_hard_negative_context8_repeat3_materialized/` keeps Outdoor held out and adds 15 generated oversample windows / 5 unique windows / 480 oversampled frames. A fresh A100 resume from the epoch-19 best checkpoint was stopped after the epoch-20 validation signal degraded, then scored at `runs/ball_tracknet_hardneg_finetune_20260630T2130Z/cvat_benchmark/tracknet_hardneg_epoch20_vs_cvat_20260630T2200Z.json`: aggregate F1@20 is 0.569, precision@20 0.555, recall@20 0.583, hidden-FP rate 0.342, and 1 teleport. Burlington improves to F1@20 0.906 and Wolverine to 0.660, but held-out Outdoor falls to F1@20 0.296 / hit recall 0.340, so the hard-negative-only iteration is rejected and not promotable. The follow-up CPU local-search postprocess at `runs/ball_local_search_postprocess_20260630T225735Z/` evaluates all 1,524 labels but is also rejected: dense local-search postprocess regresses to F1@20 0.487 and hard-negative local-search postprocess regresses to F1@20 0.502, with teleports increasing to 10 and 18 respectively. `BallStageRunner(ball_physics3d=True)` can now apply CPU bounce detection only when existing BALL frames already carry `world_xyz`, emitting `ball_physics3d_summary.json`; the current local physics3d run at `runs/local_ball_committed_eval_20260630/wolverine_mixed_0200_mid_steep_corner_stage_local_search_physics3d/` is `insufficient_world_xyz_samples` with `sample_count=0`, 0 bounces, and `applied_to_ball_track=false`, so this hook is not a BALL/PHYSICS verification gate.
- CAL no-tap diagnostics are recorded at `runs/pickleball_pretraining/court_keypoint_20260628/court_keypoint_no_tap_calibration_report_20260630_codex.json`, `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_cal_lead/`, `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_cal_validation_eval/`, `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_goal_continuation_cal_cpu_eval/`, and `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_cal_lead_verify_current/`: at the 0.5 evaluator threshold, CPU and A100 diagnostics both produce 0 confident keypoints, and the latest CPU rerun again produces no solvePnP samples at thresholds 0.5/0.12/0.1 for any active clip. At threshold 0.05, Wolverine still has reprojection p95 1747.694 px and world p95 143.691 m, Outdoor has reprojection p95 610.941 px and temporal p95 138.817 px, and Indoor has reprojection p95 1643.100 px and temporal p95 349.312 px. The overlay review at `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_cal_lead_verify_current/overlay_review/` shows Wolverine/Indoor border collapse and Outdoor mid-image clustering against the trusted calibration projection. Every active clip still blocks on reprojection/world/temporal/line/border sanity. No reprojection, temporal, or CAL-3 gate has passed.
- BODY runtime diagnostic is recorded at `runs/body_runtime_diagnostics/body_runtime_diagnostics_20260630.json`: the BODY gate remains blocked. Burlington/Wolverine/Indoor have scheduled mesh-smoke frames only, while Outdoor schedules 0 world-mesh frames because current player coverage is routed to manual review / skipped reasons instead of trusted BODY scheduling.
- BODY scheduling impact from the A100 TRK candidate is recorded at `runs/body_schedule_from_a100_trk_20260630T090000Z/`: current canonical Outdoor remains 0 scheduled BODY frames / 0 player-frames, while the candidate-local diagnostic schedules 154 BODY frames / 596 player-frames across 11 deep-mesh windows. BODY inference was not run, and the candidate does not unblock BODY because its source TRK remains `do_not_promote`, contact windows lack trusted `player_id`, and world-MPJPE/full-clip gates are still missing.
- BODY unblock evidence at `runs/body_unblock_20260630T092502Z/` recorded the old H100-layout blocker, but the later A100 guarded per-player runs used the available home-path A100 BODY runtime and wrote real Fast SAM-3D-Body outputs at `runs/body_joint_goal_smoke_20260630T001407/`. Those runs prove structural BODY output is available for Burlington and Wolverine safe scheduled player-frames, not BODY verification: both current per-player gate reports have clean structural quality and review overlays with 0 projection/alignment failures, but fail full-clip BODY/world-MPJPE due below-threshold safe-track coverage and missing labels. A live recheck at `runs/body_unblock_20260630T092502Z/live_recheck_20260630T170731Z.json` is now superseded for A100 Burlington/Wolverine runtime availability but remains relevant for canonical promotion blockers: H100 is absent, canonical Outdoor still schedules 0 world-mesh frames, and TRK is not canonical-safe.
- RKT/RPL/SHOT status is recorded at `runs/cvat_imports/2026_06_30/rkt_shot_replay_lane_audit_20260630T170539Z/rkt_shot_replay_lane_audit.json` and the goal-continuation rerun under `runs/cvat_imports/2026_06_30/rkt_shot_replay_lane_audit_20260630T_goal_continuation/`: overall status is `blocked_not_production_ready`. It confirms 2211/2211 RKT candidate frames are box-derived rectangles, 0 true-corner/reference frames, 0 promoted canonical pose frames, 4/4 replay clips are review-visual-ready but 0/4 production-ready, and SHOT has 34 review predictions but 0 reviewed pickleball shot truth labels. The companion `racket_model_runtime_readiness.json` is `blocked` with 0/6 model components runtime-ready, `asset_ready=false`, `may_run_gpu_smoke=false`, and `may_promote_rkt=false`.
- iOS status is recorded at `runs/ios_diagnostics/2026-06-30_record_first_device_smoke.md`, `runs/ios_diagnostics/2026-06-30_record_first_locked_device_pull.md`, and `runs/ios_diagnostics/2026-06-30-record-first-capture-smoke.json`: SwiftPM tests pass, generic simulator build/test-build pass, and prior physical-device build/install passed on the paired iPhone 14 Pro. The latest device smoke is blocked harder because the phone is unavailable/offline to CoreDevice, no enabled simulator devices are installed, and no `Documents/captures`, `clip.mov`, or `capture_sidecar.json` has been pulled from a real record/stop session.

Latest live BODY diagnostic update: Burlington `a100_body_video_smoke_burlington_bboxscaled_resetcap075_runtime_20260701T001500Z` and Wolverine `a100_body_video_smoke_wolverine_bboxscaled_resetcap075_runtime_20260701T002500Z` now have local synced summary JSONs, `body_mesh_readiness.json`, review queue/template/frame assets, compact `body_world_label_packet.json`, packet-quality sidecars at `body_joint_quality_from_packet.json`, selected overlay images, fail-closed label-finalization reports, and `runs/body_joint_goal_smoke_20260630T001407/body_gate_report_resetcap075.json` / `.md`. Both structural full-clip gates pass, both quality reports have empty blockers/warnings, both packet-quality sidecars have no blockers over all 265 / 163 packet samples, and both selected overlay bundles have 0 selected overlay alignment failures. The regenerated report surfaces compact packet quality as `warning` for both clips due to missing reset metadata in current synced packets and missing mesh vertices, and it surfaces BODY label review as `blocked_finalization` with 27 / 20 selected samples but 0 accepted samples in the draft templates. The finalization reports include `selected_samples_not_all_accepted`, `selected_samples_have_overlay_warnings`, and draft/not-ground-truth blockers; Burlington has 9 overlay-warning selected samples and Wolverine has 2. It also carries severity-sorted overlay `warning_samples` plus `competing_player_warning_count`, and the Markdown now includes label-finalization blocker and overlay-warning sample tables with sample IDs, containment ratios, center deltas, competing-player IDs, and overlay image paths. `scripts/racketsport/review_input_server.py --port <port>` now exposes the BODY warning-overlay queue at `/body` and saves bounded visual `body_world_label_review` decisions; those decisions are overlay review only, not world-MPJPE labels. `scripts/racketsport/merge_body_world_label_review_decisions.py` can merge saved `/body` decisions into a corrections manifest, but it only accepts/rejects overlay warning status/note corrections and intentionally leaves sample acceptance, template trust, and predicted `joints_world` corrections pending. `scripts/racketsport/apply_body_world_label_review_corrections.py` gives the review loop a safe apply step: only corrections marked `accepted` can update sample acceptance, template trust fields, or overlay `warning_review_status`, and the finalizer plus compact gate still block unresolved overlay warnings. The current Burlington/Wolverine correction manifests in `corrections/inbox/` are prefilled pending queues with 102 / 67 pending corrections and 0 accepted corrections, so they return `no_accepted_corrections` and no label samples have been accepted yet. The top Burlington sample is `frame_000047_player_2`, where projected joints drift inside the target box review, but the same-frame competing-player check reports 0 competing-player warnings and is not a proven wrong-track assignment. Both now report `missing_world_mpjpe_gate`, `body_joint_overlay_warning_review_required`, and `body_world_label_finalization_blocked`, so this is review/diagnostic evidence and not BODY promotion.

What is **not** done yet:

- No row is `VERIFIED`.
- No true no-tap court solver has passed. CAL has sidecar/manual PnP and fail-closed semantic evidence plumbing, not trained no-tap calibration.
- BALL/contact is prototype/review plus runtime/eval only. The local review UI saved explicit human contacts in `runs/review_inputs/pickleball_cv_review_latest.json`, promoted canonical `contact_windows.json` now exist for all four prototype clips, and review-alignment is 34/34 within 0 frames. The three-clip CVAT package adds 1524 reviewed ball detector boxes, official TrackNetV3 now covers the full three-clip CVAT horizon on A100, dense TrackNet-style training labels preserve 527 hidden negatives, and the first dense A100 fine-tuned candidate has an after metric. A subsequent hard-negative-only A100 iteration is also scored and rejected because it improves train clips while regressing held-out Outdoor. Quality is still below gate, and no cue-fusion/contact-timing acceptance gate has passed.
- TRK has real runner wiring, H100 candidate diagnostics, and three-clip reviewed player boxes suitable for detector/tracker evaluation. Strict Outdoor/Indoor source-30 candidates are canonical-review candidates only; widened-margin artifacts and the latest switch/temporal-coverage diagnostics remain diagnostic-only, and no labeled IDF1/spectator/ID-switch gate has passed.
- BODY has scheduled-frame H100 mesh smoke for Burlington, Wolverine, and Indoor, plus synced A100 reset-bound bbox-scaled Burlington/Wolverine full-track smokes with 265 / 163 mesh+joint player-frames. Both structural full-clip BODY gates pass for those two diagnostics (coverage 0.974265 / 0.964497), structural quality blockers/warnings are empty, max track-anchor residual is bounded to 0.748754 m / 0.721875 m, packet-quality sidecars from `body_world_label_packet.json` have no blockers, and selected overlay review now has 0 selected overlay alignment failures. `body_gate_report_resetcap075.json` can score reviewed labels from compact `body_world_label_packet.json` predictions once final labels exist and now reports BODY label review `blocked_finalization` for the 27 / 20 selected sample bundles: the templates are still draft/not-ground-truth with 0 accepted samples, selected samples include overlay warnings, and no final label file is written yet. The current report is blocked on `missing_world_mpjpe_gate`, `body_joint_overlay_warning_review_required`, and `body_world_label_finalization_blocked`. Outdoor still has no trusted scheduled BODY output.
- Burlington is retired as a court-calibration seed because fisheye curvature bends the court lines. Keep it for player-ID, ball, BODY smoke, paddle/replay, and other non-court QA only.
- RKT is review/scaffold only. Box-derived preview poses are explicitly not canonical paddle 6DoF; true-corner review crop sheets exist but have not been labeled. CVAT paddle boxes can now be converted into review-only candidates, but the readiness gate still reports missing true paddle-face corners, CAD/reference assets, ArUco/AprilTag/reference pose GT, and RKT pose evaluation.
- RPL has parser/static review artifacts only. Static review GLBs are structurally valid and explicitly review-only; the production animated GLB/USDZ export and real Three.js/R3F free-viewpoint viewer are not built.
- The iOS Swift package has scaffold/unit-test coverage, and IOS-1 has partial AVFoundation preview/recording runtime code. The capture path now has a landscape readiness/start-recording gate in code, basic estimated `capture_sidecar.json` writing, and the shared Xcode scheme has a minimal hosted `PickleballAppTests` target for camera-free app state coverage. Full simulator test execution on this machine is blocked by host disk space, and trusted ARKit/manual sidecar capture, live-frame feed, fps/shutter/luminance, codec behavior, ARKit/Vision/CoreML/RealityKit/upload, and physical-device gates remain scaffold-level.

Critical next build order:

1. Keep this truth snapshot, `IMPLEMENTATION_PHASES.md`, and generated readiness reports synchronized before any push.
2. TRK: build and verify a canonical-safe full-horizon player coverage fix. For Outdoor, the source-only selector family has now been tried and remains diagnostic-only, and the learned embedding export has now been consumed by a weak-cost selector sweep that still fails gates. The next candidate should use stronger embedding-aware repair or improve detector/tracker training over the same `tracked_detections.json`/CVAT Outdoor setup. Label-informed oracle scores are diagnostic targets only and must not construct the candidate.
3. BODY: do not run BODY from `do_not_promote` tracks except as an explicitly marked diagnostic. The latest Burlington/Wolverine A100 diagnostics now pass structural full-clip BODY coverage and selected-overlay alignment checks, and the compact packet gate can score finalized labels without copying huge motion files locally, but missing world-MPJPE still blocks promotion. Repair/promote TRK continuity/coverage for the broader clip set, then add representative world-MPJPE labels or an equivalent evaluator.
4. RKT: consume the separate racket-annotation workflow's exported true paddle-face corners, masks/keypoints, CAD/reference pose, or ArUco evidence when available; do not duplicate that work in the local crop-sheet wizard unless the external workflow stalls. Do not promote box-corner candidates.
5. DATA-1: promote real reviewed labels/assets for the accepted clips; do not treat validators or run artifacts as the dataset.
6. BALL: official TrackNetV3 checkpoint acquisition, tiny smoke, full-CVAT A100 generation, dense hidden-negative TrackNet CVAT labels, frame materialization, a first A100 dense fine-tune/after benchmark, a hard-negative materializer plus rejected A100 hard-negative resume, and a rejected CPU local-search postprocess are done. The next useful path is Outdoor localization/recall or a different model-side candidate such as PB-MAT/TrackNetV4/V5; current label-free filters, the first dense fine-tune, the hard-negative-only resume, and local-search postprocess still cannot reach the reviewed F1/contact gates.
7. CAL: keep semantic court evidence enforced in pipeline contracts, then fix/train the no-tap solver against real labeled geometry. The threshold sweep now proves low-threshold correspondences alone are insufficient because reprojection/world/temporal validation still fails.
8. RPL/report delivery: keep static review GLBs as visual QA only; build production animated GLB/USDZ, real viewer, and tiered delivery after upstream artifacts are trustworthy.
9. iOS: unlock the paired iPhone, create a real 3-5 second Record/Stop package, pull `Documents/captures`, and verify `clip.mov` plus `capture_sidecar.json` before claiming the physical capture gate.

---

## 1. Coordination protocol (mandatory)

### Status values
`TODO` → `CLAIMED` → `IN-PROGRESS` → `IN-REVIEW` → `DONE` → `VERIFIED`. Plus `SCAFFOLD` (code/test primitives exist, but the named model/algorithm is not actually running through the pipeline), `BLOCKED` (with reason), `PENDING-APPROVAL` (awaiting the human on a decision — see §1.6), and `PROTOTYPE-GATE` (temporary accepted-four lowered gate for this prototype wave).

- **TODO** — not started, available to claim (if its dependencies are `VERIFIED`).
- **CLAIMED** — an agent has taken it; Owner set. No one else may touch it.
- **IN-PROGRESS** — actively being built.
- **SCAFFOLD** — CPU-safe plumbing, schemas, adapters, validators, docs, or unit-tested primitives exist, but the row's promised model/algorithm/real-world behavior is not actually invoked through the spine. It is useful implementation surface area, not feature completion.
- **BLOCKED** — cannot proceed; the `Notes` column states why and what's needed.
- **PENDING-APPROVAL** — a model/variant (or other non-obvious) decision is waiting on the human; comparison videos rendered and linked. Does NOT unblock downstream until approved (§1.6).
- **IN-REVIEW** — built; the task's own test passes; awaiting the lead's gate check.
- **DONE** — code complete and task self-test green. If the row names a model or algorithm from `TECH_STACK.md §2.3`, `DONE` additionally requires that the exact model/variant/weight is actually invoked through a registered `StageRunner` in `threed/racketsport/orchestrator.py`, and that at least one test exercises that real path rather than a fabricated payload or precomputed adapter output.
- **VERIFIED** — the lead agent ran the phase **Acceptance Gate** (from `IMPLEMENTATION_PHASES.md`) and it passed. Only `VERIFIED` unblocks downstream tasks.
- **PROTOTYPE-GATE** — temporary lowered acceptance scope approved by the human for the current prototype wave. It can unblock prototype work on the named clips but does not replace full `VERIFIED`.

### Rules
1. **Claim before you build.** Edit the task row: set `Owner` + `Status=CLAIMED`, commit `claim <TASK-ID>`. One owner per task at a time.
2. **Respect dependencies.** Do not start a task until every task in its `Deps` is `VERIFIED`, except for the current accepted-four prototype packet where a dependency explicitly marked `PROTOTYPE-GATE` may unblock only the named prototype artifacts. If you need something not yet ready, pick another `TODO` whose deps are met.
3. **Own only your files.** Each task lists the files/modules it owns. Do not edit files owned by another `CLAIMED`/`IN-PROGRESS` task — coordinate via a `BLOCKED` note instead.
4. **Update status as you go.** `CLAIMED`→`IN-PROGRESS` when you start; `BLOCKED` with a reason if you stall; `IN-REVIEW` when your task's test passes.
5. **Finish with a handoff.** On completion: set `Status=DONE`, fill the **Handoff log** (§5) with: what you built, the artifacts/files produced, how you tested it, and what the next agent needs to know. Commit `done <TASK-ID>: <summary>`.
6. **Only the lead verifies.** The lead agent runs the phase Acceptance Gate on GPU test clips; pass → `VERIFIED`; fail → back to `IN-PROGRESS` with failing-gate notes.
7. **No phase advances on red.** A downstream phase's tasks stay `TODO` until their upstream gate tasks are `VERIFIED` (phase-gate discipline from `IMPLEMENTATION_PHASES.md §0.1`), except for artifacts explicitly scoped to the current `PROTOTYPE-GATE`.
8. **Log decisions.** Any deviation, model swap, or assumption goes in the **Decisions log** (§6) so others see it.
9. **Keep commits scoped to one task** and prefix the message with the TASK-ID.

### Definition of Done (global)
A task is `VERIFIED` only when: code merged, its module unit tests pass, the phase Acceptance Gate (numeric, on real GPU test clips spanning the varied-camera matrix — run **through the §1.5 GPU lease/queue**) passes, artifacts conform to the JSON schemas in `IMPLEMENTATION_PHASES.md`, the Handoff log entry is written (incl. the recorded gate metric), **and any model/variant choice it depends on has cleared the §1.6 approval gate.**

For rows that name a model or algorithm, `DONE` is intentionally stricter than "module tests pass": the row must run the named technology through the registered pipeline spine with the correct manifest weight/variant. A smoke, probe, scaffold, precomputed artifact adapter, or presence-count gate remains `SCAFFOLD`/`PROTOTYPE-GATE` until that real path exists.

### 1.6 Decision approval gate — human-in-the-loop on variant choices (mandatory)

**Whenever a task must choose between candidate models/variants (EVAL-0 and any per-stage variant pick), it does NOT silently finalize.** It must let the human compare and approve, *unless one option is obviously better.*

1. **Render comparison videos.** For each candidate, render its ACTUAL output overlaid on the **same ≥3 real test clips** spanning the varied-camera matrix (e.g., detection boxes / pose skeleton / SMPL mesh / ball track / foot-lock before-after / racket 6DoF / replay), as **side-by-side comparisons**, each labeled with the candidate name + its measured accuracy-gate metric + latency + VRAM. Save to `runs/eval0/<stage>/compare/` and write both `runs/eval0/<stage>/variant_selection.md` (human-readable metrics table + links) and `runs/eval0/<stage>/variant_selection.json` (machine-readable source for indexing).
2. **Auto-finalize ONLY if obvious.** The agent may lock a variant on its own — logging it in the **Decisions log (§6)** as `auto-finalized (obvious)` and still saving the videos — only when the winner is unambiguous: it **passes the gate AND is Pareto-dominant** (≥ as accurate AND ≥ as fast/cheaper than every alternative), OR every alternative **fails the gate** while it passes, OR it beats all others by a **large, clear margin** (e.g., well past the gate with no meaningful speed/VRAM penalty).
3. **Otherwise BLOCK on the human.** Set the task `Status = PENDING-APPROVAL`, note the links to `variant_selection.md`, `variant_selection.json`, and the comparison videos in the `Notes` column and the Handoff log, and **stop** — do not lock the variant, do not start dependents that need the final pick. The human reviews the videos and chooses.
4. **On approval:** record `approved: <variant> by <human> on <date>` in the Decisions log, lock the choice in `models/MANIFEST.json`, set status forward (`IN-REVIEW`/`VERIFIED`).
5. **Scope:** this applies to every model-variant decision and to any *other* non-obvious decision with close alternatives (e.g., a calibration method, a smoothing filter) — surface comparison artifacts and request approval rather than silently picking. Obvious/no-alternative choices proceed normally.

---

## 1.5 GPU coordination — ONE shared H100 (mandatory)

**All agents share a single NVIDIA H100 80GB (GCP A3) for testing/training.** Naive parallel GPU use → OOM/crashes. The rule set below lets CPU work run fully parallel while GPU work is isolated and serialized.

### The #1 rule: CPU work never touches the GPU lease
Tasks tagged **[CPU]** (code, schemas, data download/convert, iOS Swift app, web viewer, CPU post-processing, MuJoCo-CPU, report formatting) run **immediately and in parallel** — they must never block on the GPU. Only **[GPU]** tasks (model training/fine-tune, heavy server inference, and every eval or test run on test clips) acquire the lease. (iOS on-device models run on the *phone's* Neural Engine, not this H100 — so IOS-* tasks are [CPU] w.r.t. this lease, except Core ML conversion/validation.)

### Static MIG geometry target (manual/external setup)
Pick ONE externally configured mode per session; the checked-in helpers consume slot files and locks, but they do not create, drain, reset, or restore MIG geometry:
- **Eval/dev mode (default): `2 × 3g.40gb`** — the inference pipeline + parallel eval agents each get hardware-isolated 40 GB (a buggy agent OOMs only its own slice, not its neighbors). Use **MIG, not MPS** (autonomous agents need hard fault/memory isolation).
- **Training mode: `1 × 7g.80gb`** — one exclusive holder fine-tunes (SAM-3D-Body fine-tune needs ~40–55 GB → full GPU); eval agents pause.

### Lease/queue convention (local disk, `flock` — NOT NFS)
```
/run/gpu-lease/
  full-gpu.lock        # exclusive; held by the one training/full-GPU job
  slots/slotN.lock     # one flock per MIG eval instance
  slots/slotN.uuid     # MIG-<uuid> for CUDA_VISIBLE_DEVICES
  heartbeat/<pid>      # pid+slot+epoch ts while a helper command is running
```
- **[GPU] eval or inference task:** `scripts/gpu-eval-run.sh` takes a shared `full-gpu.lock`, scans `slot*.lock` with `flock -n`, exports `CUDA_VISIBLE_DEVICES=$(cat slotN.uuid)`, writes a heartbeat, runs the command, and releases. If no slot is free, it blocks on the first slot lock; `flock` does not promise strict FIFO ordering.
- **[GPU] training task:** `scripts/gpu-train-lock.sh` acquires exclusive `full-gpu.lock` and runs the command. Any drain, MIG mode switch, or restore step is manual/external orchestration before/after this helper.
- **Heartbeat files are observability only** in the checked-in scripts; there is no TTL watchdog eviction. **Monitor with DCGM** (`dcgmi dmon -e 203,204,252`); stop DCGM before any manual `--gpu-reset`/mode switch (it counts as a GPU client).

Helper (`scripts/gpu-eval-run.sh`): grab a pre-created slot via `flock`, set `CUDA_VISIBLE_DEVICES`, run the command, release — see `IMPLEMENTATION_PHASES.md` Phase 0 for the script.

### VRAM budget (80 GB; estimates — measure SAM-3D-Body FIRST, it paces the pipeline)
| Job | VRAM | Fits |
|---|---|---|
| Full inference pipeline (all models resident) | ~18–28 GB | 3g.40gb slice |
| SAM-3D-Body **inference** (largest single model) | ~10–20 GB | 3g.40gb slice |
| SAM-3D-Body **fine-tune** (AdamW states) | ~40–55 GB | **needs 7g.80gb (training mode)** |
| YOLO26 / RTMW fine-tune | ~8–15 GB | a slice (eval-tier lease) |
| 3 parallel eval agents (YOLO+TrackNet+RTMW, no SAM) | ~20 GB each | 3× 1g.20gb |

### GPU-lease tasks (everything else is [CPU], no lease)
**[GPU]:** **EVAL-0 (variant benchmarking)** · DATA-2, DATA-3, DATA-4 (training/auto-label inference) · CAL-3 (train keypoint net) · TRK-1 (eval runs) · BODY-1, BODY-2, BODY-3, BODY-4 · FOOT-1 (eval) · BALL-1, BALL-2, BALL-4 (train/eval) · RKT-1 (train/eval) · MET-1 (eval) · SHOT-1 (train) · RPL-1 (render bake, if GPU-rendered) · **EVAL-1, EVAL-2, EVAL-4 (all test-clip runs)**. FOOT-2 physics is [GPU] only if using MuJoCo-MJX/PHC training, else CPU.

### Testing is a GPU job
Every phase Acceptance Gate is run on test clips **through this lease/queue**. A task is `VERIFIED` only after its numeric gate passes on the shared H100 and the result is recorded in the Handoff log (§5).

---

## 2. Workstreams & dependency graph

```
ENV ──┬─> IOS (capture/calibration/on-device live/replay viewer) [iOS client track]
      └─> DATA (datasets, auto-label, fine-tune)             [runs alongside]
            |
ENV ─> IOS/CAL LIVE (ARKit seed/cache, capture guidance, on-device person/pose/ball/contact)
            └─> TRK (server detect/track support for offline BODY/report)
                  ├─> BALL (on-device ball spike + server track/audio/events/3D physics)
                  └─> BODY (SERVER OFFLINE Fast SAM-3D-Body + world-grounding)
                        ├─> FOOT (foot-skate kill + physics)
                        ├─> RKT (racket 6DoF; needs BALL for rebound-validate)
                        └─> MET (metrics + insights + confidence)  [needs FOOT, BALL, RKT]
                              ├─> SHOT (shot class + drill verify)
                              └─> RPT (LLM copy + report + viz)
                                    └─> RPL (3D replay: bake USDZ+GLB, RealityKit + web viewers)
EVAL  ── spans everything (harness + per-phase gates + regression CI)
```
Critical path: **ENV → IOS/CAL live slice → TRK support → BALL/contact risk → BODY/FOOT/RKT deep tier → MET → RPT → RPL.** The **DATA** track runs in parallel from ENV.

**Parallelization model (max throughput on one H100):** the lead spawns subagents to work concurrently. **[CPU] tasks run fully in parallel, anytime** (their deps permitting) — IOS app, web viewer, schemas, data download/convert, module scaffolding, CPU post-processing, report formatting. **[GPU] tasks** (training, heavy inference, all test-clip eval) **serialize through the §1.5 lease/queue** (or run concurrently in separate MIG eval slots when they fit). So: keep many CPU tasks in flight; let GPU tasks queue. FOOT and BALL can proceed in parallel once BODY is VERIFIED; SHOT/RPT can proceed once MET is VERIFIED.

---

## 3. Checklist

Columns: **☐** (done) · **ID** · **Task** · **Owns (files)** · **Deps** · **Phase** · **Owner** · **Status**. The **Phase** column maps each task to `IMPLEMENTATION_PHASES.md`: iOS client tasks → **Client Track C1–C3**; server tasks → the named **Pipeline Track phases (0–11)** — go there for the exact build steps + Acceptance Gate. (Phase names, not numbers, are used for server tasks so they stay valid if phases are renumbered.) **GPU vs CPU:** the **[GPU] tasks that must use the §1.5 lease are enumerated in §1.5**; every other task is **[CPU]** and runs in parallel without the lease.

### ENV — Environment & scaffolding
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☑ | ENV-1 | Server env, deps, repo scaffolding under `threed/racketsport/`, `models/MANIFEST.json` | repo skeleton, env files | — | Phase 0 | Codex | DONE |
| ☑ | ENV-2 | Fetch + checksum model checkpoints (incl. verify-before-commit flags + fallbacks) | `models/` | ENV-1 | Phase 0 | Codex | DONE |
| ☑ | ENV-3 | iOS Xcode project scaffolding (`ios/`), Swift package layout | `ios/` | — | Phase 0 | Codex | DONE |
| ☑ | ENV-4 | ffmpeg ingest/QC + capture-quality scoring + decode benchmark scaffolding; representative NvDEC/DALI gate still pending | `threed/racketsport/io_decode.py`, `scripts/racketsport/ingest_testclips.py`, `scripts/racketsport/benchmark_decode.py`, `threed/racketsport/capture_quality.py` | ENV-1 | Phase 0 | Codex | DONE |

### IOS — iOS client (capture, calibration, on-device live, replay viewer)
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | IOS-1 | AVFoundation capture contracts plus partial preview/recording runtime; basic estimated sidecar writing exists, while trusted ARKit/manual sidecar capture, live-frame feed, fps/shutter/luminance measurement, codec validation, and physical-device gates are still missing | `ios/Capture/`, `ios/App/CameraPreviewView.swift`, `ios/App/CaptureViewModel.swift` | ENV-3 | C1 | Codex | SCAFFOLD |
| ☐ | IOS-2 | Calibration sidecar/manual-tap contracts and scaffolding; real ARKit setup pass still needs device runtime implementation and validation | `ios/Calibration/` | IOS-1 | C1 | Codex | SCAFFOLD |
| ☐ | IOS-3 | ON-DEVICE LIVE module scaffolding; real Vision/Core ML person/2D pose/ball/contact preview still needs on-device implementation and latency validation | `ios/FastTier/` | IOS-1 | C2 | Codex | SCAFFOLD |
| ☐ | IOS-4 | Guidance contracts/scaffolding; real corner/exposure/blur/CoreMotion guidance UX still needs device implementation and validation | `ios/Guidance/` | IOS-2, IOS-3 | C2 | Codex | SCAFFOLD |
| ☐ | IOS-5 | Upload contracts/scaffolding; real resumable upload, trim, and sidecar transfer still need implementation and backgrounding tests | `ios/Upload/` | IOS-2 | C1 | Codex | SCAFFOLD |
| ☐ | IOS-6 | Replay module scaffolding; real RealityKit/USDZ playback and free-viewpoint device validation still need implementation | `ios/Replay/` | RPL-1 | C3 | Codex | SCAFFOLD |

### CAL — Court calibration (server)
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☑ | CAL-1 | Court templates (PB/tennis) + zones (NVZ, service boxes) + net plane from regulation geometry | `court_templates.py`, `court_zones.py`, `net_plane.py` | ENV-1 | Court Calibration | Codex | DONE |
| ☑ | CAL-2 | Trusted sidecar/manual-review calibration artifacts plus automatic semantic court-line evidence attempt; video-backed runs fail closed when sport-specific line evidence (pickleball NVZ/centerlines; tennis service lines) plus top-net evidence is not ready | `court_calibration.py`, `court_line_evidence.py`, `court_auto_evidence.py`, `orchestrator.py`, `net_plane.py` | CAL-1, IOS-2 | Court Calibration | Codex | DONE |
| ☐ | CAL-3 | (Phase-2) Train and verify no-tap auto court-keypoint/line solver (fine-tune TennisCourtDetector + synthetic viewpoints) | `court_keypoint_net.py`, `court_line_evidence.py` | CAL-2, DATA-1 | Court Calibration | Codex | SCAFFOLD |

### TRK — Person detection, tracking, doubles ID
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | TRK-1 | Server offline support path: YOLO26m + BoT-SORT-ReID detect/track; court-polygon filter; ground-plane association; N-lock + coach 1-tap anchor. The ON-DEVICE LIVE analogue is IOS-3 with CoreML YOLO26n/s plus lightweight ByteTrack/BoT-SORT and must not be replaced by this server row. Current blocker: canonical accepted-four tracks are sparse; strict H100 source-30 candidates are canonical-review candidates only, widened-margin and switch/temporal-coverage outputs are diagnostic_only, Outdoor best-IDF1 coverage stops at frame 599 of CVAT frame 1150, and labeled IDF1/spectator gates have not passed. | `person_fast.py`, `track_lock.py`, `doubles_id.py` | CAL-2 | Person Detection/Tracking | Codex | IN-PROGRESS |

### BALL — Ball tracking + events + 3D physics
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | BALL-1 | Server offline TrackNetV3 (→V5) fine-tuned ball tracking + tap-track fallback; ON-DEVICE LIVE requires a separate distilled/quantized CoreML heatmap tracker at ~288p and that is the top live-tier optimization risk. | `ball_tracknet.py`, `ball_tap_track.py` | CAL-2, DATA-3 | Ball + Events | Codex | SCAFFOLD |
| ☐ | BALL-2 | Server offline audio "pop" two-stage detector (onset + CNN) + distance-delay correction; ON-DEVICE LIVE uses mic onset timing as the cheap first cue. | `audio_pop.py` | DATA-3 | Ball + Events | Codex | SCAFFOLD |
| ☐ | BALL-3 | Event fusion (audio + wrist-vel peak + ball inflection) → contact windows; doubles attribution. Current review-alignment report proves saved human contacts match promoted windows, but machine cue fusion and BALL label/timing gates have not passed. ON-DEVICE LIVE contact timing is the mic-onset + wrist-velocity peak subset only. | `event_fusion.py` | BALL-1, BALL-2, BODY-2 | Ball + Events | Codex | SCAFFOLD |
| ☐ | BALL-4 | Server offline 3D ball physics (EKF + RANSAC parabola + z=0 bounce + Magnus + pickleball aero). ON-DEVICE LIVE line calls stay cheap: homography + bounce on cached court, using known Z=0 to resolve single-cam depth. | `ball_physics3d.py` | BALL-1, CAL-2 | Ball + Events | Codex | SCAFFOLD |

Explicit spike task outside the counted table: before more server replay polish, prove or reject an ON-DEVICE LIVE distilled/quantized CoreML heatmap ball tracker at ~288p on a physical iPhone. Required evidence: latency/FPS, hidden false positives, visible-ball recall, thermal behavior, and whether homography+bounce line calls are stable enough for capture guidance. Keep status `SCAFFOLD` until device evidence exists.

### BODY — 3D body mesh (core)
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | BODY-1 | SERVER OFFLINE / deep tier: Fast SAM-3D-Body per player crop (MHR→SMPL via MLP). Current accepted-four outputs are mesh smoke/structural coverage only; Burlington/Wolverine A100 full-track coverage and selected-overlay alignment checks now pass structurally, but BODY gate is still blocked on representative world-MPJPE/accuracy evidence and broader trusted clip coverage. | `hmr_deep.py` | TRK-1 | 3D Body | Codex | SCAFFOLD |
| ☐ | BODY-2 | Our world-grounding: project per-frame to world via known camera + court Z=0 → temporal smooth; current world meshes are scheduled-frame H100 smoke, not accuracy-verified BODY. | `worldhmr.py` | BODY-1, CAL-2 | 3D Body | Codex | SCAFFOLD |
| ☐ | BODY-3 | `server-fast` camera-space mesh preview (SAT-HMR / Multi-HMR 2), explicitly not phone-real-time and not part of ON-DEVICE LIVE. | `hmr_fast.py` | TRK-1 | 3D Body | Codex | SCAFFOLD |
| ☐ | BODY-4 | Racket-motion fine-tune (BEDLAM2→AthletePose3D→CalTennis→RICH→AMASS) + world-MPJPE eval | `scripts/racketsport/finetune_pose.py` | BODY-1, DATA-2 | 3D Body | Codex | SCAFFOLD |

### FOOT — Foot-skate elimination & physics
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | FOOT-1 | Foot-contact detection vs known Z=0 + zero-velocity + CCD-IK foot-lock (≤3 mm, 0 penetration) | `footlock.py` | BODY-2 | Foot-Skate & Physics | Codex | SCAFFOLD |
| ☐ | FOOT-2 | Physics refinement: PhysPT default; PHC/PULSE on MuJoCo+MJX flagship; MultiPhys for doubles | `physics_refine.py` | FOOT-1 | Foot-Skate & Physics | Codex | SCAFFOLD |

### RKT — Racket 6DoF
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | RKT-1 | Detect/track paddle with SAM 3 concept prompts when approved, else DINO-X/Grounded-SAM-2; derive true mask/keypoints/corners → FoundationPose/GigaPose/FoundPose CAD or reference pose → grip prior → PnP-IPPE → UKF SE(3) → physics-validate; contact-point + face-normal. Current accepted-four crop sheets need true-corner/GT labeling before any canonical `racket_pose.json`. | `racket6dof.py` | BODY-2, BALL-4, DATA-4 | Racket 6DoF | Codex | SCAFFOLD |

### MET — Metrics, insights, confidence
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | MET-1 | Biomechanics metrics (foot/NVZ, zones, spacing, balance, X-factor, contact point, velocity w/ tiers) | `movement_metrics.py` | FOOT-1, BALL-3, RKT-1 | Metrics & Insights | Codex | SCAFFOLD |
| ☑ | MET-2 | Rule-based insight engine + confidence gating primitives. Product use still waits on MET-1 metrics and real gates. | `insight_rules.py`, `confidence.py` | MET-1 | Metrics & Insights | Codex | DONE |

### SHOT — Shot classification + drills
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | SHOT-1 | Shot classifier path: event scorer, semantic BODY adapter, transfer fallback, CPU centroid sanity baseline, then approved BST/PoseConv3D training after DATA-5 labels | `shot_classifier.py`, `shot_transfer_baseline.py`, `shot_trainable_baseline.py`, `threed/racketsport/eval/shot_event_eval.py` | MET-1, DATA-5 | Shot Classification | Codex | SCAFFOLD |
| ☐ | SHOT-2 | Drill rep counting/verification (wrist-vel peak + contact + state machine) | `drill_verify.py` | MET-1, BALL-3 | Shot Classification | Codex | SCAFFOLD |

### RPT — Report, LLM copy, visualization
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☑ | RPT-1 | Report artifact primitives (`habit_report.json`, `coach_report.json`, correction-exclusion model). Production report delivery still waits on upstream verified metrics and copy gates. | `report_model.py`, `habit_model.py` | MET-2 | Report & Delivery | Codex | DONE |
| ☐ | RPT-2 | LLM coaching copy (latest Claude; facts-in/copy-out; 100% faithfulness gate) | `llm_copy.py` | RPT-1 | Report & Delivery | Codex | SCAFFOLD |
| ☐ | RPT-3 | Visualization payload scaffolds (court map/ghost/overlay metadata); rendered visuals and tiered delivery are still unbuilt | `viz/` | RPT-1 | Report & Delivery | Codex | SCAFFOLD |

### RPL — 3D replay
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | RPL-1 | Server render bake → one animated scene → export USDZ (OpenUSD) + GLB (pygltflib/smplx); MeshOpt+Draco+KTX2; CDN. Current static review GLBs validate structurally but are not animated/production replay. | `replay_export.py` | FOOT-2, RKT-1, BALL-4 | 3D Replay | Codex | SCAFFOLD |
| ☐ | RPL-2 | Web review viewer with video sync, parser coverage, and React Three Fiber / Three.js court-world inspection. Production animated free-viewpoint replay remains blocked on RPL-1 export and upstream BODY/BALL/RKT gates. | `web/replay/` | RPL-1 | 3D Replay | Codex | SCAFFOLD |

### DATA — Data & training infrastructure (parallel)
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | DATA-1 | Test-clip dataset (varied camera-height/angle matrix) + label schema. Current local CVAT package has three reviewed clips and generated detector datasets, but it is not the full 24-clip DATA-1 matrix and does not mark phase gates verified. The DATA-1 substitute manifest is blocked with 43 missing inputs, including the missing Indoor CVAT video export; generated label skeletons are explicitly not ground truth. | `data/testclips/`, `scripts/racketsport/*testclip*.py`, `scripts/racketsport/extract_label_frames.py`, `scripts/racketsport/init_label_workdir.py`, `docs/racketsport/manifests/testclip_seed_manifest.json`, `runs/cvat_imports/2026_06_30/` | ENV-1 | Test-Clip Spec | Codex | IN-PROGRESS |
| ☐ | DATA-2 | Body pose datasets download + fine-tune pipeline + auto-label/distill loop | `data/pose/`, `scripts/autolabel.py` | DATA-1 | Data Infra | Codex | PROTOTYPE-GATE |
| ☐ | DATA-3 | Ball/audio dataset schemas, manifests, and validators; real populated ball/audio assets are still missing | `data/ball/`, `data/audio/` | DATA-1 | Data Infra | Codex | SCAFFOLD |
| ☐ | DATA-4 | Racket data schema/validator scaffolding; real RacketVision/synthetic paddle-CAD/ArUco-GT assets are still missing locally | `data/racket/`, `docs/racketsport/racket_dataset_schema.json`, `scripts/racketsport/validate_racket_dataset.py` | DATA-1 | Data Infra | Codex | SCAFFOLD |
| ☐ | DATA-5 | Shot-class dataset schema/validator plus reviewed-label builder; real populated train/val/test pickleball shot labels are still missing | `data/pb_shots/`, `scripts/racketsport/build_shot_dataset.py` | DATA-1, BALL-3 | Data Infra | Codex | SCAFFOLD |

### EVAL — Validation, gates, CI (spans all)
| ☐ | ID | Task | Owns | Deps | Phase | Owner | Status |
|---|----|------|------|------|-------|-------|--------|
| ☐ | **EVAL-0** | **[GPU] Model variant selection** — benchmark each stage's `TECH_STACK §2.3` candidates on real clips (offline via §1.5 lease; live on device); **render side-by-side comparison videos + get human approval per §1.6 (auto-finalize only if obvious)**; lock the approved variant in `models/MANIFEST.json` (offline=accurate, live=light). Benchmark Fast SAM-3D-Body first. | `models/MANIFEST.json`, `threed/racketsport/eval/`, `runs/eval0/` | DATA-1, ENV-2 | Model Variant Selection | Codex | PROTOTYPE-GATE |
| ☑ | EVAL-1 | Eval harness (`threed/racketsport/eval/`, one evaluator per phase → `metrics.json`) | `threed/racketsport/eval/` | DATA-1 | Cross-cutting | Codex | DONE |
| ☐ | EVAL-2 | Validation protocol harness hardening; remaining phase gates still need real accuracy checks, especially BALL contact timing and full replay/device quality gates | `threed/racketsport/eval/`, `tests/racketsport/test_eval_gates.py` | EVAL-1 | Cross-cutting | Codex | SCAFFOLD |
| ☑ | EVAL-3 | Regression checker tests, script/CLI hygiene workflow, manual baseline/current metric comparison workflow, and corrections-flywheel plumbing. It does not yet universally block merges on live phase drops because persisted CI metric roots are still missing. | `.github/workflows/racketsport-regression.yml`, `scripts/racketsport/check_eval_regression.py`, `tests/racketsport/test_eval_regression.py`, `corrections/` | EVAL-1 | Cross-cutting | Codex | DONE |
| ☐ | EVAL-4 | End-to-end readiness/review spine only; true 20-min clip integration, perf, cost, and final acceptance are still missing | `threed/racketsport/orchestrator.py`, `threed/racketsport/pipeline_contracts.py`, `scripts/racketsport/validate_pipeline_artifacts.py`, `threed/racketsport/eval/e2e_eval.py` | all VERIFIED | End-to-End | Codex | SCAFFOLD |

---

## 4. Phase-gate summary (must pass before dependents start)

| Gate | Numeric acceptance (see `IMPLEMENTATION_PHASES.md` for the exact test) |
|---|---|
| On-device live tier | physical-device court/person/2D pose/capture guidance passes; ~288p ball tracker spike recorded or line/contact cues disabled fail-closed |
| Court calibration | overlay matches ≥8/10 clips across ≥4 distinct viewpoints; feet-to-world within per-viewpoint budget |
| Person track | stable IDs ≥90% on 2-player clips after confirmation; 20-min clip throughput target met |
| Ball + events | ball F1 ≥0.90 (FP <5%); contact timing ≤±2 frames / ±4 ms (audio) |
| 3D body | per-stage world-MPJPE target on EMDB2/CalTennis; fine-tune ladder hits ~50–70 mm |
| Foot-skate & physics | foot-slide ≤3 mm; zero floor & inter-player penetration |
| Racket 6DoF | face-angle ≤5° vs ArUco GT; contact-point ±1–3 cm |
| Metrics/insights | metric accuracy vs manual review; confidence calibration; velocity Protocol A/B/C pass |
| Replay | loads fast; free-viewpoint works; coach "looks right" review passes |
| End-to-end | ON-DEVICE LIVE first screen <10 s; deep-tier p95 under agreed SLA; gross margin target |

---

## 5. Handoff log (append-only — newest last)

> Format: `[TASK-ID] <agent> — built: <what>; artifacts: <files>; tested: <how/result>; next: <what the next agent needs>`
> Historical entries below may contain pre-split labels such as "fast-tier" for server previews or latency gates. Interpret current work by the checklist rows above: ON-DEVICE LIVE is the iPhone ANE during-play tier; `server-fast` is post-upload/offline preview; SERVER OFFLINE is the deep async tier.

- [ENV-1] Codex — historical initial scaffold note: created the first `main` repo, Phase 0 Python package surface under `threed/racketsport/`, artifact schema registry, ffprobe-based clip metadata probe, test-clip ingest script, GPU eval/train lock helpers, placeholder model manifest, Triton scaffold, and web replay package placeholder. Current module truth has moved beyond that initial scaffold shape; use `CAPABILITIES.md` and the current checklist rows for what is real, scaffold-only, or verified. Artifacts: `threed/racketsport/`, `tests/racketsport/`, `scripts/racketsport/`, `scripts/gpu-*.sh`, `models/MANIFEST.json`, `serving/triton/racketsport_ensemble/README.md`, `web/replay/package.json`; tested: local `.venv/bin/python -m pytest -q` passed (5 tests), local ingest smoke wrote `frames_meta.json`, local GPU-lock fallbacks ran, H100 container `/workspace/pickleball/.venv/bin/python -m pytest ... -q` passed (5 tests), H100 `nvidia-smi` via `scripts/gpu-eval-run.sh` showed `NVIDIA H100 80GB HBM3, 81559 MiB, 0 MiB, 0 %`; next: ENV-2 should fill `models/MANIFEST.json` with real checkpoints/checksums and replace the dry-run SAM-3D-Body benchmark with real inference.
- [ENV-2] Codex — built: H100 model manifest verifier, Fast-SAM isolated conda env installer, Detectron2 CUDA build fixes, warm-run benchmark wrapper, and H100 checkpoint inventory for Fast-SAM/SAM assets, MoGe2, SAM2, existing YOLO, YOLO26m, RTMW-l 384, TrackNetV3, TrackNetV3 InpaintNet, and SAT-HMR; artifacts: `scripts/racketsport/smoke_models.py`, `scripts/racketsport/install_fast_sam_env.sh`, `scripts/racketsport/run_fast_sam_benchmark.sh`, `tests/racketsport/test_smoke_models.py`, `models/MANIFEST.json`; tested: H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (18 tests), H100 `smoke_models.py --check-files-only` verified file presence and sha256 integrity for all 10 then-declared `available_on_h100` checkpoint files, Fast-SAM-3D-Body profiler ran on H100 with one warmup + five measured warm runs: average 326.91 ms, std 1.72 ms, min 324.68 ms, max 329.90 ms, peak allocated GPU memory 4718.04 MB; next at that time: `mujoco_mjx` was still pending because it is a runtime package/physics stack rather than a simple checkpoint file, and EVAL-0 still needed real test clips plus comparison artifacts before any model choice was approved.
- [ENV-4] Codex — built: deterministic capture-quality scorer for framing, reprojection, blur, exposure, luminance stability, FPS, shutter, shake, and ARKit tracking state; artifacts: `threed/racketsport/capture_quality.py`, `tests/racketsport/test_capture_quality.py`; tested: local `.venv/bin/python -m pytest tests/racketsport` passed (15 tests before DATA-1 merge, then 18 tests after); next: wire real frame/audio QC extraction into ingest so the scorer is fed from video probes instead of sidecar/server signals only.
- [DATA-1] Codex + Helmholtz — built: CPU-only test-clip readiness manifest and CLI validator for required label files from the Test-Clip Dataset Spec; artifacts: `threed/racketsport/testclips.py`, `scripts/racketsport/validate_testclips.py`, `tests/racketsport/test_testclips.py`; tested: local `.venv/bin/python -m pytest tests/racketsport` passed (18 tests), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (18 tests); next: collect or mount real `data/testclips/` clips and labels, then run `scripts/racketsport/validate_testclips.py` until it exits 0.
- [CAL-1] Codex — built: regulation pickleball/tennis court templates, schema-backed zone artifacts, and vertical net-plane artifacts in the shared court world frame; artifacts: `threed/racketsport/court_templates.py`, `threed/racketsport/court_zones.py`, `threed/racketsport/net_plane.py`, `tests/racketsport/test_court_geometry.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_geometry.py -q` passed (6 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (24 tests); next: CAL-2 can consume `get_court_template()`, `build_court_zones()`, and `build_net_plane()` for solvePnP world points, overlay zones, and net references.
- [ENV-4] Codex — built: sampled ffmpeg clip-QC probe wired into test-clip ingest, including pure-Python blur/luminance metrics, QC decode FPS, and `capture_quality` output in each `frames_meta.json`; artifacts: `threed/racketsport/io_decode.py`, `scripts/racketsport/ingest_testclips.py`, `tests/racketsport/test_io_decode.py`; tested in the then-current local focused/full Python suites; next: replace/augment this CPU sampled path with true H100 NvDEC iteration and measure the Phase 0 decode throughput gate on real 1080p/4K test clips.
- [ENV-3] Codex — built: iOS Swift package module scaffold, shared `CaptureSidecar` Codable contract, minimal SwiftUI app entry point, and Xcode project/scheme skeleton; artifacts: `ios/Package.swift`, `ios/README.md`, `ios/App/`, `ios/Core/`, `ios/Capture/`, `ios/Calibration/`, `ios/FastTier/`, `ios/Guidance/`, `ios/Upload/`, `ios/Replay/`, `ios/Pickleball.xcodeproj/`; tested: `swift package --package-path ios describe` passed, `swift test --package-path ios` passed (2 tests), `plutil -lint ios/Pickleball.xcodeproj/project.pbxproj ios/App/Info.plist` passed, `xmllint --noout` passed for the shared scheme/workspace XML; blocked: any `xcodebuild` command fails before project evaluation because `/Applications/Xcode.app/Contents/Frameworks/IDESimulatorFoundation.framework` cannot load a `DVTDownloads` symbol, `xcodebuild -runFirstLaunch` hung for >2 minutes, and passwordless sudo is unavailable; next: repair/reinstall Xcode or run the admin first-launch flow, then rerun `xcodebuild -project ios/Pickleball.xcodeproj -scheme Pickleball -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build` before moving ENV-3 to DONE.
- [ENV-4] Codex — built: explicit ffmpeg decode-throughput benchmark helper with CPU/CUDA backends for Phase 0 ingest validation; artifacts: `threed/racketsport/io_decode.py`, `scripts/racketsport/benchmark_decode.py`, `tests/racketsport/test_io_decode.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_io_decode.py -q` passed (7 tests), local CLI smoke decoded a generated 10-frame clip, H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_io_decode.py -q` passed (7 tests), H100 ffmpeg advertised CUDA/CUVID decoders (`h264_cuvid`, `hevc_cuvid`) and CUDA filters (`scale_cuda`, `hwupload_cuda`), H100 synthetic 5 s 1080p60 H.264 decode benchmark measured CPU 1626 fps / 27.1x realtime and CUDA 307 fps / 5.1x realtime; next: run the benchmark on real 20-min 1080p/4K test clips and pick the backend empirically instead of assuming CUDA wins.
- [ENV-2] Codex — built: isolated MuJoCo/MJX runtime env installer and smoke test for physics-stack availability on the H100; artifacts: `scripts/racketsport/install_mujoco_mjx_env.sh`, `scripts/racketsport/smoke_mujoco_mjx.py`, `models/MANIFEST.json`; tested: H100 `/opt/conda/envs/racketsport_mjx/bin/python /tmp/mjx_smoke.py` passed with `jax_version=0.10.2`, `mujoco_version=3.10.0`, device `cuda:0`, and a 5-step MJX sphere simulation; next: FOOT-2 still needs task-specific physics integration and validation, but `mujoco_mjx` is no longer pending as an installed runtime package.
- [ENV-2] Codex — built: completed the current H100 checkpoint manifest inventory with Multi-HMR2.b plus MMPose RTMW-x 384, RTMPose Body26 m/l/x 384, and RTMPose COCO-WholeBody m 256 + l/x 384 entries; artifacts: `models/MANIFEST.json`, `tests/racketsport/test_model_manifest.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_model_manifest.py tests/racketsport/test_manifest_report.py -q` passed (4 tests), local manifest report shows 18 declared `available_on_h100` checkpoint files plus 1 declared `available_runtime_on_h100` runtime and 0 pending/missing entries, H100 `/opt/conda/envs/fast_sam_3d_body/bin/python scripts/racketsport/smoke_models.py --manifest models/MANIFEST.json --check-files-only --json` verified file presence and sha256 integrity for all 18 file-backed entries with 0 integrity failures; next: EVAL-0 must benchmark candidate variants on real clips and render comparison artifacts before any default model choice is approved.
- [CAL-2] Codex — built: CPU-safe sidecar/manual-tap calibration slice with sidecar intrinsics loading, net-center court-template corner correspondences, pure-Python world-to-image homography, reprojection median/p95 gate constants, sidecar capture-quality merge, static multi-frame tap averaging, and a calibration CLI that writes `court_calibration.json`, `court_zones.json`, and `net_plane.json`; artifacts: `threed/racketsport/court_calibration.py`, `scripts/racketsport/calibrate.py`, `tests/racketsport/test_court_calibration.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed (8 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (37 tests), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed (8 tests), and H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (37 tests); next: add real OpenCV `solvePnP` pose refinement, overlay rendering, drift guard, and run Phase 1 gates on real labeled test clips before marking CAL-2 done.
- [CAL-2] Codex — built: optional OpenCV `solve_camera_pose()` helper for full 6-DoF PnP refinement from world/image correspondences plus sidecar intrinsics, returning schema-compatible `CourtExtrinsics` and camera height from the solved camera center; artifacts: `threed/racketsport/court_calibration.py`, `tests/racketsport/test_court_calibration.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed with the OpenCV-specific test skipped because local `.venv` lacks `cv2`/`numpy`, H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed (9 tests, including solvePnP); next: wire solvePnP into the calibration CLI with confidence/fallback policy, then add overlay rendering and drift guard on real labeled clips.
- [CAL-2] Codex — built: wired solved OpenCV PnP extrinsics into `calibration_from_manual_taps()` / multi-frame artifact creation with fallback to ARKit sidecar seed when `cv2`/`numpy` are unavailable or the solve fails; artifacts: `threed/racketsport/court_calibration.py`, `tests/racketsport/test_court_calibration.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed (8 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_court_calibration.py -q` passed (10 tests, including solved-pose preference); next: add overlay rendering, drift guard, explicit solver-status metadata if the schema expands, and run Phase 1 gates on real labeled clips.
- [CAL-2] Codex — built: deterministic drift guard for calibrated court points, including scheduled frame checks and p95 reprojection drift detection against the 15 px Phase 1 gate; artifacts: `threed/racketsport/drift_guard.py`, `tests/racketsport/test_drift_guard.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_drift_guard.py -q` passed (3 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (40 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (42 tests); next: feed observed points from ORB/optical-flow court-line tracking and validate injected 20 px bumps on real clips.
- [CAL-2] Codex — built: world-point zone classification for pickleball and tennis with specific-zone priority before broad court fallback, supporting later court-polygon filtering and metric/NVZ rules; artifacts: `threed/racketsport/court_zones.py`, `tests/racketsport/test_court_geometry.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_geometry.py -q` passed (8 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (42 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (44 tests); next: use `classify_point()` in TRK-1/MET rules after CAL-2 produces real calibrated world points.
- [CAL-2] Codex — built: pure-Python world-point projection from solved calibration extrinsics/intrinsics and regulation net-plane endpoint projection for overlay/net-line cross-checks; artifacts: `threed/racketsport/court_calibration.py`, `threed/racketsport/net_plane.py`, `tests/racketsport/test_court_calibration.py`, `tests/racketsport/test_court_geometry.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed (44 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (46 tests); next: use projected net endpoints in overlay rendering and compare against labeled/visible net lines on real clips.
- [TRK-1] Codex — built: CPU-only tracking primitives for calibrated court filtering and identity locking: typed person detections with foot world points, court-polygon filtering through CAL-2 zone geometry, confidence-ranked N-lock, and metric ground-step plausibility for teleport rejection; artifacts: `threed/racketsport/person_fast.py`, `threed/racketsport/track_lock.py`, `tests/racketsport/test_tracking_primitives.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_tracking_primitives.py -q` passed (4 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (48 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (50 tests); next: integrate YOLO26m detections/BoT-SORT outputs into these primitives and evaluate on real clips once CAL-2 has verified world points.
- [TRK-1] Codex — built: deterministic doubles identity helpers for court-side/lateral role assignment from world positions plus one-tap coach anchor to bind a semantic label to the nearest locked track within a metric radius; artifacts: `threed/racketsport/doubles_id.py`, `tests/racketsport/test_tracking_primitives.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_tracking_primitives.py -q` passed (6 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (50 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (52 tests); next: combine with tracker outputs and color/ReID recovery once real clip tracks exist.
- [ENV-4] Codex — tested: H100 copied existing smoke clip `/Users/arnavchokshi/Desktop/CV_pipeline/sam4dbody/tmp/pickleball_body4d_smoke/source_vQhtz8l6VqU.mp4` into `/workspace/pickleball/data/testclips/smoke/` and ran `scripts/racketsport/benchmark_decode.py` on real 12 s 1280x720 H.264/25 fps video; CPU decode measured 2040 fps / 81.6x realtime and CUDA decode measured 416.6 fps / 16.7x realtime; next: still need representative 1080p/4K 20-min clips before selecting a default decode backend.
- [EVAL-0] Codex — tested: extracted `/workspace/pickleball/runs/phase0/real_smoke/frame_001.jpg` from the same 12 s pickleball smoke clip and ran `scripts/racketsport/run_fast_sam_benchmark.sh runs/phase0/fast_sam_real_smoke_profile` with `FAST_SAM_WARMUP_RUNS=1`, `FAST_SAM_BENCHMARK_RUNS=3`, and `FAST_SAM_IMAGE_PATH` pointing at that frame; H100 Fast-SAM-3D-Body detected 4 people and measured average 342.35 ms, std 2.50 ms, min 340.38 ms, max 345.87 ms, peak allocated GPU memory 4720.29 MB; next: EVAL-0 still needs representative labeled real clips, side-by-side artifacts, and the human approval gate before model choice is approved.
- [TRK-1] Codex — built: detector-box to ground-plane association bridge using calibrated homography inversion, including bottom-center footpoint conversion from image-space bboxes into court world coordinates before court-polygon filtering; artifacts: `threed/racketsport/court_calibration.py`, `threed/racketsport/person_fast.py`, `tests/racketsport/test_court_calibration.py`, `tests/racketsport/test_tracking_primitives.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_court_calibration.py tests/racketsport/test_tracking_primitives.py -q` passed (17 passed, 2 skipped), local `.venv/bin/python -m pytest tests/racketsport -q` passed (52 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (54 tests); next: connect this adapter to real detector/tracker outputs and evaluate on labeled clips.
- [EVAL-0] Codex — built: machine-readable Fast-SAM benchmark recorder that parses `profile_nsight.py` stdout into `sam3dbody_benchmark.json`, plus wrapper teeing to `profile_stdout.log` before writing metrics and normalizing relative output dirs before `cd`; artifacts: `scripts/racketsport/benchmark_sam3dbody.py`, `scripts/racketsport/run_fast_sam_benchmark.sh`, `tests/racketsport/test_benchmark_sam3dbody.py`, `tests/racketsport/test_shell_scripts.py`, `/workspace/pickleball/runs/phase0/fast_sam_real_smoke_json_check2/`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_benchmark_sam3dbody.py tests/racketsport/test_shell_scripts.py -q` passed (5 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (56 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (58 tests), H100 real-smoke wrapper wrote `profile_stdout.log` and `sam3dbody_benchmark.json` with status `measured`, 4 detected people, average 350.07 ms over one measured run, and peak VRAM 4720.29 MB; next: EVAL-0 still needs representative labeled clips, side-by-side variant artifacts, and human/obvious-winner approval before any model choice is locked.
- [DATA-1] Codex — built: optional `clip_metadata.json` schema and coverage-matrix accounting for the required 24-clip varied-camera test set, including counts and gap messages for camera height/angle, play type, environment, frame rate, length, and ArUco racket-GT coverage without changing label-readiness semantics; the validator now exposes `dataset_ready` and exits 0 only when both labels and the dataset matrix are complete; artifacts: `threed/racketsport/testclips.py`, `scripts/racketsport/validate_testclips.py`, `tests/racketsport/test_testclips.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_testclips.py -q` passed (6 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (59 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (61 tests), H100 real `validate_testclips.py --root /workspace/pickleball/data/testclips` exited 1 as expected with `total_clips=1`, `ready_clips=0`, `metadata_ready_clips=0`, `dataset_ready=false`, and the 24-clip matrix gaps listed; next: add actual collected clips/metadata under `data/testclips/` and drive `coverage_gaps` to empty.
- [EVAL-1] Codex — built: CPU-only phase-eval scaffold with shared `PhaseEvalMetrics` schema/registry entry, JSON writer helpers, and first Phase 1 calibration evaluator that consumes DATA-1 label readiness plus `court_calibration.json`, `court_zones.json`, and `net_plane.json` run artifacts to write `runs/phase1/metrics.json`; artifacts: `threed/racketsport/schemas/__init__.py`, `threed/racketsport/eval/metrics.py`, `threed/racketsport/eval/calib_eval.py`, `tests/racketsport/test_eval_metrics.py`, `tests/racketsport/test_schemas.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py tests/racketsport/test_schemas.py -q` passed (7 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (63 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (65 tests), H100 `python -m threed.racketsport.eval.calib_eval --root /workspace/pickleball/runs/phase1 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase1/metrics.json` wrote ignored `metrics.json` with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: run calib_eval against real DATA-1 clips, then add track/body/ball/racket evaluator modules and wire EVAL-2 numeric gates.
- [EVAL-1] Codex — built: Phase 2 `track_eval` scaffold that consumes DATA-1 label readiness plus `tracks.json`, writes `runs/phase2/metrics.json`, validates the `Tracks` artifact, and records initial artifact-readiness/player-count/frame-count metrics while leaving IDF1, ID switches, spectator rejection, and throughput gates for EVAL-2; artifacts: `threed/racketsport/eval/track_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (5 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (65 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (67 tests), H100 `python -m threed.racketsport.eval.track_eval --root /workspace/pickleball/runs/phase2 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase2/metrics.json` wrote ignored `metrics.json` with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add body/ball/racket evaluator modules and numeric EVAL-2 gates.
- [EVAL-1] Codex — built: Phase 3 `body_eval` scaffold that consumes DATA-1 label readiness plus `smpl_motion.json` and `skeleton3d.json`, writes `runs/phase3/metrics.json`, validates `SmplMotion`/`Skeleton3D`, and records initial SMPL player/frame plus preview skeleton counts while leaving MPJPE, foot/NVZ, angle, spacing, and FPS gates for EVAL-2; artifacts: `threed/racketsport/eval/body_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (7 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (67 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (69 tests), H100 `python -m threed.racketsport.eval.body_eval --root /workspace/pickleball/runs/phase3 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase3/metrics.json` wrote ignored `metrics.json` with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add ball/racket evaluator modules and numeric EVAL-2 gates.
- [EVAL-1] Codex — built: Phase 4 `physics_eval` scaffold that consumes DATA-1 label readiness plus refined `smpl_motion.json`, writes `runs/phase4/metrics.json`, validates `SmplMotion`, and records initial player/frame, foot-contact-frame, skate-free-player, physics-mode, and GRF-frame counts while leaving foot-slide, floor penetration, inter-player penetration, contact precision/recall, and acceleration-jitter gates for EVAL-2; artifacts: `threed/racketsport/eval/physics_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (14 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (75 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (77 tests), H100 `python -m threed.racketsport.eval.physics_eval --root /workspace/pickleball/runs/phase4 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase4/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: wire EVAL-2 numeric gates and dashboard/CI.
- [EVAL-1] Codex — built: Phase 5 `ball_event_eval` scaffold that consumes DATA-1 label readiness plus `ball_track.json` and `contact_windows.json`, writes `runs/phase5/metrics.json`, validates `BallTrack`/`ContactWindows`, and records initial ball-frame/contact/bounce counts while leaving ball F1, contact timing, bounce/net accuracy, and physics-fit residuals for EVAL-2; artifacts: `threed/racketsport/eval/ball_event_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (9 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (69 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (71 tests), H100 `python -m threed.racketsport.eval.ball_event_eval --root /workspace/pickleball/runs/phase5 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase5/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add racket evaluator module and numeric EVAL-2 gates.
- [EVAL-1] Codex — built: Phase 6 `racket_eval` scaffold that consumes DATA-1 label readiness plus `racket_pose.json`, writes `runs/phase6/metrics.json`, validates `RacketPose`, and records initial racket-player/frame/contact counts while leaving face-angle, contact-point, ArUco GT, and physics-validation gates for EVAL-2; artifacts: `threed/racketsport/eval/racket_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (11 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (71 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (73 tests), H100 `python -m threed.racketsport.eval.racket_eval --root /workspace/pickleball/runs/phase6 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase6/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: wire EVAL-2 numeric gates and dashboard/CI.
- [EVAL-1] Codex — built: Phase 7 `metric_eval` scaffold that consumes DATA-1 label readiness plus `racket_sport_metrics.json` and draft `habit_report.json`, writes `runs/phase7/metrics.json`, validates `RacketSportMetrics`/`HabitReport`, and records initial player, shot, metric-value, gated-metric, habit, and coverage counts while leaving metric accuracy, rule precision, confidence calibration, and rotation-gating gates for EVAL-2; artifacts: `threed/racketsport/eval/metric_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (16 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (77 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (79 tests), H100 `python -m threed.racketsport.eval.metric_eval --root /workspace/pickleball/runs/phase7 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase7/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add shot/drill, copy, replay, and e2e evaluators.
- [EVAL-1] Codex — built: Phase 8 `shot_drill_eval` scaffold that consumes DATA-1 label readiness plus `racket_sport_metrics.json` and `drill_report.json`, writes `runs/phase8/metrics.json`, validates `RacketSportMetrics`/`DrillReport`, and records initial shot, shot-type, drill-rep, clean-rep, and fault-rep counts while leaving shot macro-F1, top-2 accuracy, and rep-count error gates for EVAL-2; artifacts: `threed/racketsport/eval/shot_drill_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (18 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (79 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (81 tests), H100 `python -m threed.racketsport.eval.shot_drill_eval --root /workspace/pickleball/runs/phase8 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase8/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add copy, replay, and e2e evaluators.
- [EVAL-1] Codex — built: Phase 9 `copy_faithfulness` scaffold that consumes DATA-1 label readiness plus `habit_report.json` and `coach_report.json`, writes `runs/phase9/metrics.json`, validates both reports through the `HabitReport` schema, and records initial habit-count, coach-habit-count, priority-match, and coverage counts while leaving copy faithfulness, fast-tier latency, streaming, APNs, and coach usefulness gates for EVAL-2; artifacts: `threed/racketsport/eval/copy_faithfulness.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (20 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (81 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (83 tests), H100 `python -m threed.racketsport.eval.copy_faithfulness --root /workspace/pickleball/runs/phase9 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase9/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add replay and e2e evaluators.
- [EVAL-1] Codex — built: Phase 10 `replay_eval` scaffold that consumes DATA-1 label readiness plus `replay_scene.json`, writes `runs/phase10/metrics.json`, validates `ReplayScene`, checks referenced local GLB files, and records initial player, point, GLB-presence, and largest-point-GLB-size counts while leaving GLB/USDZ validation, cold-load, FPS, visible-skate, ball/racket consistency, and coach-review gates for EVAL-2; artifacts: `threed/racketsport/eval/replay_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (22 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (83 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (85 tests), H100 `python -m threed.racketsport.eval.replay_eval --root /workspace/pickleball/runs/phase10 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase10/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: add e2e evaluator.
- [EVAL-1] Codex — built: Phase 11 `e2e_eval` scaffold that consumes DATA-1 label readiness plus the integrated artifact set, writes `runs/phase11/metrics.json`, validates all major JSON artifacts, checks replay GLB references, and records required-artifact and referenced-GLB completeness while leaving fast-tier latency, full-SLA, cost/margin, and integrated accuracy gates for EVAL-2/EVAL-4; artifacts: `threed/racketsport/eval/e2e_eval.py`, `tests/racketsport/test_eval_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_metrics.py -q` passed (24 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (85 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (87 tests), H100 `python -m threed.racketsport.eval.e2e_eval --root /workspace/pickleball/runs/phase11 --labels /workspace/pickleball/data/testclips --out /workspace/pickleball/runs/phase11/metrics.json` wrote ignored `metrics.json` and exited 1 with `status=not_measured`, `total_clips=1`, `ready_clips=0`, and `evaluated_clips=0` as expected for the current incomplete smoke dataset; next: start EVAL-2 numeric gates/dashboard/CI and DATA-1 real clip collection.
- [DATA-1] Codex — built: test-clip registration CLI that copies or symlinks a candidate clip into `data/testclips/<name>/`, writes schema-checked `clip_metadata.json`, creates the `labels/` skeleton directory, and reports current manifest readiness for collection triage; artifacts: `scripts/racketsport/register_testclip.py`, `tests/racketsport/test_testclips.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_testclips.py -q` passed (7 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (86 passed, 2 skipped), local `.venv/bin/python -m py_compile scripts/racketsport/register_testclip.py` passed, local `git diff --check` passed, H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (88 tests), and H100 CLI smoke registered `/workspace/pickleball/data/testclips/smoke/source_vQhtz8l6VqU.mp4` under `/tmp/pickleball_register_cli_check` with `metadata_ready_clips=1`, `ready_clips=0`, `dataset_ready=false`, and expected validator exit 1 because labels/matrix remain incomplete; next: use the CLI to register real collected clips until the 24-clip coverage matrix and required labels are complete.
- [DATA-1] Codex — built: batch test-clip manifest registrar so a JSON `clips` list can register many collected source files with schema-checked metadata and manifest-relative paths in one command; artifacts: `scripts/racketsport/register_testclips_manifest.py`, `scripts/racketsport/register_testclip.py`, `tests/racketsport/test_testclips.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_testclips.py -q` passed (8 tests), local `.venv/bin/python -m py_compile scripts/racketsport/register_testclip.py scripts/racketsport/register_testclips_manifest.py` passed, local `git diff --check` passed, local `.venv/bin/python -m pytest tests/racketsport -q` passed (87 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (89 tests), and H100 two-row manifest smoke under `/tmp/pickleball_batch_register` registered `failed_count=0`, `metadata_ready_clips=2`, `ready_clips=0`, `dataset_ready=false`; next: use the manifest registrar to bulk-register downloaded/collected clips outside git, then fill the nine required label files per clip.
- [DATA-1] Codex — collected: seven real public-video clips on H100 and recorded reproducible source URLs/time ranges/metadata classifications because `data/testclips/` media is gitignored; artifacts: `docs/racketsport/manifests/testclip_seed_manifest.json`, H100 `/workspace/pickleball/data/testclips/{ppa_austin_md_qf_1200_high_baseline,ppa_austin_md_qf_1200_long_high_baseline,side_view_game5_0100_high_side_fence,wolverine_mixed_0200_mid_steep_corner,gear360_0200_high_near_overhead,burlington_gold_0300_low_steep_corner,ppa_singles_0500_high_baseline}/`; tested: local `.venv/bin/python -m json.tool docs/racketsport/manifests/testclip_seed_manifest.json` passed, H100 visual frame checks rejected bad tutorial/challenge segments and accepted seven gameplay segments, and H100 `validate_testclips.py` reported `total_clips=8`, `metadata_ready_clips=7`, `ready_clips=0`, with all camera heights and all camera angles represented, `short_60_120_s=6`, `long_15_20_min=1`, `singles_drill=1`; next: find true 120/240 fps and ArUco-GT sources or capture them directly, add more long clips, and fill manual label files.
- [DATA-1] Codex — built: manual-label frame-pack extractor that samples registered clip videos into `runs/label_frames/<clip>/frame_*.jpg` plus `label_frame_manifest.json` for annotation work; artifacts: `scripts/racketsport/extract_label_frames.py`, `tests/racketsport/test_io_decode.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_io_decode.py -q` passed (8 tests), local `.venv/bin/python -m py_compile scripts/racketsport/extract_label_frames.py` passed, local `git diff --check` passed, local `.venv/bin/python -m pytest tests/racketsport -q` passed (88 passed, 2 skipped), H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed (90 tests), H100 frame-pack smoke wrote `/tmp/pickleball_label_frames_smoke` with `clip_count=8`, `total_frames=16`, and 8 `label_frame_manifest.json` files, and H100 full extraction wrote `/workspace/pickleball/runs/label_frames` with `clip_count=8`, `total_frames=1707`, and `38M` of frame packs; next: fill the nine required label files from those frame packs, then add more clips until the DATA-1 coverage matrix passes.
- [DATA-1] Codex + Russell — built: draft label-workdir initializer that creates manual annotation templates under `runs/label_drafts/<clip>/labels/*.json` from registered clips and optional `runs/label_frames/` manifests without writing into `data/testclips/<clip>/labels/` or falsely marking clips ready; clarified that the tracked seed manifest is a source/reproducibility manifest, not a batch-registrar input; artifacts: `scripts/racketsport/init_label_workdir.py`, `tests/racketsport/test_label_workdir.py`, `docs/racketsport/manifests/testclip_seed_manifest.json`, `SWAY_BODY_PICKLEBALL_MVP.md`, `BUILD_CHECKLIST.md`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_label_workdir.py tests/racketsport/test_testclips.py -q` passed (11 tests), local `.venv/bin/python -m py_compile scripts/racketsport/init_label_workdir.py` passed, local `.venv/bin/python -m json.tool docs/racketsport/manifests/testclip_seed_manifest.json` passed, local `git diff --check` passed; next: run `scripts/racketsport/init_label_workdir.py --root data/testclips --frames-root runs/label_frames --out runs/label_drafts` on H100, complete real annotation files, and keep using `validate_testclips.py` as the only readiness signal.
- [DATA-1] Codex + Heisenberg — collected and registered four additional 15-minute public-video clips on H100 (`ppa_singles_q4zd_0500_long_high_baseline`, `metro_rec_b2kg_0500_long_messy_indoor`, `indoor_doubles_fwuks_0500_long_mid_baseline`, `outdoor_webcam_iynbd_1500_long_high_baseline`) and recorded source URLs/time ranges/metadata in the reproducibility manifest; artifacts: `docs/racketsport/manifests/testclip_seed_manifest.json`, H100 `/workspace/pickleball/data/testclips/{ppa_singles_q4zd_0500_long_high_baseline,metro_rec_b2kg_0500_long_messy_indoor,indoor_doubles_fwuks_0500_long_mid_baseline,outdoor_webcam_iynbd_1500_long_high_baseline}/`, H100 `/workspace/pickleball/data/source_clips/*.mp4`; tested: H100 `ffprobe` verified each new source as 1920x1080 H.264 with durations about 900 s and fps 60, 59.94, 29.97, and 60 respectively; H100 `validate_testclips.py --root /workspace/pickleball/data/testclips` reported `total_clips=12`, `metadata_ready_clips=11`, `ready_clips=0`, `long_15_20_min=5`, `short_60_120_s=6`, `doubles=7`, `singles_drill=2`, `messy_real_world=2`, `indoor=6`, `outdoor=5`, `dataset_ready=false`; next: capture or source true 120/240 fps and ArUco-GT clips, add more short/singles/messy clips, and fill the nine required label files.
- [EVAL-2] Codex — built: reusable numeric gate helper for phase metrics (`NumericGate`, `evaluate_numeric_gates`) with pass/fail/not-measured handling; artifacts: `threed/racketsport/eval/metrics.py`, `tests/racketsport/test_eval_gates.py`; tested: local focused audit suite `.venv/bin/python -m pytest tests/racketsport/test_eval_gates.py tests/racketsport/test_sidecar_intrinsics.py tests/racketsport/test_drift_guard.py tests/racketsport/test_court_geometry.py tests/racketsport/test_label_workdir.py -q` passed (26 tests), and H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_sidecar_intrinsics.py tests/racketsport/test_eval_gates.py -q` passed (9 tests); next: wire Protocols A/B/C/D and phase-specific physics/racket gate maps into each evaluator, then run on real DATA-1-ready clips before moving EVAL-2 to DONE.
- [DATA-1] Codex — built: three-clip CVAT import/export package for Burlington, Wolverine, and Outdoor; video 3 is capped at source frames `0..1150`; CVAT `ball` ellipses are converted to detector boxes; generated YOLO datasets for player, paddle, ball, and combined detectors with Outdoor as validation; artifacts: `threed/racketsport/cvat_video.py`, `threed/racketsport/cvat_detection_dataset.py`, `threed/racketsport/cvat_annotation_sanity.py`, `scripts/racketsport/import_cvat_video_annotations.py`, `scripts/racketsport/export_cvat_detection_yolo_dataset.py`, `scripts/racketsport/check_cvat_video_annotations.py`, `runs/cvat_imports/2026_06_30/manifest.json`, `runs/cvat_imports/2026_06_30/yolo_datasets/*/data.yaml`; tested: local focused CVAT/dataset suites and artifact validation passed, plus CPU decode baseline on all three active source videos; next: run GPU training/eval when a GPU is available, then use the trained detector outputs for real TRK/BALL/RKT gates rather than treating dataset export as verification.
- [RKT-1/DATA-1] Codex — built: CVAT `paddle` rectangle to review-only `racket_candidates.json` conversion plus explicit RKT readiness fields for runnable-now actions and missing labels/assets. Generated local ignored reports under `runs/cvat_imports/2026_06_30/<clip>/`: Burlington 822, Wolverine 556, and Outdoor 833 box-derived paddle candidates, all `blocked_preview_only` with `true_corner_frame_count=0`, `reference_gt_frame_count=0`, and `can_write_canonical_racket_pose=false`; `racket_model_runtime_readiness.json` is `blocked` on missing model/runtime entries plus missing face-corner labels, paddle CAD/reference assets, reference images, and ArUco/AprilTag/reference pose GT. Tested: focused RKT conversion/readiness/stage-runner/review-packet tests and `git diff --check`; next: collect true paddle-face corners or CAD/reference/ArUco GT, then rerun RKT pose/eval gates.
- [EVAL-3] Codex — built: measured-numeric phase-metrics regression checker with default >2% drop failure, CLI JSON output/exit status, and GitHub Actions workflow that runs checker tests plus manual baseline/current comparison via `workflow_dispatch`; artifacts: `.github/workflows/racketsport-regression.yml`, `scripts/racketsport/check_eval_regression.py`, `tests/racketsport/test_eval_regression.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_regression.py -q` passed (3 tests); next: wire automatic baseline/current metrics sources so PRs actually block on real phase drops. Until that source wiring exists, EVAL-3 is checker/corrections plumbing, not a universal merge-blocking gate.
- [DATA-1] Codex + Planck — built: incremental label-frame extraction so new clips can be sampled without re-extracting the whole registered set, using repeatable `--clip NAME` filters with useful missing-clip errors; artifacts: `scripts/racketsport/extract_label_frames.py`, `tests/racketsport/test_io_decode.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_io_decode.py tests/racketsport/test_eval_regression.py tests/racketsport/test_eval_e2e_gates.py tests/racketsport/test_corrections.py -q` passed (26 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (144 passed, 2 skipped), and H100 `init_label_workdir.py --root data/testclips --frames-root runs/label_frames --out runs/label_drafts` produced draft label templates for all 12 registered clips while `validate_testclips.py` still honestly reported `ready_clips=0`; next: run filtered extraction for the four new long clips, then complete real label JSON files under `data/testclips/<clip>/labels/` only after manual review.
- [EVAL-2] Codex + Erdos — built: Phase 11 end-to-end numeric gates using the shared `NumericGate` helper for required artifact count and replay GLB-reference completeness while preserving `not_measured` for incomplete labels and `blocked` for missing major artifacts; artifacts: `threed/racketsport/eval/e2e_eval.py`, `tests/racketsport/test_eval_e2e_gates.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_e2e_gates.py tests/racketsport/test_eval_metrics.py -k e2e_eval tests/racketsport/test_corrections.py -q` passed (6 passed, 31 deselected), local `.venv/bin/python -m pytest tests/racketsport -q` passed (144 passed, 2 skipped); next: run all EVAL-2 phase gates on DATA-1-ready clips once real labels/artifacts exist, then decide whether remaining placeholder gates need richer acceptance metrics.
- [EVAL-3] Codex + Kant — built: root-directory regression comparison mode for paired `**/metrics.json` artifacts plus workflow-dispatch inputs for either direct metrics files or baseline/current roots, preserving the measured-numeric >2% drop rule; artifacts: `.github/workflows/racketsport-regression.yml`, `scripts/racketsport/check_eval_regression.py`, `tests/racketsport/test_eval_regression.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_regression.py -q` passed (4 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (144 passed, 2 skipped), `.venv/bin/python -m py_compile scripts/racketsport/check_eval_regression.py` passed, and workflow YAML parsed with Ruby `YAML.load_file`; next: provide real persisted baseline/current metrics roots in CI so PRs block on actual phase drops instead of only synthetic/manual comparisons.
- [EVAL-3] Codex — hardened: missing measured baseline metrics now fail closed instead of passing with `checked_metrics=0`; GitHub workflow now installs `requirements-racketsport.txt`, runs collect-only plus script/CLI hygiene tests on script/test changes, and skips manual dispatch comparison clearly when no metric inputs are supplied; artifacts: `.github/workflows/racketsport-regression.yml`, `scripts/racketsport/check_eval_regression.py`, `tests/racketsport/test_eval_regression.py`; tested: local focused wrap-up suite passed (`207 passed, 3 skipped`) and workflow YAML parsed; next: provide real persisted baseline/current metrics roots in CI so PRs block on actual phase drops instead of only synthetic/manual comparisons.
- [EVAL-3] Codex — built: corrections queue artifact builder that validates strict correction manifests, flattens them into a `corrections_queue.json`, summarizes by operation/artifact/clip, and rejects duplicate queued correction keys across inputs; artifacts: `scripts/racketsport/build_corrections_queue.py`, `tests/racketsport/test_corrections.py`, `corrections/README.md`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_corrections.py -q` passed inside the focused 26-test integration run, local `.venv/bin/python -m py_compile scripts/racketsport/build_corrections_queue.py` passed, local `.venv/bin/python -m pytest tests/racketsport -q` passed (144 passed, 2 skipped); next: connect this queue to the real review/export surface and active-learning batch selection before calling the corrections flywheel done.
- [DATA-1] Codex — tested: H100 pulled `aa38e8e`, ran focused tests with `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport/test_eval_e2e_gates.py tests/racketsport/test_eval_regression.py tests/racketsport/test_corrections.py tests/racketsport/test_io_decode.py -q` (26 passed), then used filtered extraction for the four new long clips; artifacts: H100 `runs/label_frames/{ppa_singles_q4zd_0500_long_high_baseline,metro_rec_b2kg_0500_long_messy_indoor,indoor_doubles_fwuks_0500_long_mid_baseline,outdoor_webcam_iynbd_1500_long_high_baseline}/` with frame counts 1800, 1799, 900, and 1800; H100 `runs/label_frames` is 178M and `runs/label_drafts` is 11M; validator still reports `total_clips=12`, `metadata_ready_clips=11`, `ready_clips=0`, `dataset_ready=false`; next: complete real manual labels and add/capture short, singles, messy, 120/240 fps, and ArUco-GT coverage.
- [CAL-2] Codex + Huygens — built: CPU-only calibration overlay artifact generation from existing `court_calibration.json`/`net_plane.json`, including projected regulation court lines, net points, deterministic SVG output, optional JSON summary, and clean CLI failures for missing/invalid inputs; artifacts: `threed/racketsport/calibration_overlay.py`, `scripts/racketsport/render_calibration_overlay.py`, `tests/racketsport/test_calibration_overlay.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_calibration_overlay.py tests/racketsport/test_track_cli.py tests/racketsport/test_ball_audio_dataset.py tests/racketsport/test_racket_dataset.py -q` passed (15 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (159 passed, 2 skipped); next: render overlays on real DATA-1 calibration artifacts once labels/manual taps exist and use them for Phase 1 visual review.
- [TRK-1] Codex + Boole — built: CPU-safe `track.py` bridge that converts precomputed person bbox detections plus calibration into schema-valid `tracks.json`, using existing footpoint projection, court-polygon filtering, and plausible ground-step rejection; artifacts: `scripts/racketsport/track.py`, `tests/racketsport/test_track_cli.py`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_track_cli.py tests/racketsport/test_tracking_primitives.py tests/racketsport/test_schemas.py -q` passed inside the 159-test suite, and focused second-wave suite passed (15 tests); next: feed real YOLO26m/BoT-SORT detections into this bridge and evaluate ID stability after CAL-2/DATA-1 labels are ready.
- [DATA-3] Codex + Hume — built: scaffold-only ball/audio dataset manifest validator for `ball_track` and `pop_audio` entries, strict relative paths, split/source-type labels, duplicate ID rejection, optional frame/sample-rate metadata, and nonfatal coverage gaps for missing ball or 44.1 kHz pop-audio sources; artifacts: `scripts/racketsport/validate_ball_audio_dataset.py`, `tests/racketsport/test_ball_audio_dataset.py`, `docs/racketsport/ball_audio_dataset_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_ball_audio_dataset.py -q` passed inside the focused 15-test run, JSON schema parse passed, and local `.venv/bin/python -m pytest tests/racketsport -q` passed (159 passed, 2 skipped); next: only after DATA-1 is ready, populate real ball/audio manifests and connect Roboflow/TrackNet/audio-pop collection without claiming training.
- [DATA-4] Codex + Poincare — built: scaffold-only racket dataset source validator for `racketvision`, `synthetic_blenderproc`, and `aruco_gt` entries, strict safe paths, optional camera/fps/marker metadata, duplicate source IDs, and nonfatal ArUco-GT gap reporting; artifacts: `scripts/racketsport/validate_racket_dataset.py`, `tests/racketsport/test_racket_dataset.py`, `docs/racketsport/racket_dataset_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_racket_dataset.py -q` passed inside the focused 15-test run, JSON schema parse passed, and local `.venv/bin/python -m pytest tests/racketsport -q` passed (159 passed, 2 skipped); next: collect or synthesize actual RacketVision/BlenderProc/ArUco assets and keep ArUco-GT coverage as a reported gap until real marker clips exist.
- [DATA-1] Codex + Ohm — built: read-only coverage-gap report CLI that reuses the DATA-1 manifest builder, includes optional `runs/label_frames` frame-pack counts, prints JSON by default, and can write Markdown triage without creating labels or marking clips ready; artifacts: `scripts/racketsport/report_testclip_coverage.py`, `tests/racketsport/test_testclip_coverage_report.py`, `docs/racketsport/testclip_coverage_report.md`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_testclip_coverage_report.py tests/racketsport/test_variant_comparison.py tests/racketsport/test_decode_benchmark_summary.py -q` passed (9 tests), local `.venv/bin/python -m pytest tests/racketsport -q` passed (168 passed, 2 skipped), and `.venv/bin/python -m py_compile scripts/racketsport/report_testclip_coverage.py` passed; next: run this on H100 after each clip/label addition to prioritize coverage gaps without mutating `data/testclips`.
- [EVAL-0] Codex + Locke — built: variant-comparison artifact generator that validates candidate metric/overlay records, writes `variant_selection.md` and `variant_selection.json`, defaults approval to `pending`, supports explicit `auto_finalized_obvious` only with a non-empty reason, and never edits `models/MANIFEST.json`; artifacts: `scripts/racketsport/build_variant_comparison.py`, `tests/racketsport/test_variant_comparison.py`; tested: local focused 9-test suite passed, local `.venv/bin/python -m pytest tests/racketsport -q` passed (168 passed, 2 skipped), and `.venv/bin/python -m py_compile scripts/racketsport/build_variant_comparison.py` passed; next: feed real candidate overlays/metrics from H100 EVAL-0 runs and keep model choice pending until the §1.6 approval gate or a truly obvious winner is documented.
- [ENV-4] Codex + Kepler — built: decode benchmark summarizer for one or more `benchmark_decode.py`-compatible JSON artifacts, reporting fastest backend per clip plus backend aggregates while explicitly not choosing a global default backend; artifacts: `scripts/racketsport/summarize_decode_benchmarks.py`, `tests/racketsport/test_decode_benchmark_summary.py`; tested: local focused 9-test suite passed, local `.venv/bin/python -m pytest tests/racketsport -q` passed (168 passed, 2 skipped), and `.venv/bin/python -m py_compile scripts/racketsport/summarize_decode_benchmarks.py` passed; next: run CPU/CUDA decode benchmarks across representative real 1080p/4K/20-min clips and summarize results before selecting any backend default.
- [ENV-4] Codex — tested: H100 real-clip decode benchmark across three DATA-1 clips (short 60 fps, long 30 fps, long 60 fps), summarized without changing the global backend default; artifacts: `docs/racketsport/archive/h100_decode_benchmark_summary.md`, H100 ignored `runs/phase0/decode_benchmarks/h100_real_clip_decode_benchmarks.json`; tested: CPU was fastest on all sampled clips, mean CPU 1809.448 decode fps / 37.012x realtime vs CUDA 898.987 decode fps / 21.953x realtime; next: repeat on future 4K, 120 fps, and 240 fps captures before any default selection.
- [DATA-2] Codex + Aquinas — built: scaffold-only pose/body dataset manifest validator for BEDLAM2, AthletePose3D, CalTennis, RICH, AMASS, and EMDB eval entries with safe paths, split/source validation, duplicate ID rejection, optional metadata, JSON schema, and nonfatal fine-tune/eval coverage gaps; artifacts: `scripts/racketsport/validate_pose_dataset.py`, `tests/racketsport/test_pose_dataset.py`, `docs/racketsport/pose_dataset_schema.json`; tested: included in local full suite `221 passed, 2 skipped`; next: populate real manifests only when sources are mounted, keeping DATA-1 readiness separate.
- [DATA-5] Codex + Rawls — built: scaffold-only shot-class dataset manifest validator plus reviewed-label feature-window builder for audio-snapped pose/manual/synthetic entries, split/source/shot-label coverage, safe paths, duplicate ID rejection, and nonfatal missing-class/split gaps; artifacts: `scripts/racketsport/validate_shot_dataset.py`, `scripts/racketsport/build_shot_dataset.py`, `threed/racketsport/shot_dataset_builder.py`, `tests/racketsport/test_shot_dataset.py`, `tests/racketsport/test_shot_dataset_builder.py`, `docs/racketsport/shot_dataset_schema.json`; tested: included in local full suite `221 passed, 2 skipped`; next: populate after DATA-1 and BALL-3 produce real contact/pose labels.
- [EVAL-0] Codex + Ramanujan — built: read-only index over `runs/eval0/**/variant_selection.json` artifacts that preserves pending approval status and never edits `models/MANIFEST.json`; artifacts: `scripts/racketsport/build_eval0_index.py`, `tests/racketsport/test_eval0_index.py`, `docs/racketsport/eval0_index_schema.json`; tested: included in local full suite `221 passed, 2 skipped`; next: run after real EVAL-0 candidate comparisons are generated.
- [TRK-1] Codex + Goodall — built: deterministic locked-ID update and coach-anchor application helpers that preserve track order on plausible metric steps and fail closed on missing, duplicate, or teleporting tracks; artifacts: `threed/racketsport/track_lock.py`, `threed/racketsport/doubles_id.py`, `tests/racketsport/test_tracking_primitives.py`; tested: included in local full suite `221 passed, 2 skipped`; next: feed real BoT-SORT/ReID track outputs when CAL-2/DATA-1 labels are ready.
- [BODY-2] Codex + Halley — built: CPU-only world-grounding primitives for snapping player root translations to court Z=0, applying the same vertical delta to mesh vertices, per-player EMA smoothing, and residual metrics; artifacts: `threed/racketsport/worldhmr.py`, `tests/racketsport/test_worldhmr.py`; tested: included in local full suite `221 passed, 2 skipped`; next: integrate Fast SAM-3D-Body outputs after BODY-1/TRK-1 gates.
- [FOOT-1] Codex + Bohr — built: CPU-only foot-contact/foot-lock primitives for contact hysteresis, stance-foot z snapping, stance velocity zeroing, and slide/penetration metrics, explicitly without SMPL IK; artifacts: `threed/racketsport/footlock.py`, `tests/racketsport/test_footlock.py`; tested: included in local full suite `221 passed, 2 skipped`; next: wire to BODY-2 outputs and add CCD-IK only after body artifacts exist.
- [BALL-2] Codex + Jason — built: CPU-only audio-pop timing primitives for speed-of-sound delay correction, court-time onset shifting, onset candidate validation, mel-window bounds, and raw-onset fusion, explicitly without detector/classifier training; artifacts: `threed/racketsport/audio_pop.py`, `tests/racketsport/test_audio_pop.py`; tested: included in local full suite `221 passed, 2 skipped`; next: connect real 44.1 kHz pop datasets and classifier training after DATA-3.
- [BALL-4] Codex + Tesla — built: CPU-only ball-physics primitives for simple 3D parabola fitting, residual outlier rejection, bounce detection from z-minimum/vertical sign change, and BallTrack-compatible bounce projection; `BallStageRunner(ball_physics3d=True)` can emit `ball_physics3d_summary.json` from existing `world_xyz` samples, but the current local run is `insufficient_world_xyz_samples` with `sample_count=0` and it is not a BALL/PHYSICS verification gate; artifacts: `threed/racketsport/ball_physics3d.py`, `tests/racketsport/test_ball_physics3d.py`, `threed/racketsport/ball_stage_runner.py`, `tests/racketsport/test_ball_stage_runner.py`; tested: included in local full suite `221 passed, 2 skipped`; next: replace scaffold with EKF/RANSAC/Magnus pipeline once BALL-1 tracks and CAL-2 world points exist.
- [RKT-1] Codex + Dalton — built: CPU-only racket/paddle pose primitives for paddle dimension validation, face-normal normalization, contact-point bounds against paddle dimensions, and a minimal SE3 confidence container, explicitly without detector/SAM2/PnP/GigaPose; artifacts: `threed/racketsport/racket6dof.py`, `tests/racketsport/test_racket6dof_primitives.py`; tested: included in local full suite `221 passed, 2 skipped`; next: integrate real pose estimation once BODY-2, BALL-4, and DATA-4 are ready.
- [MET-2] Codex + Hypatia — built: CPU-only confidence-gating and fact-only insight-rule primitives with `allow`/`relative`/`omit` decisions and deterministic habit-like outputs, explicitly without LLM/report integration; artifacts: `threed/racketsport/confidence.py`, `threed/racketsport/insight_rules.py`, `tests/racketsport/test_confidence_insight_rules.py`; tested: included in local full suite `221 passed, 2 skipped`; next: connect to MET-1 metrics and RPT-1 once upstream artifacts are verified.
- [SHOT-2] Codex + Boyle — built: CPU-only drill rep counting primitives from contact timestamps and wrist-velocity samples, clean/fault classification from supplied event flags, and DrillReport-compatible output, explicitly without ML/BALL-3/MET-1 integration; artifacts: `threed/racketsport/drill_verify.py`, `tests/racketsport/test_drill_verify.py`; tested: included in local full suite `221 passed, 2 skipped`; next: wire confirmed contacts and metric signals after BALL-3/MET-1.
- [RPT-1] Codex + Harvey — built: CPU-only deterministic report artifact scaffold from existing metric facts, optional corrections-queue report exclusions, and placeholder copy marked as metric review rather than final coaching; artifacts: `threed/racketsport/report_model.py`, `threed/racketsport/habit_model.py`, `scripts/racketsport/build_report_artifacts.py`, `tests/racketsport/test_report_artifacts.py`, `docs/racketsport/report_artifacts_schema.json`; tested: included in local full suite `221 passed, 2 skipped`; next: consume MET-2 confidence gates and RPT-2 LLM copy only after MET-1/MET-2 are verified.
- [RPL-1] Codex + Bernoulli — built: CPU-only ReplayScene export manifest helpers that validate safe relative GLB paths, referenced files, measured point sizes, players, and fps without doing OpenUSD/GLB rendering; artifacts: `threed/racketsport/replay_export.py`, `tests/racketsport/test_replay_export.py`; tested: included in local full suite `221 passed, 2 skipped`; next: add real USDZ/GLB baking after FOOT-2/RKT-1/BALL-4 gates.
- [RPL-2] Codex + Volta — built: initial minimal web replay scaffold with strict TypeScript `ReplayScene` parser and placeholder React summary view for scene/player/point counts, explicitly without Three.js rendering claims; later passes replaced this with the current review-only React Three Fiber / Three.js court-world viewer. Current production replay status is still scaffold because animated GLB/USDZ export and upstream BODY/BALL/RKT gates remain missing; artifacts: `web/replay/package.json`, `web/replay/src/`, `web/replay/README.md`; tested: `npm test` passed (3 tests) and `npm run typecheck` passed; next: build production animated replay after RPL-1 exports real scenes.
- [MET-1] Codex + Hubble — built: CPU-only movement/biomechanics metric primitives for NVZ margin, inter-player spacing, support-proxy balance score, and signed shoulder-vs-hip X-factor angle; artifacts: `threed/racketsport/movement_metrics.py`, `threed/racketsport/biomech.py`, `tests/racketsport/test_movement_metrics.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed (240 passed, 2 skipped), plus py_compile and `git diff --check`; next: connect to real FOOT/BALL/RKT artifacts and run MET gates before promoting MET-1.
- [BALL-1] Codex + Averroes — built: CPU-only TrackNet-compatible ball-frame validation/interpolation and tap-track correction primitives with frame-index/timestamp matching and fail-closed mismatch handling, explicitly without TrackNet inference or training; artifacts: `threed/racketsport/ball_tracknet.py`, `threed/racketsport/ball_tap_track.py`, `tests/racketsport/test_ball_tracking_primitives.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed (240 passed, 2 skipped), plus py_compile and `git diff --check`; next: connect real TrackNet outputs and DATA-3 ball labels before any BALL-1 gate claim.
- [BALL-3] Codex + Socrates — built: CPU-only deterministic event-fusion/contact-window primitives requiring audio onset, wrist-velocity peak, and ball-inflection cues inside the time gate before emitting a contact, with nearest-player attribution by wrist-to-ball world distance; artifacts: `threed/racketsport/event_fusion.py`, `threed/racketsport/contact_windows.py`, `tests/racketsport/test_event_fusion.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed (240 passed, 2 skipped), plus py_compile and `git diff --check`; next: feed real BALL-1/BALL-2/BODY-2 cues and validate timing gates on DATA-1 clips.
- [BALL-3] Codex — built: standalone cue-fusion CLI plus action-dashboard commands so existing `audio_onsets.json`, `wrist_velocity_peaks.json`, and `ball_inflections.json` can be fused into canonical `contact_windows.json` without a full BALL stage rerun; artifacts: `scripts/racketsport/build_contact_windows_from_cues.py`, `threed/racketsport/event_fusion.py`, `threed/racketsport/review_action_manifest.py`, `tests/racketsport/test_event_fusion.py`, `tests/racketsport/test_review_action_manifest.py`; tested: local focused event/BALL/action suite passed; next: keep this fail-closed and do not promote BALL until label F1/contact-timing gates pass.
- [RPT-2] Codex + McClintock — built: CPU-only copy faithfulness checks that flatten metric/habit facts, detect unknown metric-like keys and invented numeric values, preserve the priority habit, and report deterministic pass/fail summaries without calling an LLM; artifacts: `threed/racketsport/llm_copy.py`, `tests/racketsport/test_llm_copy_faithfulness.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed (240 passed, 2 skipped), plus py_compile and `git diff --check`; next: use these checks around real generated coaching copy after RPT-1/MET gates are available.
- [BODY-1] Codex + Kuhn — built: CPU-only deep-tier HMR scaffold primitives for player-crop validation, Fast SAM-3D-Body/MHR-to-SMPL-like payload normalization, per-player artifact packaging, and confidence/failure gating, explicitly without model inference or GPU use; artifacts: `threed/racketsport/hmr_deep.py`, `tests/racketsport/test_hmr_deep_primitives.py`; tested: local focused suite `.venv/bin/python -m pytest tests/racketsport/test_hmr_deep_primitives.py tests/racketsport/test_hmr_fast_primitives.py tests/racketsport/test_physics_refine.py tests/racketsport/test_viz_artifacts.py tests/racketsport/test_worldhmr.py tests/racketsport/test_footlock.py tests/racketsport/test_report_artifacts.py -q` passed (37 passed), plus py_compile and `git diff --check`; next: real BODY-1 still needs approved model variant/checkpoint, GPU inference integration through the lease, and real clip evaluation.
- [BODY-3] Codex + Dewey — built: CPU-only fast-tier camera-space mesh preview scaffold primitives with validated preview meshes, latency-tier metadata, per-frame/player packaging, and fail-closed preview payloads, explicitly without SAT-HMR/Multi-HMR/Core ML inference; artifacts: `threed/racketsport/hmr_fast.py`, `tests/racketsport/test_hmr_fast_primitives.py`; tested: included in the 37-test local focused suite, plus py_compile and `git diff --check`; next: integrate real fast-tier model outputs and device/Core ML validation before any BODY-3 gate claim.
- [FOOT-2] Codex + Curie — built: CPU-only physics-refinement scaffold primitives for request validation, padded/merged refinement-window selection, contact/floor/inter-player constraint summaries, MJX-required vs CPU-fallback execution planning, and schema-friendly artifact packaging, explicitly without simulation/training; artifacts: `threed/racketsport/physics_refine.py`, `tests/racketsport/test_physics_refine.py`; tested: included in the 37-test local focused suite, plus py_compile and `git diff --check`; next: connect PhysPT/MuJoCo-MJX/PHC/PULSE/MultiPhys and run FOOT-2 gates before status promotion.
- [RPT-3] Codex + Cicero — built: CPU-only visualization payload helpers for court-map heatmap/path/priority-marker data, self-vs-self ghost contact alignment, and overlay/telestration metadata, explicitly without image/video rendering or replay/web changes; artifacts: `threed/racketsport/viz_courtmap.py`, `threed/racketsport/viz_ghost.py`, `threed/racketsport/viz_overlay.py`, `tests/racketsport/test_viz_artifacts.py`; tested: included in the 37-test local focused suite, plus py_compile and `git diff --check`; next: render real visualization artifacts and verify report delivery gates after upstream reports exist.
- [SHOT-1] Codex + Euclid — built: CPU-only shot-classifier scaffold primitives plus event-level shot evaluator, SAM-3D-Body/MHR70 semantic joint adapter, transfer/heuristic review baseline for abstract `fh_shot`/`bh_shot` fallback, DATA-5 feature-window metadata, a centroid trainable sanity baseline, pickleball shot taxonomy, prediction confidence gating, fail-closed unknown labels, and metrics-style shot sequence payloads, explicitly without approved BST/PoseConv3D training or inference; external eval snapshot: THETIS 60 clips = 51.7% family / 66.7% top-2 with serve 0/15 and overhead 0/5, OpenSportsLab 100 events = 81.0% broad but serve 0/19; artifacts: `threed/racketsport/shot_classifier.py`, `threed/racketsport/shot_transfer_baseline.py`, `threed/racketsport/shot_trainable_baseline.py`, `threed/racketsport/skeleton3d.py`, `threed/racketsport/eval/shot_event_eval.py`, `scripts/racketsport/generate_shot_classifications.py`, `scripts/racketsport/train_shots.py`, `scripts/racketsport/build_shot_dataset.py`, `tests/racketsport/test_shot_classifier_primitives.py`, `tests/racketsport/test_shot_transfer_baseline.py`, `tests/racketsport/test_shot_trainable_baseline.py`, `tests/racketsport/test_body_semantics.py`, `tests/racketsport/test_shot_event_eval.py`, `tests/racketsport/test_shot_dataset_builder.py`, `runs/shot_external_eval/thetis_pose_eval_60/`, `runs/shot_external_eval/opensportslab_tennis_eval_100/`; tested: local focused shot suite `32 passed`, plus external H100 eval artifacts; next: collect DATA-5 reviewed pickleball labels, train PoseConv3D/PoseC3D first, then compare BST-style fusion and run SHOT gates before status promotion.
- [CAL-3] Codex + Aristotle — built: CPU-only court-keypoint-net scaffold primitives for pickleball court keypoint taxonomy, synthetic/training-plan config validation, pure-Python heatmap subpixel decode, and solvePnP-ready correspondence packaging, explicitly without TennisCourtDetector download or training; artifacts: `threed/racketsport/court_keypoint_net.py`, `scripts/racketsport/train_court_keypoint_heatmap.py`, `tests/racketsport/test_court_keypoint_net.py`; tested: included in local full suite `284 passed, 2 skipped`, plus py_compile and `git diff --check`; next: replace the scaffold with real licensed court-keypoint training only after CAL-2/DATA-1 readiness and run CAL-3 training/eval gates.
- [EVAL-3] Codex + Beauvoir — built: scaffold-only corrections flywheel plumbing with accepted/pending/rejected status validation, phase/metric/clip queue summaries, and accepted-only training-manifest candidates with safe relative paths; artifacts: `threed/racketsport/corrections.py`, `scripts/racketsport/build_corrections_queue.py`, `scripts/racketsport/validate_corrections.py`, `tests/racketsport/test_corrections.py`; tested: included in local full suite `284 passed, 2 skipped`, plus py_compile and `git diff --check`; next: connect to the real review/export surface and active-learning/training batch creation before marking EVAL-3 done.
- [BODY-4] Codex + James — built: CPU-only pose fine-tune planning scaffold that validates the BEDLAM2 → AthletePose3D → CalTennis → RICH → AMASS ladder, reads pose-manifest coverage, writes `pose_finetune_plan.json`, records H100 lease requirements, and fails closed on missing sources/eval coverage; artifacts: `scripts/racketsport/finetune_pose.py`, `tests/racketsport/test_finetune_pose_scaffold.py`; tested: included in local full suite `284 passed, 2 skipped`, plus py_compile and `git diff --check`; next: run real fine-tuning through the H100 training lease only after DATA-2/BODY-1 readiness.
- [IOS-1] Codex + Raman — built: Swift package capture-policy scaffold for mode/fps/resolution/codec choices, shutter/ISO/focus/WB lock clamping, landscape orientation acceptance, and session metadata; artifacts: `ios/Capture/Sources/PickleballCapture/CapturePolicy.swift`, `ios/Capture/Sources/PickleballCapture/CaptureSessionScaffold.swift`, `ios/Capture/Tests/PickleballCaptureTests/CapturePolicyTests.swift`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check -- ios/Package.swift ios/Capture ios/Calibration ios/FastTier ios/Guidance ios/Upload ios/Replay`; next: complete and physically validate the real AVFoundation camera/session path.
- [IOS-2] Codex + Godel — built: Swift package calibration scaffold for ARKit setup-pass sidecar modeling, seed validation, manual 4-corner tap validation/ordering, and `CaptureSidecar` packaging with manual-fallback quality flags; artifacts: `ios/Calibration/Sources/PickleballCalibration/CalibrationSeed.swift`, `ios/Calibration/Sources/PickleballCalibration/ManualCourtTaps.swift`, `ios/Calibration/Sources/PickleballCalibration/CalibrationSidecarPackager.swift`, `ios/Calibration/Tests/PickleballCalibrationTests/`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check`; next: connect real ARKit plane/pose/intrinsics capture and manual-tap UI after the Xcode/device path works.
- [IOS-3] Codex + Ptolemy — built: Swift package fast-tier preview metadata scaffold for Vision/CoreML pose/ball/racket track descriptors, preview-only metric labels, and fail-closed payload status; artifacts: `ios/FastTier/Sources/PickleballFastTier/OnDevicePoseTrack.swift`, `ios/FastTier/Tests/PickleballFastTierTests/FastTierPreviewPayloadTests.swift`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check`; next: integrate real Apple Vision/Core ML inference and latency checks on device.
- [IOS-4] Codex + Ptolemy — built: Swift package capture-guidance rule scaffold for framing, tracking state, exposure/clipping, blur, level, and shake, including fail-closed handling for missing measurements; artifacts: `ios/Guidance/Sources/PickleballGuidance/GuidanceFlag.swift`, `ios/Guidance/Tests/PickleballGuidanceTests/CaptureGuidanceEvaluatorTests.swift`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check`; next: feed real ARKit/AVFoundation/CoreMotion measurements into UI guidance once ENV-3 builds.
- [IOS-5] Codex + Zeno — built: Swift package upload-manifest scaffold for safe relative path validation, sidecar-first upload ordering, pose/depth/clip part planning, and resumable chunk metadata; artifacts: `ios/Upload/Sources/PickleballUpload/UploadManifest.swift`, `ios/Upload/Tests/PickleballUploadTests/UploadManifestTests.swift`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check`; next: add URLSession/TUS-style network upload, retry/resume persistence, and real server contract tests.
- [IOS-6] Codex + Zeno — built: Swift package replay metadata scaffold for USDZ/GLB asset validation, asset references, timeline descriptors, and explicit metadata-only capability reporting; artifacts: `ios/Replay/Sources/PickleballReplay/ReplayAsset.swift`, `ios/Replay/Tests/PickleballReplayTests/ReplayAssetTests.swift`, `ios/Package.swift`; tested: included in `swift test --package-path ios` and `git diff --check`; next: integrate RealityKit/RealityView loading after RPL-1 emits real replay assets and ENV-3 device validation is available.
- [EVAL-4] Codex + Mendel — built: CPU-only pipeline artifact readiness contracts and CLI that validate expected per-stage JSON artifacts plus dependency blocking before an end-to-end run is considered ready, without running models or claiming final integration; artifacts: `threed/racketsport/pipeline_contracts.py`, `scripts/racketsport/validate_pipeline_artifacts.py`, `tests/racketsport/test_pipeline_contracts.py`, `docs/racketsport/pipeline_contracts_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_pipeline_contracts.py -q` passed (5 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: feed real per-clip artifact directories from the integrated pipeline and keep EVAL-4 gated on all upstream VERIFIED tasks.
- [DATA-3] Codex + Darwin — built: CPU-only ball/audio dataset manifest validator for Roboflow/TrackNet x,y ball labels, 44.1 kHz WAV pop/negative audio samples, augmentation metadata, safe relative paths, duplicate IDs, and nonfatal split/source/class coverage gaps; artifacts: `scripts/racketsport/validate_ball_audio_dataset.py`, `tests/racketsport/test_ball_audio_dataset.py`, `docs/racketsport/ball_audio_dataset_schema.json`; tested: local focused dataset/serving suite passed (16 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: populate real `data/ball/` and `data/audio/` manifests/assets only after DATA-1 readiness, then run DATA-3/BALL gates before status promotion.
- [DATA-4] Codex + Singer — built: CPU-only racket dataset manifest validator/schema for RacketVision, synthetic BlenderProc paddle-CAD, and ArUco-GT entries with safe relative paths, train/val/test/eval split/source coverage summaries, duplicate ID rejection, top/bottom/handle keypoint plus face-corner metadata checks, pose-label and mask metadata checks, and nonfatal coverage gaps; artifacts: `scripts/racketsport/validate_racket_dataset.py`, `tests/racketsport/test_racket_dataset.py`, `docs/racketsport/racket_dataset_schema.json`; tested: local focused dataset/serving suite passed (16 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: populate real DATA-4 manifests/assets and keep DATA-4/RKT-1 unpromoted until real collection, training, and eval gates pass.
- [ENV-2/EVAL-0] Codex + Plato — built: CPU-only serving and runtime readiness manifest mapping `models/MANIFEST.json` entries to `offline_deep` and `live_light` tiers with safe-path, checkpoint/runtime availability, Triton scaffold, and pending/missing reporting; artifacts: `threed/racketsport/serving_manifest.py`, `scripts/racketsport/build_serving_manifest.py`, `tests/racketsport/test_serving_manifest.py`, `docs/racketsport/serving_manifest_schema.json`, `serving/triton/racketsport_ensemble/README.md`; tested: local focused dataset/serving suite passed (16 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: after EVAL-0 approval, use this report to generate real Triton model configs without inferring ENV/EVAL completion.
- [DATA-1] Codex + Banach — built: CPU-only label-draft audit summary for `runs/label_drafts` before dataset-label promotion, checking draft presence, JSON validity, schema/status, target-file match, frame-pack context, and annotation item counts without writing into `data/testclips`; artifacts: `scripts/racketsport/audit_label_drafts.py`, `tests/racketsport/test_label_draft_audit.py`, `docs/racketsport/label_draft_audit_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_label_draft_audit.py -q` passed (5 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: run the audit against real draft workdirs and promote labels only after review, this does not mark DATA-1 ready.
- [EVAL-2/EVAL-4] Codex + Hilbert — built: CPU-only read-only eval summary aggregation of phase `metrics.json` files with phase/status counts, metric/gate/clip counts, missing metrics reporting, and highest-risk phase ranking; artifacts: `threed/racketsport/eval/summary.py`, `scripts/racketsport/summarize_eval_runs.py`, `tests/racketsport/test_eval_summary.py`, `docs/racketsport/eval_summary_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_eval_summary.py -q` passed (5 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: run this after real eval metrics are generated to support reassessment, without mutating metrics or satisfying gates.
- [EVAL-4] Codex + Hooke — built: CPU-only scaffold-tool index reporter for `scripts/racketsport/*.py`, `scripts/racketsport/*.sh`, top-level `scripts/*.py`, and top-level `scripts/*.sh`, inferred workstream/task hints, tool categories, related-test links, direct CLI path-reference coverage, schema coverage, and gaps without running scaffold commands; artifacts: `scripts/racketsport/list_scaffold_tools.py`, `tests/racketsport/test_scaffold_tool_index.py`, `docs/racketsport/scaffold_tool_index_schema.json`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_scaffold_tool_index.py -q` passed (3 tests), py_compile passed, JSON schema parsed, and `git diff --check` passed; next: use it during future handoffs to find existing CLIs and missing coverage faster.
- [STATUS-HYGIENE] Codex — built: checklist status reset matching the approved semantics: code-complete and unit-green scaffold rows are `DONE`, full acceptance remains `VERIFIED`, and the current accepted-four lowered packet is marked `PROTOTYPE-GATE`; artifacts: `BUILD_CHECKLIST.md`; tested: the then-current local status parser and `git diff --check` passed; next: use DATA-2 autolabel bootstrap plus qualitative overlays to move real clip artifacts toward prototype verification without waiting for the full 24-clip/ArUco matrix. Current counts live only in the top truth snapshot.
- [DATA-2/EVAL-0/RPT] Codex — built: initial prototype autolabel bootstrap, YOLO26m teacher artifact runner, qualitative MP4 label-overlay renderer with frame-pack fallback for AV1 decode failures, and human-review/CVAT-style export/import flow; the current default packet is accepted-four and excludes `side_view_game5_0100_high_side_fence`; artifacts: `threed/racketsport/autolabel.py`, `scripts/racketsport/run_prototype_autolabel.py`, `scripts/racketsport/run_yolo26_teacher.py`, `threed/racketsport/label_overlay.py`, `scripts/racketsport/render_label_overlays.py`, `threed/racketsport/label_review.py`, `scripts/racketsport/export_review_frames.py`, `scripts/racketsport/export_cvat_tasks.py`, `scripts/racketsport/import_cvat_labels.py`, now-consolidated review instructions in `docs/racketsport/prototype_gate_h100_v2_usage.md`, H100 `runs/eval0/prototype_gate/*/labels/`, H100 `runs/eval0/prototype_gate/*/compare/all_labels_overlay.mp4`, H100 `runs/eval0/prototype_gate/review_bundle/`, and H100 `runs/eval0/prototype_gate/cvat_tasks/`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `316 passed, 4 skipped`, H100 `/opt/conda/envs/fast_sam_3d_body/bin/python -m pytest tests/racketsport -q` passed `320 passed`, H100 YOLO26m teacher run wrote player/ball/racket teacher files for the then-selected prototype clips where detected, H100 autolabel rerun reported `teacher_inference_wired=true`, H100 overlay render wrote nonempty MP4s, and review export wrote capped review items plus CVAT task folders; next: human should watch accepted-four overlays, click/correct 4 court corners per active clip, and import corrections before treating CAL/TRK/BALL/MET prototype outputs as accepted.
- [EVAL-4] Codex — built: fail-closed pipeline spine with a `StageRunner` protocol, real manual-sidecar calibration runner, precomputed-detection tracking runner, schema validation after every stage, CLI summary writing, and explicit `blocked` status when body/downstream runners are not registered instead of fabricating artifacts; current behavior also embeds semantic readiness in `pipeline_run.json`, changes an otherwise passing run to `blocked` when readiness says not ready, and reports reused review helpers separately from newly produced helpers; artifacts: `threed/racketsport/orchestrator.py`, `tests/racketsport/test_orchestrator_spine.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `326 passed, 4 skipped` at commit time and later `337 passed, 4 skipped`, H100 `.venv/bin/python -m pytest tests/racketsport/test_orchestrator_spine.py -q` passed; next: register real GPU runners stage-by-stage, starting with TrackNet ball and then body/contact/replay, and keep `VERIFIED=0` until a real clip passes the documented gates.
- [EVAL-2] Codex — built: no-op gate hardening so zero-evidence physics/ball/racket artifacts no longer pass acceptance-style phase evals (`foot_contact_frames`, `grf_frames`, `contact_events`, `bounce_events`, `racket_contacts` now require positive evidence); artifacts: `threed/racketsport/eval/physics_eval.py`, `threed/racketsport/eval/ball_event_eval.py`, `threed/racketsport/eval/racket_eval.py`, `tests/racketsport/test_eval_noop_gate_hardening.py`, related eval tests; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `330 passed, 4 skipped`, H100 `.venv/bin/python -m pytest tests/racketsport/test_eval_noop_gate_hardening.py -q` passed; next: replace remaining weak count/completeness gates with the documented numeric accuracy gates as labels/artifacts become available.
- [CAL-2/DATA-2] Codex — built: human-review calibration bridge that consumes court-corner correction templates, maps keyed corners into sidecar/manual-tap order, creates `capture_sidecar.json`, then writes schema-valid `court_calibration.json`, `court_zones.json`, and `net_plane.json` without needing original iOS sidecars; this bridge was later narrowed by the accepted-four import below, with `side_view_game5_0100_high_side_fence` deferred/rejected for this wave; artifacts: `threed/racketsport/court_corner_review.py`, `scripts/racketsport/build_calibration_from_review.py`, `tests/racketsport/test_court_corner_review_calibration.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `333 passed, 4 skipped`, H100 `.venv/bin/python -m pytest tests/racketsport/test_court_corner_review_calibration.py -q` passed; next: after the human fills accepted-four `review_bundle/corrections/<clip>/court_corners.json` files, run the bridge and rerender overlays/tracks from corrected calibration artifacts.
- [CAL-2/DATA-2] Codex — built: reviewed-corner prototype calibration import for the accepted four static/user-realistic clips only, with `side_view_game5_0100_high_side_fence` marked `DEFERRED_REJECTED_SIDE_FISHEYE`, plus real-frame calibration overlay rendering for visual review on review-frame packs; artifacts: `threed/racketsport/calibration_overlay.py`, `tests/racketsport/test_calibration_overlay.py`, ignored run artifacts under `runs/eval0/prototype_gate_h100_v2/{burlington_gold_0300_low_steep_corner,wolverine_mixed_0200_mid_steep_corner,outdoor_webcam_iynbd_1500_long_high_baseline,indoor_doubles_fwuks_0500_long_mid_baseline}/`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_calibration_overlay.py tests/racketsport/test_court_corner_review_calibration.py -q` passed `10 passed`; next: regenerate the same accepted-four artifacts on H100 with OpenCV solvePnP available, inspect the frame overlays, then convert real `labels/players.json` detections into `detections.json` for TRK-1 instead of treating label files as contract artifacts.
- [TRK-1/DATA-2] Codex — built: prototype player-label-to-detection bridge that converts accepted YOLO teacher `labels/players.json` items into the `scripts/racketsport/track.py` `detections.json` contract, skipping uncertain boxes by default and keeping label IDs as source metadata instead of claiming stable tracks; artifacts: `threed/racketsport/detection_bridge.py`, `scripts/racketsport/convert_player_labels_to_detections.py`, `tests/racketsport/test_detection_bridge.py`, ignored `runs/eval0/prototype_gate_h100_v2/*/{detections.json,tracks.json}`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `343 passed, 4 skipped`, H100 `.venv/bin/python -m pytest tests/racketsport/test_detection_bridge.py -q` passed `4 passed`, and accepted-four H100 conversion/tracking wrote schema-valid `tracks.json` with 4/4/4/3 prototype player tracks respectively; next: render a track-review overlay in a later pass and keep TRK-1 unverified until a real IDF1/spectator-rejection gate passes on labeled clips.
- [BALL-1] Codex — built: TrackNetV3 adapter seam that converts official `Frame,Visibility,X,Y` prediction CSVs into schema-valid `ball_track.json`, writes runtime metadata, can run official `predict.py` when the TrackNetV3 repo/runtime exists, and fails closed when runtime pieces are missing; artifacts: `threed/racketsport/tracknet_adapter.py`, `scripts/racketsport/run_tracknet_ball.py`, `tests/racketsport/test_tracknet_adapter.py`; tested: local `.venv/bin/python -m pytest tests/racketsport -q` passed `337 passed, 4 skipped`, H100 `.venv/bin/python -m pytest tests/racketsport/test_tracknet_adapter.py -q` passed, H100 `smoke_models.py --check-files-only --json` verified TrackNetV3 and InpaintNet checkpoint SHA256s, H100 cloned official TrackNetV3 at `/workspace/TrackNetV3` commit `77c123ad4dd449b7d275f16cc43f316ba5b54042`, installed missing `parse`, patched that external clone to `num_workers=0` for low-`/dev/shm` Docker smoke runs, and ran a 2-second Burlington smoke clip through the real predictor producing `runs/tracknet_smoke/burlington/ball_track.json` with 120 frames and 108 visible detections; next: this is runtime smoke only, not Phase 5 verification, because contact windows, labels, F1/timing gates, and full-clip/event fusion still remain.
- [BALL-1/BODY-1] Codex — built: prototype review tooling for real H100 smoke artifacts without promoting gates. TrackNet wrapper now exposes upstream `--video-range` with explicit median-only semantics, `ball_overlay.py` renders schema-validated ball-track review videos, and `sam3dbody_probe.py` records probe-only FastSAM-3D-Body output metadata without fabricating `smpl_motion.json` or `skeleton3d.json`; artifacts: `threed/racketsport/tracknet_adapter.py`, `scripts/racketsport/run_tracknet_ball.py`, `threed/racketsport/ball_overlay.py`, `scripts/racketsport/render_ball_track_overlay.py`, `threed/racketsport/sam3dbody_probe.py`, `scripts/racketsport/run_sam3dbody_probe.py`, `tests/racketsport/test_tracknet_adapter.py`, `tests/racketsport/test_ball_overlay.py`, `tests/racketsport/test_sam3dbody_probe.py`, `docs/racketsport/archive/prototype_gate_h100_v2_tracknet_body_smoke.md`, ignored `runs/eval0/prototype_gate_h100_v2/*/tracknet_smoke_0000_0010/`, and ignored `runs/eval0/prototype_gate_h100_v2/burlington_gold_0300_low_steep_corner/body_probe_0000/`; tested: local focused suite passed `12 passed, 5 skipped`, H100 FastSAM env focused suite passed `17 passed`, accepted-four H100 TrackNet 10s smokes produced schema-valid ball tracks and H.264 overlays, and a Burlington real FastSAM-3D-Body probe returned four person records with `pred_vertices`, `pred_keypoints_3d`, `pred_joint_coords`, `pred_cam_t`, and pose/shape parameter fields; next: inspect the overlay quality, then add full-window/full-clip TrackNet jobs selectively and build the BODY court-world conversion before registering a BODY StageRunner.
- [STATUS-HYGIENE] Codex — built: stricter `DONE` semantics for named model/algorithm rows, demoted the overstated ML/CV rows to `SCAFFOLD`, added root `CAPABILITIES.md` as the per-stage truth matrix, and relabeled count-only evaluator gates as `presence_check.*` or `artifact_check.*`; artifacts: `BUILD_CHECKLIST.md`, `CAPABILITIES.md`, `threed/racketsport/eval/*.py`, `tests/racketsport/test_truthful_capabilities.py`, related eval tests; tested: local `.venv/bin/python -m pytest tests/racketsport/test_truthful_capabilities.py -q` passed, local focused eval/docs suite passed `58 passed`; next: do not promote any SCAFFOLD row until the exact named model/variant/weight runs through a registered StageRunner and a real-label accuracy gate passes.
- [BALL-1/EVAL-0] Codex — built: no-click ball benchmark and current strict review path for the accepted-four prototype clips: held-out `ball_points.json` scorer, TrackNet target-court/temporal candidates, VballNet verifier fusion, and a local trajectory-consistency filter that hides points far from the surrounding path; artifacts: `threed/racketsport/ball_benchmark.py`, `threed/racketsport/ball_model_fusion.py`, `threed/racketsport/ball_temporal_filter.py`, `scripts/racketsport/benchmark_ball_trackers.py`, `scripts/racketsport/fuse_ball_tracks.py`, `scripts/racketsport/filter_ball_temporal.py`, consolidated runbook `docs/racketsport/prototype_gate_h100_v2_usage.md`, and ignored accepted-four `ball_track_fusion_temporal_vball100_localtraj*` artifacts; tested: local focused ball suite passed `9 passed`, H100 FastSAM env focused ball suite passed `9 passed`, and the four-clip benchmark improved teleports from 8 to 0, hidden false positives from 0.425 to 0.294, and p90 error from 46.8 px to 38.9 px versus the looser fusion track while hit recall dropped from 0.563 to 0.509; next: use `ball_track_fusion_temporal_vball100_localtraj.json` for strict prototype review, compare against `ball_track_fusion_temporal_vball100.json` when recall matters, then build a real BALL StageRunner and improve model-side candidate generation before attempting BALL F1/contact-timing gates.
- [TRK-1] Codex — built: registered real tracking spine runner for manifest-verified YOLO26m plus Ultralytics BoT-SORT ReID, with explicit precomputed/manual mode retained; artifacts: `threed/racketsport/orchestrator.py`, `threed/racketsport/model_manifest.py`, `configs/racketsport/botsort_reid.yaml`, `tests/racketsport/test_real_tracking_runner.py`, `tests/racketsport/test_orchestrator_spine.py`, `CAPABILITIES.md`, `pyproject.toml`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_real_tracking_runner.py tests/racketsport/test_orchestrator_spine.py -q` passed `5 passed, 1 skipped`, local `.venv/bin/python -m pytest tests/racketsport -q` passed `370 passed, 10 skipped`, local `git diff --check` passed, lease-wrapped real prototype smoke reached tracking with `real_model=true`/`source_mode=yolo26m_botsort_reid` and failed loudly on missing `/workspace/checkpoints/body4d/yolo26/yolo26m.pt`; next: rerun the real smoke on the H100 where the checkpoint/runtime exist, then add/pass the IDF1, spectator-rejection, ID-switch, and throughput gate before moving TRK-1 beyond `IN-PROGRESS`.
- [BODY-1/BODY-2] Codex — built: registered `BodyStageRunner` for the fail-closed spine, manifest sha256 verification for `fast_sam_3d_body_dinov3`, `sam_3d_body_mhr_model`, `moge_2_vitl_normal`, and `yolo26m`, Fast SAM-3D-Body runtime loader using registry YOLO26m instead of YOLO11n, tracked-player bbox invocation, and camera-to-court/world-SMPL artifact conversion from `court_calibration.json` into schema-valid `smpl_motion.json` and `skeleton3d.json`; `smpl_motion.json` now preserves runtime world joints plus optional `mesh_vertices_world` for replay/review; artifacts: `threed/racketsport/orchestrator.py`, `threed/racketsport/hmr_deep.py`, `threed/racketsport/worldhmr.py`, `tests/racketsport/test_body_runner_real_path.py`, `tests/racketsport/test_orchestrator_spine.py`, `tests/racketsport/test_truthful_capabilities.py`, `CAPABILITIES.md`; tested: local `.venv/bin/python -m pytest tests/racketsport/test_body_runner_real_path.py tests/racketsport/test_orchestrator_spine.py -q` passed `5 passed`, local overlap suite passed `26 passed`, local `.venv/bin/python -m pytest tests/racketsport -q` passed `370 passed, 10 skipped`, local prototype BODY run on `burlington_gold_0300_low_steep_corner` reached BODY with `real_model=true`/`source_mode=fast_sam_3d_body` then failed loudly on missing `/workspace/checkpoints/body4d/sam-3d-body-dinov3/model.ckpt`, and `scripts/gpu-eval-run.sh` local fallback reported no NVIDIA GPU and the same missing-checkpoint BODY failure; next: rerun the BODY spine smoke on the H100 via `scripts/gpu-eval-run.sh /opt/conda/envs/fast_sam_3d_body/bin/python -m threed.racketsport.orchestrator ... --stage body --tracking-mode precomputed --max-frames 1`, confirm real `smpl_motion.json`/`skeleton3d.json`, then run the documented BODY world-MPJPE/foot-NVZ/angle gates before moving BODY-1/BODY-2 beyond `SCAFFOLD`.
- [RKT-1/EVAL-4] Codex — built: browser-reviewable racket-candidate overlays for the accepted-four prototype packet, including OpenCV overlay rendering, optional H.264 transcoding, 960x540 label-coordinate to 1920x1080 video-coordinate scaling, packet indexing, and explicit coverage accounting when candidates extend beyond the source video window; recovered 30-second public-source slices for Outdoor webcam and Indoor doubles so their long candidate timelines render fully; artifacts: `threed/racketsport/racket_candidate_overlay.py`, `scripts/racketsport/render_racket_candidate_overlay.py`, `tests/racketsport/test_racket_candidate_overlay.py`, `threed/racketsport/review_packet.py`, `tests/racketsport/test_review_packet.py`, ignored `runs/eval0/prototype_gate_h100_v2/*/racket_candidates/source_0000_0030.mp4`, `racket_candidate_overlay*.mp4`/index artifacts, and refreshed `runs/review_packets/prototype_gate_h100_v2/prototype_gate_h100_v2_review.*`; tested: focused overlay/packet tests passed, accepted-four overlays rendered, browser check over local HTTP found 4 candidate overlay sections, 20 videos, no video element errors, and scale details; after source recovery all four overlays rendered all current candidates; next: replace box-corner candidates with true detector/keypoint/CAD paddle corners before attempting RKT promotion.
- [RKT-1/RPL-1/EVAL-4] Codex — built: preview-only paddle pose and virtual-world assembly for ambiguous box-derived candidates without weakening the fail-closed racket gate. `scripts/racketsport/build_racket_pose_preview.py` writes schema-valid `racket_pose_preview.json` with `:pnp_ippe_preview` sources and `not_gate_verified=true`; `virtual_world_paddle_preview.json` then renders the paddle mesh into the court world while warning `ambiguous_paddle_pose`. Accepted-four paddle preview counts are Burlington 42, Wolverine 36, Outdoor webcam 397, and Indoor doubles 280; after the scheduled H100 BODY run the preview worlds include BODY mesh samples for Burlington, Wolverine, and Indoor, while Outdoor still warns `missing_mesh_vertices`; artifacts: `threed/racketsport/racket_pose_preview.py`, `scripts/racketsport/build_racket_pose_preview.py`, `threed/racketsport/virtual_world.py`, `threed/racketsport/review_packet.py`, `tests/racketsport/test_racket_pose_preview.py`, `tests/racketsport/test_virtual_world.py`, `tests/racketsport/test_review_packet.py`, ignored accepted-four `racket_pose_preview.json`/`virtual_world_paddle_preview.json`, and refreshed packet outputs; tested: focused preview/world/packet tests passed and packet index shows all four preview worlds; next: true detector/keypoint/CAD paddle corners plus ArUco/GT face-angle gates are still required before RKT promotion.
- [RPL-1/EVAL-4] Codex — built: virtual-world browser review pages and ball court-plane fallback for accepted-four review. `virtual_world.py` now projects visible 2D ball detections through the calibration homography when `world_xyz` is missing and marks those fallback points `approx=true`; `scripts/racketsport/build_virtual_world_review.py` writes Three.js `virtual_world_paddle_preview.html` plus `virtual_world_review_index.json` so the packet links a visual court/player/ball/paddle review surface. Accepted-four court-plane ball points: Burlington 288, Wolverine 119, Outdoor webcam 489, Indoor doubles 117; artifacts: `threed/racketsport/virtual_world_review.py`, `scripts/racketsport/build_virtual_world_review.py`, `threed/racketsport/virtual_world.py`, `threed/racketsport/review_packet.py`, `tests/racketsport/test_virtual_world_review.py`, `tests/racketsport/test_virtual_world.py`, `tests/racketsport/test_review_packet.py`, ignored accepted-four `virtual_world_paddle_preview.html`/`virtual_world_review_index.json`, and refreshed packet outputs; tested: focused viewer/world/packet tests passed, browser DOM/console check loaded the Burlington Three.js page and packet links with no console errors; screenshot capture from the in-app browser timed out on the WebGL page, so no screenshot-pixel proof is claimed; next: real GLB/USDZ export and replay gates remain required before RPL promotion.
- [RKT-1] Codex — research refresh only: current public/official sources and code audit point to the practical paddle-pose ordering of true-corner PnP-IPPE as the canonical acceptance gate, GigaPose/FoundPose as near-term RGB/CAD coarse-pose or hypothesis scorers, SAM2/Grounded-SAM2 as mask/video candidate generators rather than pose solvers, and FoundationPose as a strong candidate when depth/ARKit/pseudo-depth or runtime constraints are satisfied. The repo docs now reflect that ordering while preserving `SCAFFOLD` status; next: add manifest/runtime probes only after a paddle CAD/reference asset and H100 runtime decision exist.
- [BODY-2/RPL-1] Codex — tightened adaptive-planning review outputs after audit: omitted ball frames in an otherwise-present ball track are now treated as `ball_missing` uncertainty in `frame_compute_plan.json`, `BodyStageRunner` consumes existing `deep_mesh_windows` through `body_compute_execution.json`, and `virtual_world.json` preserves track-only frames around sparse SMPL mesh samples instead of dropping them from the player timeline. Superseded scheduling note: later human-reviewed `contact_windows.json` files now schedule Burlington 3 frames / 9 player-frames, Wolverine 4 / 12, Outdoor 0 / 0, and Indoor doubles 2 / 6. Artifacts: `threed/racketsport/frame_rating.py`, `threed/racketsport/body_compute.py`, `scripts/racketsport/build_body_compute_execution.py`, `threed/racketsport/orchestrator.py`, `threed/racketsport/virtual_world.py`, `tests/racketsport/test_frame_rating.py`, `tests/racketsport/test_body_compute.py`, `tests/racketsport/test_body_runner_real_path.py`, `tests/racketsport/test_orchestrator_spine.py`, `tests/racketsport/test_virtual_world.py`; tested: focused adaptive/BODY/review tests passed; next: complete player coverage and run BODY accuracy gates before promotion.
- [BODY-2/RPL-1/EVAL-4] Codex — built: CPU-only `body_mesh_readiness.json` audits so review packets distinguish real BODY mesh vertices from joints-only or missing BODY output. Current accepted-four audits report `mesh_available_needs_accuracy_gate` for Burlington (9 mesh player-frames), Wolverine (12), and Indoor doubles (6), all with 18,439 vertices per player-frame; Outdoor webcam remains `missing_body_output` because no world-mesh frames are scheduled. The audit never creates synthetic meshes from joints and does not promote BODY without world-MPJPE/full-clip gates. Artifacts: `threed/racketsport/body_mesh_readiness.py`, `scripts/racketsport/build_body_mesh_readiness.py`, `threed/racketsport/review_packet.py`, `threed/racketsport/review_action_manifest.py`, `tests/racketsport/test_body_mesh_readiness.py`, `tests/racketsport/test_review_packet.py`, `tests/racketsport/test_review_action_manifest.py`, ignored accepted-four `body_mesh_readiness.json`, and refreshed packet/action outputs; tested: focused body-mesh/packet/action tests passed; next: add labeled world-MPJPE/full-window BODY gates before promotion.
- [BODY-2/EVAL-4] Codex — tightened the BODY representation audit so `body_mesh_readiness.json` now compares `frame_compute_plan.json` world-mesh demand, `body_compute_execution.json` scheduling, and actual BODY mesh/joint availability. The review packet surfaces `Representation decision`, world-mesh requested/scheduled/available counts, and representation target counts. Current audits report Burlington, Wolverine, and Indoor as `world_mesh_required_available_unverified`; Outdoor remains `no_world_mesh_requested`. These are scheduled-frame H100 smoke outputs only, not BODY verification. Artifacts: `threed/racketsport/body_mesh_readiness.py`, `scripts/racketsport/build_body_mesh_readiness.py`, `threed/racketsport/review_packet.py`, `threed/racketsport/review_action_manifest.py`, `tests/racketsport/test_body_mesh_readiness.py`, `tests/racketsport/test_review_packet.py`, `tests/racketsport/test_review_action_manifest.py`, refreshed accepted-four `body_mesh_readiness.json`; tested: focused body-mesh/packet/action tests passed; next: complete player coverage and run BODY accuracy gates before promotion.
- [RPL-1/EVAL-4] Codex — tightened virtual-world review summaries so virtual-world JSON artifacts, the packet, and the Three.js HUD now expose approximate court-plane ball frame counts, mesh/joint/track-only player-frame counts, paddle player counts, and ambiguous paddle frame counts. Regenerated accepted-four worlds show approximate ball frames Burlington 288, Wolverine 119, Outdoor webcam 489, Indoor doubles 117; preview paddle frames remain fully ambiguous at Burlington 42, Wolverine 36, Outdoor webcam 397, Indoor doubles 280. Artifacts: `threed/racketsport/virtual_world.py`, `threed/racketsport/virtual_world_review.py`, `threed/racketsport/review_packet.py`, `threed/racketsport/schemas/__init__.py`, `tests/racketsport/test_virtual_world.py`, `tests/racketsport/test_virtual_world_review.py`, `tests/racketsport/test_review_packet.py`, refreshed accepted-four virtual-world JSON, review pages, and packet outputs; tested: focused virtual-world/review/packet tests passed; next: human visual review and true paddle/ball/depth evidence are still required before RKT/RPL promotion.
- [RKT-1/EVAL-2] Codex — hardened Phase 6 RKT evaluation so a syntactically valid `racket_pose.json` no longer passes or fails from player/frame/contact presence counts alone. `racket_eval` now records those counts as `artifact_check.*` context, reports `not_measured` without reviewed `labels/racket_pose.json` reference/ArUco labels, and gates only `label_check.racket_face_angle_p90_error_deg <= 5` plus contact-point p90 error when reviewed reference labels exist. Artifacts: `threed/racketsport/eval/racket_eval.py`, `threed/racketsport/eval/label_checks.py`, `tests/racketsport/test_eval_ball_racket_gates.py`, `tests/racketsport/test_eval_noop_gate_hardening.py`, `tests/racketsport/test_eval_metrics.py`, docs updates; tested: focused RKT/label/eval tests passed; next: populate real DATA-4 ArUco/reference labels before any RKT verification claim.
- [STATUS-HYGIENE] Codex — built: 2026-06-28 audit reset of the build checklist, implementation phases, capability counts, and truthfulness test after six subsystem reviews; artifacts: `BUILD_CHECKLIST.md`, `IMPLEMENTATION_PHASES.md`, `CAPABILITIES.md`, `tests/racketsport/test_truthful_capabilities.py`; tested: local docs truth test and diff checks in this audit pass; next: keep the authoritative counts only in the top truth snapshot until a row has the evidence demanded by its phase gate.
- [TRK-1/BODY-2] Codex + Poincare — tested: real H100 player-coverage diagnostics on actual 30-second source clips without promoting canonical tracks. Canonical accepted-four four-player coverage remains sparse (Burlington 2/180, Wolverine 1/91, Outdoor 1/1786, Indoor 0/900). Fresh YOLO26m source-30 candidates improved Outdoor to 864/1800 and Indoor to 424/900 four-player frames, but widened-margin diagnostics are unsafe to promote because Indoor samples include background/spectator-like boxes. Artifacts: ignored `runs/phase2/person_coverage_actual_source30_h100_yolo26m_fulltb3_20260628/` metrics, overlays, and montage JPEGs; tested: focused tracking overlay/benchmark tests passed; next: fix canonical-safe player coverage and labeled ID/spectator gates before BODY scheduling depends on those tracks.
- [BODY-1/BODY-2/EVAL-4] Codex + Bacon — built: fail-closed BODY gate report that separates mesh smoke from BODY verification. Accepted-four report status is `blocked`: Burlington 3 scheduled BODY frames / 9 mesh player-frames, Wolverine 4 / 12, Indoor 2 / 6, Outdoor 0 / 0; all clips lack world-MPJPE and full-clip BODY gate evidence. Artifacts: `threed/racketsport/eval/body_gate_report.py`, `scripts/racketsport/build_body_gate_report.py`, `tests/racketsport/test_body_gate_report.py`, ignored `runs/eval0/prototype_gate_h100_v2/body_gate_report.{json,md}`; tested: focused BODY gate/readiness tests passed and default clip discovery now ignores non-clip run folders; next: add reviewed BODY world-joint labels or an equivalent evaluator plus `body_full_clip_gate.json`, then rerun without `--allow-not-verified`.
- [RKT-1/EVAL-4] Codex + Ptolemy — built: fail-closed paddle true-corner review lane and box-source rejection before canonical RKT promotion. Accepted-four `paddle_true_corner_review.json` artifacts and crop sheets exist, but all candidates are box-derived (Burlington 42, Wolverine 36, Outdoor 397, Indoor 280), true-corner labels are 0, reference/GT count is 0, and `RacketStageRunner` rejects them before writing canonical `racket_pose.json`. Artifacts: `threed/racketsport/racket_true_corners.py`, `scripts/racketsport/build_paddle_true_corner_review.py`, `threed/racketsport/racket_stage_runner.py`, `threed/racketsport/review_packet.py`, related tests, ignored crop sheets; tested: focused RKT tests and accepted-four rejection smoke passed; next: label actual paddle-face corners or add CAD/reference/ArUco GT before RKT evaluation.
- [RPL-1/RPL-2/EVAL-4] Codex + Pauli — built: fail-closed replay readiness report and accepted-four review visual manifest. All four clips have review visuals and two valid static GLBs each, but production replay and metrics readiness remain `blocked` by BODY accuracy, preview-only paddle pose, approximate ball projection, missing labels root, and Outdoor missing BODY mesh. Artifacts: `threed/racketsport/replay_readiness.py`, `scripts/racketsport/build_replay_readiness_report.py`, `tests/racketsport/test_replay_readiness.py`, ignored `runs/eval0/prototype_gate_h100_v2/replay_readiness_report.{json,html}` and per-clip static GLBs; tested: focused replay/virtual-world/e2e suites passed and real artifact assertion confirmed 4/4 review visuals with valid referenced GLBs; next: do not build/present production replay until BODY/BALL/RKT gates are trustworthy.
- [REVIEW-UI/TRK/BODY/BALL/RPL] Codex + Avicenna/Hegel/Popper/James — built: focused localhost `/paddle` review page for true paddle-face corner clicks and tracking-video promotion decisions, canonical-safe TRK audit, stricter BODY label/full-clip gate reporting, contact timing review-alignment evaluator, and replay static-vs-production guardrails. Real accepted-four evidence after regeneration: paddle queues Burlington 42, Wolverine 36, Outdoor 48 rendered crops, Indoor 48 rendered crops; tracking strict candidates audit `canonical_candidate_not_gate_verified` for Outdoor 864/1800 and Indoor 424/900, while margin diagnostics audit `diagnostic_only`; `contact_timing_review_alignment.json` matches 34/34 saved review contacts at 0-frame delta but `ball_verified=false`; `body_gate_report.json` remains `blocked`; `replay_readiness_report.json` remains `blocked` with static GLBs classified as review-only. Artifacts: `scripts/racketsport/review_input_server.py`, `threed/racketsport/person_tracking_promotion_audit.py`, `threed/racketsport/eval/body_gate_report.py`, `threed/racketsport/eval/contact_timing_review_eval.py`, `threed/racketsport/replay_export.py`, `threed/racketsport/replay_readiness.py`, related tests and reports; tested: focused integrated suite and browser/curl checks passed; next: human paddle-corner labels plus tracking-video decisions, then labeled TRK/BODY/BALL/RKT gates.
- [STATUS-HYGIENE/A100-DATA/TRK/BALL/CAL/BODY/RKT/RPL/IOS] Codex overnight manager — built: synchronized status docs around the 2026-06-30 parallel-agent evidence without promoting gates; artifacts include `runs/gpu_diagnostics/a100_runtime_20260630_codex.json`, `runs/a100_detector_eval_20260630/summary.json`, `runs/cvat_imports/2026_06_30/coverage_sanity_report.json`, `runs/cvat_imports/2026_06_30/data1_substitute/manifest.json`, `runs/phase2/person_track_gt_scoring_20260630/TRK_FAILURE_ANALYSIS_20260630.json`, `runs/phase2/person_track_gt_scoring_20260630/TRK_OUTDOOR_PLAYER_ID_DIAGNOSTICS_20260630.json`, `runs/eval0/prototype_gate_h100_v2/ball_tracker_benchmark/cvat_ball_horizon_candidate_benchmark_20260630.json`, `runs/eval0/prototype_gate_h100_v2/ball_tracker_benchmark/outdoor_ball_candidate_horizon_scan_20260630.json`, `runs/pickleball_pretraining/court_keypoint_20260628/court_keypoint_no_tap_calibration_report_20260630_codex.json`, `runs/body_runtime_diagnostics/body_runtime_diagnostics_20260630.json`, `runs/cvat_imports/2026_06_30/rkt_shot_replay_lane_audit_20260630_codex.json`, and `runs/ios_diagnostics/2026-06-30_record_first_device_smoke.md`; tested: focused Python/iOS/web suites in the subagent and lead verification passes; next: clean Outdoor player coverage first, import the missing Indoor CVAT export, complete physical iOS record-tap smoke, then rerun TRK/BODY/BALL gates on prepared GPU hardware.
- [BODY-2/EVAL-4] Codex — superseded the earlier anchor-exact/batched BODY evidence with guarded per-player A100 full-track smokes. Burlington `a100_body_video_smoke_burlington_guarded_per_player_rootcap8_runtime_20260630T215500Z` produced 84 scheduled BODY frames / 99 safe mesh+joint player-frames; Wolverine `a100_body_video_smoke_wolverine_guarded_per_player_rootcap8_runtime_20260630T215500Z` produced 63 scheduled BODY frames / 102 safe mesh+joint player-frames. This note is now superseded for current BODY status by the later reset-bound bbox-scaled full-track diagnostics (`a100_body_video_smoke_burlington_bboxscaled_resetcap075_runtime_20260701T001500Z`, `a100_body_video_smoke_wolverine_bboxscaled_resetcap075_runtime_20260701T002500Z`), which pass structural full-clip coverage for Burlington/Wolverine and have 0 selected overlay alignment failures but still lack world-MPJPE. Artifacts: `threed/racketsport/orchestrator.py`, `threed/racketsport/body_compute.py`, `threed/racketsport/worldhmr.py`, `threed/racketsport/body_joint_quality.py`, tests, ignored A100 run folders and review packets/bundles/overlays; tested: focused BODY runner/compute/video/overlay tests passed and real A100 BODY smokes completed on Burlington/Wolverine; next: create representative reviewed/equivalent BODY world-joint labels before promotion.
- [STATUS-HYGIENE/TRK/BALL/CAL/DATA/BODY/RKT/RPL/IOS] Codex manager continuation — built: continued the 2026-06-30 evidence pass on `main` without promoting gates; added a learned TRK crop-embedding exporter, dense CVAT-to-TrackNet label builder, CAL threshold/reprojection/world-validation reporting, refreshed RKT/SHOT/RPL audit, DATA missing-export proof, BODY live preflight proof, and synchronized docs. Artifacts include `runs/phase2/trk_reid_embedding_export_20260630T171133Z/person_reid_embeddings.json`, `runs/ball_tracknet_cvat_dataset_20260630T171321Z/ball_tracknet_cvat_dataset_manifest.json`, `runs/pickleball_pretraining/court_keypoint_20260628/diagnostic_20260630_cal_validation_eval/court_keypoint_no_tap_validation_eval.json`, `runs/cvat_imports/2026_06_30/data1_next_step_20260630T170631Z/data1_missing_input_manifest.json`, `runs/body_unblock_20260630T092502Z/live_recheck_20260630T170731Z.json`, and `runs/cvat_imports/2026_06_30/rkt_shot_replay_lane_audit_20260630T170539Z/rkt_shot_replay_lane_audit.json`; code artifacts include `threed/racketsport/person_reid_diagnostics.py`, `scripts/racketsport/export_person_reid_embeddings.py`, `threed/racketsport/ball_tracknet_cvat_dataset.py`, `scripts/racketsport/build_ball_tracknet_cvat_dataset.py`, `threed/racketsport/court_keypoint_eval.py`, and `scripts/racketsport/evaluate_court_keypoint_no_tap.py`. Tested: focused TRK/BALL/CAL/RKT suites and JSON schema/parse checks passed locally; `python3 -m ruff` passed for CAL files while `.venv` lacks `ruff`; iOS SwiftPM tests and generic simulator build/test-build passed in the iOS worker, but device capture remains blocked because the phone is offline/unavailable. Next: consume embeddings in a source-only identity repair and rescore TRK, materialize/fine-tune the dense TrackNet labels on GPU, import the missing Indoor CVAT export, fix CAL against real labeled geometry, and keep BODY/RKT/RPL/iOS unpromoted until their gates pass.
- [BALL-1/EVAL-0] Codex manager continuation — ran the first dense hidden-negative TrackNetV3 A100 fine-tune without promoting BALL. Materialized dataset: `runs/ball_tracknet_cvat_dataset_20260630T175124Z_materialized/` with 600/300/1151 frame PNGs and medians for Burlington/Wolverine/Outdoor. Remote training resumed from official `TrackNet_best.pt` in `/home/arnavchokshi/ball_runtime_isolated/20260630_tracknetv3/pickleball_dense_hidden_train_20260630T1812Z/`; the best checkpoint is sha256 `fe6701fb0f1a63be4b8e9463cd5c39dfa78ea5c4e8001daf469383c2b9dc6cba`. Local predictions/converted tracks and the benchmark are under `runs/ball_tracknet_finetune_20260630T1812Z/`; benchmark JSON: `runs/ball_tracknet_finetune_20260630T1812Z/cvat_benchmark/tracknet_dense_hidden_finetune_vs_cvat_20260630T183151Z.json`. Metrics improved versus raw A100 TrackNet across all 1,524 visible labels: F1@20 0.554 -> 0.626, precision@20 0.508 -> 0.606, recall@20 0.610 -> 0.647, hidden-FP rate 0.607 -> 0.342, teleports 61 -> 2. BALL remains unverified because F1/hidden-FP/contact timing are still below gate; contact cue coverage is still 12/24 reviewed contacts with only 5 within 2 frames.

---

## 6. Decisions log (append-only)

> Record any model swap, deviation, assumption, or resolved ambiguity so other agents see it.

- Stack baseline as of handoff: Fast SAM-3D-Body backbone + our world-grounding; YOLO26m + BoT-SORT-ReID; foot-lock to Z=0 + PhysPT/MuJoCo; racket PnP-IPPE; TrackNetV3→V5; RealityKit/USDZ native + Three.js/GLB web. Fallbacks: Fast SAM-3D-Body→original→NLF; YOLO26→YOLO11; SAT-HMR↔Multi-HMR 2; PhysPT→PHC. Single-camera product; multi-cam future; multi-view training-only.
- 2026-06-28 Codex: current status counts live only in the top truth snapshot, which supersedes all earlier parser/count notes. Historical `Sway*` iOS target/module names in this append-only log are also superseded by the current `Pickleball*` package names documented in `IMPLEMENTATION_PHASES.md`.
- 2026-06-28 Codex: "review visual ready" is not a production replay claim. Static review GLBs, `virtual_world_paddle_preview.html`, crop sheets, montage JPEGs, and overlay MP4s are inspection aids only unless their phase accuracy/performance gates pass.
- 2026-06-28 Codex: box-derived paddle candidates must never promote to canonical `racket_pose.json`. Only true paddle-face corners, masks/keypoints, CAD/reference pose, or ArUco/AprilTag/reference GT evidence can enter the RKT promotion path.
- 2026-06-28 Codex: BODY mesh-smoke availability is not BODY verification. Accepted-four BODY remains blocked until world-MPJPE and full-clip BODY coverage evidence exist; Outdoor additionally needs schedulable BODY output from canonical-safe tracks.
- 2026-06-28 Codex: widened court margins can improve player coverage numerically but are not canonical-safe by default. The Indoor diagnostic shows background/spectator-like boxes, so tracker promotion requires visual false-positive review plus labeled ID/spectator gates.
- 2026-06-29 Codex: user decision for this handoff: if the accepted-four CVAT-labeled dataset is being produced, skip the ad-hoc Step-3 player-marking wizard in `review_input_server.py` and use the CVAT export as the preferred TRK/BODY review input. A separate racket annotation workflow owns the improved paddle-highlight/label UI; this agent should consume its exported true-corner/reference labels when ready instead of asking for duplicate local crop-sheet review. The localhost review UI remains an optional quick-triage fallback, not the next required user action.
- 2026-06-30 Codex docs/status: live GPU state is H100 unavailable and A100 diagnostic-only. Verified `gcloud compute instances list` has no `body4d-gcp-prod`, `body4d-waker-ctrl` is running, and `pickleball-a100-spot-ase1a` is running idle A100-SXM4-40GB but lacks `/workspace`, `/workspace/pickleball`, and `/opt/conda/envs/fast_sam_3d_body`. Promotion evidence remains unchanged: H100-specific rows require a recreated H100/container path plus recorded gate outputs; A100 outputs require explicit row-level acceptance before promotion; CVAT detector datasets and accepted-four artifacts remain inputs/review evidence, not `DATA-1` completion or `VERIFIED` gates.
- 2026-06-30 Codex overnight manager: A100 detector smoke may guide the next data/model iteration, but it does not update the model registry or promote TRK. Current blockers are evidence-backed and unchanged: TRK fails IDF1/switch/FP/coverage criteria, BALL lacks full-horizon/gate metrics, CAL no-tap is under-confident at the evaluator threshold, BODY lacks representative world-MPJPE gates plus broader trusted full-clip/Outdoor coverage, RKT has only box-derived paddle rectangles, RPL has review-static GLBs only, and iOS still lacks simulator/device runtime capture proof.
- 2026-06-30 Codex BODY per-player guarded full-track smoke: A100 Burlington `a100_body_video_smoke_burlington_guarded_per_player_rootcap8_runtime_20260630T215500Z` and Wolverine `a100_body_video_smoke_wolverine_guarded_per_player_rootcap8_runtime_20260630T215500Z` proved the real runtime could emit safe scheduled player-frame BODY output, but this evidence is superseded by the reset-bound bbox-scaled full-track diagnostics. Current synced BODY evidence is Burlington `a100_body_video_smoke_burlington_bboxscaled_resetcap075_runtime_20260701T001500Z` (265 player-frames, coverage 0.974265, 0 selected overlay alignment failures / 9 warnings, max track-anchor residual 0.748754 m) and Wolverine `a100_body_video_smoke_wolverine_bboxscaled_resetcap075_runtime_20260701T002500Z` (163 player-frames, coverage 0.964497, 0 selected overlay alignment failures / 2 warnings, max track-anchor residual 0.721875 m). Both remain unpromoted because representative world-MPJPE labels/equivalent evaluator evidence are missing and Outdoor has no trusted scheduled BODY output. Do not call BODY accurate or verified from these smokes.
- 2026-06-30 Codex BODY continuity guard: the root-motion jumps above were traced to source-track `world_xy`/bbox discontinuities. `threed/racketsport/body_compute.py` now fails closed before BODY scheduling by skipping player-frames whose source-track speed would exceed the 10 m/s BODY review threshold, and records `track_continuity` diagnostics plus `unsafe_track_continuity` skips in `body_compute_execution.json`. CPU replays at `runs/body_joint_goal_smoke_20260630T001407/body_track_continuity_guard_20260630T204210Z/` schedule 99 Burlington and 102 Wolverine safe player-frames, skip 173 and 67 unsafe player-frames, and leave zero scheduled track-root speeds over threshold. BODY remains unverified until TRK continuity/broader trusted coverage and world-MPJPE gates pass.
- 2026-06-30 Codex BODY root-speed cap, bbox scaling, and smoothing reset: `BodyStageRunner` now defaults grounded BODY root motion to an 8 m/s cap while preserving explicit overrides, scales tracked bboxes into the materialized BODY frame before invoking Fast SAM, and resets temporal smoothing when the previous smoothed state would exceed the 0.75 m track-anchor bound. The earlier bounded Wolverine root-cap smoke remains diagnostic only, and the later reset-bound bbox-scaled Burlington/Wolverine runs now pass structural full-clip coverage and selected-overlay alignment checks. They still do not promote BODY because world-MPJPE/equivalent accuracy evidence is missing, `pipeline_run.json` remains `blocked`, and Outdoor has no trusted scheduled BODY output.
- 2026-07-01 Codex BODY compact gate/report sync: reset-bound Burlington/Wolverine review queue/template/frame assets and `body_mesh_readiness.json` were synced locally without pulling huge `smpl_motion.json`/`skeleton3d.json` files. `threed/racketsport/eval/body_gate_report.py` now scores world-MPJPE from compact `body_world_label_packet.json` predictions when finalized reviewed labels exist, and compact copies no longer stay blocked only because the heavyweight motion files are absent. The regenerated `runs/body_joint_goal_smoke_20260630T001407/body_gate_report_resetcap075.json` reports both clips as mesh-smoke `pass`, full-clip BODY `pass`, overlay `warning` with 0 alignment failures, BODY label review `blocked_finalization` with 27 / 20 selected samples, and `blocked` on `missing_world_mpjpe_gate`, `body_joint_overlay_warning_review_required`, and `body_world_label_finalization_blocked`.
- 2026-07-01 Codex BODY compact packet-quality sidecar: `threed/racketsport/body_joint_quality.py` can now use compact `body_world_label_packet.json` predictions as the joint source when heavyweight motion files are absent, and `run_body_video_smoke.py` writes `body_joint_quality_from_packet.json` automatically. Existing reset-bound sidecars are `quality_checked_needs_accuracy_gate` with no quality blockers over Burlington 265 / Wolverine 163 packet samples; they warn that the current synced packets lack temporal-reset metadata for compact-only root-speed checks and contain no mesh vertices. New label packets preserve `temporal_smoothing_reset` metadata for future compact checks. `body_gate_report_resetcap075.json` now includes a `Packet quality` status column and carries those warnings without adding promotion blockers.
- 2026-07-01 Codex BODY label-review gate report: `threed/racketsport/eval/body_gate_report.py` now includes a `body_label_review` section and Markdown `Label review` column. Ready review assets remain non-promoting, and blocked finalization reports add `body_world_label_finalization_blocked`. Current reset-bound Burlington/Wolverine bundles have 27 / 20 selected samples and no missing review frames, but fail-closed finalization reports mark them `blocked_finalization` because their templates still have 0 accepted samples, `not_ground_truth=true`, and selected overlay warnings. BODY remains blocked on `missing_world_mpjpe_gate`, `body_joint_overlay_warning_review_required`, and `body_world_label_finalization_blocked`.
- 2026-07-01 Codex BODY overlay warning sample surfacing: `body_gate_report.py` now includes severity-sorted `body_review_overlay_alignment.warning_samples` and `competing_player_warning_count` so review starts from the worst selected overlay instead of only a count. Current reset-bound report surfaces 9 Burlington and 2 Wolverine warning samples; the worst Burlington sample is `frame_000047_player_2`, a target-box joint-cloud alignment warning rather than a floor-anchor projection failure, and the regenerated same-frame competing-player check reports 0 competing-player warnings, so it is not a proven wrong-track assignment.
- 2026-07-01 Codex BODY review-decision pipeline: `scripts/racketsport/run_body_world_label_review_decision_pipeline.py` now runs the safe merge/apply/finalize sequence for `/body` overlay-review decisions. A real no-decision diagnostic run under `/tmp/pickleball_body_pipeline/` kept Burlington and Wolverine blocked, with 0 accepted overlay warnings, 9 / 2 missing overlay-review decisions, `no_accepted_corrections`, no finalized `body_world_joints.json`, and blockers `selected_samples_not_all_accepted`, `selected_samples_have_overlay_warnings`, `template_not_reviewed`, `template_marked_not_ground_truth`, and `template_not_trusted_for_world_mpjpe`.
- 2026-07-01 Codex fresh A100 BODY submit check: live GCP state was project `gifted-electron-498923-h1`, account `hello@swayformations.com`, `body4d-waker-ctrl` running, and `pickleball-a100-spot-ase1a` running idle with CUDA Torch on one A100-SXM4-40GB. A bounded Burlington smoke first reproduced the expected A100/H100 manifest mismatch (`models/MANIFEST.json` still points BODY checkpoints at `/workspace/checkpoints/...`), then passed with `--manifest runs/body_joint_goal_smoke_20260630T001407/a100_body_manifest.json` and `--fast-sam-repo /home/arnavchokshi/body_runtime/Fast-SAM-3D-Body`. The synced run `runs/body_joint_goal_smoke_20260630T001407/a100_body_video_smoke_burlington_fresh_max1_a100manifest_20260701T024336Z/` has `body_runtime_ran=true`, 1 extracted frame, 4 scheduled player-frames, 4 world-joint/mesh outputs, 70 joints per frame, 18,439 mesh vertices per frame, no quality blockers, 4 review samples, and 2 overlay alignment warnings. It is a runtime/submission smoke only: full-clip coverage is 0.014706, `full_clip_body_coverage_below_threshold` and `missing_world_mpjpe_gate` still block promotion.
- 2026-06-30 Codex manager continuation: learned TRK embeddings and dense BALL TrackNet labels are implementation/evidence advances, not status promotions. TRK stays blocked until an embedding-aware or retrained model candidate passes labeled IDF1/spectator/ID-switch/coverage gates. BALL stays blocked until a trained candidate improves reviewed F1/hidden-FP/teleport metrics and passes contact timing. CAL stays blocked because low-threshold correspondences failed reprojection/world/temporal validation. DATA-1 still needs the missing 900-frame Indoor CVAT export.
- 2026-06-30 Codex goal-continuation evidence pass: DATA remains blocked after a 58-ZIP search found only duplicate Burlington/Wolverine/Outdoor CVAT exports; iOS remains blocked because the paired iPhone is offline and no `Documents/captures` package exists; TRK embedding analysis found 0.963704/0.986169/0.999300 track-centroid cosine min/mean/max, so embedding-only repair is unsafe; CAL CPU rerun blocks all three active clips; RKT runtime readiness is blocked with 0/6 components ready. No row moved to `VERIFIED`.
- 2026-06-30 Codex continuation wave 2: fresh iOS live check still shows paired iPhone unavailable/offline, no available simulator device entries, and no `Documents/captures` package; fresh DATA/RKT asset check still finds no Indoor CVAT export and no true paddle-face/CAD/reference/ArUco evidence, with the DATA recheck recorded at `runs/cvat_imports/2026_06_30/data1_indoor_export_recheck_20260630_goal_continuation/`; BODY audit confirmed floor-anchor projection was no longer the current Burlington/Wolverine blocker, and the later reset-bound A100 smokes clear selected-overlay alignment failures for those two clips, but world-MPJPE remains blocked; BALL error analysis at `runs/ball_tracknet_finetune_20260630T1812Z/error_analysis_20260630_goal_continuation/` cross-checks the fine-tune benchmark, adds annotated review sheets for the concrete error spans, and keeps BALL unverified.
- 2026-06-30 Codex continuation wave 3: TRK embedding verification confirms 6,918/6,918 learned rows join to raw detections; the selector/CLI now consumes them in the scored diagnostic sweep at `runs/phase2/trk_embedding_source_selection_20260630T223103Z/`, but centroid cosine similarity remains too high for embedding-only repair and the best sweep still fails IDF1/switch/FP/coverage gates; CAL fresh CPU diagnostic again has 0 gate-passed clips and the overlay review shows the low-threshold predictions are not close to the trusted calibration projection; BODY remains blocked on missing world-MPJPE plus broader trusted coverage, not floor-anchor projection; RKT/SHOT/RPL remain preview-only with 0 true-corner/reference frames, 0 production replay-ready clips, and 0 reviewed pickleball shot truth labels. No row moved to `VERIFIED`.
- 2026-06-30 Codex continuation wave 4: RKT true-corner review inputs are consolidated at `runs/cvat_imports/2026_06_30/rkt_true_corner_review_manifest_20260630_goal_continuation/`. The accepted-four packets require 755 true-corner labels and have 174 rendered crop-sheet crops; CVAT paddle-box packets add 2,211 rectangle-derived review candidates across Burlington/Wolverine/Outdoor. They remain `review_only_no_rkt_promotion` with 0 true-corner/reference/CAD/ArUco labels and must not be copied into canonical `racket_pose.json`. A broader DATA search across repo, Downloads, Desktop, `cvat_upload/exports`, and `/Users/arnavchokshi/Desktop/CV_pipeline/CVAT` scanned 58 deduped ZIPs and 6 CVAT XML ZIPs but still found 0 exact or Indoor-equivalent CVAT exports for `04_indoor_doubles_fwuks_0500_long_mid_baseline_cvat_for_video_1.1.zip`. BALL A100 checkpoint audit found the current TrackNet resume target is epoch 21 and worse than epoch 19 best; a follow-up script audit led to the hard-negative materializer from epoch-19 best, and the scored A100 hard-negative resume is diagnostic-only rejected because it improves train clips while regressing held-out Outdoor. Canonical H100 BODY promotion remains blocked: local manifest preflight reports 18/18 declared H100 checkpoint files failing integrity, the H100 instance is absent, and the A100 has only a partial noncanonical home-path SAM-3D-Body setup with missing MoGe/canonical runtime paths. This is superseded for Burlington/Wolverine diagnostic availability by the later reset-bound bbox-scaled A100 smokes above, but not for promotion.
- 2026-06-26 Codex: canonical server filenames are the `IMPLEMENTATION_PHASES.md §0.2` names (`person_fast.py`, `audio_pop.py`, `footlock.py`, `racket6dof.py`, etc.). Older MVP shorthand names such as `person_fasttier.py`, `audio_events.py`, `foot_lock.py`, and `racket_pose6dof.py` are retired aliases and should not be implemented.
- 2026-06-26 Codex: external existence check found official Ultralytics YOLO26 docs and assets plus public Fast SAM-3D-Body project/GitHub pages. This verifies the names exist, not our target H100 performance, checkpoint access, or commercial license posture.
- 2026-06-26 Codex: CAL-1 uses a shared court world frame with origin at net center, +x across court width, +y toward the far baseline, +z up, and meters for artifact coordinates. Regulation dimensions are stored in feet/inches and converted at the API boundary: pickleball 20x44 ft, 7 ft NVZ, 22 ft net, 34/36 in net heights; tennis 78 ft length, 36 ft doubles width, 27 ft singles width, 21 ft service line, 42 ft net between posts, 36/42 in net heights.
- 2026-06-26 Codex: Fast-SAM-3D-Body must use the isolated `/opt/conda/envs/fast_sam_3d_body` env on the H100. Detectron2 builds successfully there only after installing CUDA 12.4 toolkit plus conda-forge GCC/G++ 13 and setting `CUDAHOSTCXX`; GCC 14 is rejected by CUDA 12.4.
- 2026-06-26 Codex: Fast-SAM-3D-Body H100 smoke benchmark is successful but not variant approval. Measured on one sample image with one detected person, YOLO11n detector, MoGe2 FOV, no SAM2 segmentation: average 326.91 ms over five warm runs and 4718.04 MB peak allocated GPU memory. EVAL-0 still requires real test clips, side-by-side artifacts, and the §1.6 approval gate.
- 2026-06-26 Codex: ENV-4 now emits clip-QC and `capture_quality` from sampled ffmpeg frames as the deterministic local fallback. This validates scoring and metadata plumbing, but it is not the Phase 0 NvDEC performance gate; true H100 NvDEC decode throughput still needs implementation and measurement on real clips.
- 2026-06-26 Codex: ENV-3 uses checklist ownership folders (`ios/Capture/`, `ios/Calibration/`, etc.) with Swift package target names (`PickleballCapture`, `PickleballCalibration`, etc.) so task ownership stays aligned with `BUILD_CHECKLIST.md` while import/module names stay idiomatic. The Xcode project uses Xcode 26 file-system-synchronized app sources plus a local package reference to `ios/Package.swift`.
- 2026-06-26 Codex: H100 container ffmpeg exposes CUDA/CUVID decode support, but a short synthetic 1080p60 benchmark made CPU decode faster than CUDA because CUDA startup/copy overhead dominates that clip. ENV-4 decode backend should be selected from real clip measurements; do not hardcode CUDA as always faster.
- 2026-06-26 Codex: MuJoCo/MJX is installed as an isolated runtime env at `/opt/conda/envs/racketsport_mjx` with `jax[cuda12]` and `mujoco-mjx[warp]`, following the official package guidance. Keep it separate from the Fast-SAM env unless a later integration task deliberately consolidates environments.
- 2026-06-27 Codex: to increase parallel throughput, this wave added CPU-only downstream scaffolds before their strict DATA/GPU dependencies were VERIFIED, but did not promote checklist statuses to DONE/VERIFIED. Treat these as implementation building blocks only; acceptance still waits on DATA-1 labels, real stage artifacts, H100 gate runs, and any required human approval.
- 2026-06-27 Codex: H100 real-clip decode benchmark on three ignored DATA-1 clips again favored CPU ffmpeg over CUDA ffmpeg for the sampled 1080p H.264 clips. This is not a global default; rerun on future 4K/120/240 fps captures before selecting backend policy.
- 2026-06-27 Codex: BALL-1/BALL-3/MET-1/RPT-2 now have CPU-only primitives that are useful for downstream integration, but they are not substitutes for the documented model/data/acceptance gates. Keep rows unpromoted until real artifacts and H100 gate runs exist.
- 2026-06-27 Codex: BODY-1/BODY-3/FOOT-2/RPT-3 scaffold primitives were reviewed and hardened for input validation, but remain scaffold-only. Do not treat their artifacts as measured model, physics, or rendering outputs until the documented GPU/device gates are run.
- 2026-06-27 Codex: SHOT-1/CAL-3/EVAL-3/BODY-4 scaffold primitives landed as CPU-only planning, validation, and queue plumbing. They increase implementation surface area, but do not change the strict gating story: real data, model training, H100/device evaluation, and human approval gates still control DONE/VERIFIED.
- 2026-06-27 Codex: IOS-1 through IOS-6 now have Swift package contracts and unit tests, but they are intentionally scaffold-only until the local Xcode `IDESimulatorFoundation`/`DVTDownloads` blocker is repaired and real AVFoundation/ARKit/Vision/CoreML/RealityKit device paths are implemented and tested.
- 2026-06-27 Codex: EVAL-4 gained artifact-readiness contracts only. This helps prevent accidental end-to-end runs on incomplete outputs, but final acceptance still requires the documented real 20-minute clip, performance, cost, and accuracy gates.
- 2026-06-27 Codex: DATA-3/DATA-4 and serving readiness validators are scaffold-only manifest/report tools. They improve data/runtime hygiene but do not download data, train models, select variants, start Triton, use the GPU, or satisfy DATA/RKT/EVAL gates.
- 2026-06-27 Codex: DATA-1 label-draft audit is a pre-promotion quality check only. It intentionally reports draft readiness separately from dataset label readiness and never writes reviewed labels into `data/testclips`.
- 2026-06-27 Codex: eval-summary aggregation is read-only and CPU-only. It is useful for researcher handoff/reassessment, but it does not run evaluation or convert missing/not-measured metrics into passing gates.
- 2026-06-27 Codex: scaffold-tool indexing is an inventory/handoff helper only. It reports CLI/test/schema gaps and intentionally does not execute scaffold commands or imply build progress.
- 2026-06-27 Codex: human approved a temporary `PROTOTYPE-GATE`: run the `ACCURACY_AND_TRAINING.md §1/§11` teacher → confidence/physics/reprojection filter → distill loop on the best 5 existing clips now, produce draft labels and qualitative overlays, and defer the full 24-clip matrix, 120/240 fps subset, and ArUco racket-6DoF GT to a later wave.
- 2026-06-29 Codex: iOS is deferred for this phase and blocks nothing beyond Swift-package code/unit-test hygiene. The prior first-launch `DVTDownloads` Xcode failure is superseded by the user's successful `sudo xcodebuild -runFirstLaunch`; SwiftPM tests and app build-for-testing have passed in local runs, and the shared scheme now includes a minimal hosted app test target. Full simulator test execution on this host is still blocked by disk space, and no physical-device verification is required for the current prototype gate.
- 2026-06-27 Codex: standing approval policy is active: auto-finalize only an obvious/Pareto-dominant winner or the only variant passing the gate and log it here; otherwise render comparison videos, mark `PENDING-APPROVAL`, and stop for human review.
- 2026-06-27 Codex: DATA-2 is `PROTOTYPE-GATE`, not full `DONE`/`VERIFIED`: the 5 selected clips now have draft label packages, YOLO26m teacher detections for players/balls/rackets where available, qualitative overlay videos, and a capped review bundle. Remaining full DATA-2 work includes real Fast SAM-3D-Body/TrackNet/pose teacher wiring, distillation/training, and full data-matrix gates.
- 2026-06-27 Codex: OpenCV could not decode two AV1 source clips on H100, so overlay rendering falls back to `runs/label_frames/<clip>/` sampled frame packs. This produced watchable sampled overlays for `ppa_austin_md_qf_1200_high_baseline` and `ppa_singles_0500_high_baseline`; the other three overlays rendered from full source videos.
- 2026-06-27 Codex: the earlier broadcast/360 prototype clips are rejected for this wave, and `side_view_game5_0100_high_side_fence` is now `DEFERRED_REJECTED_SIDE_FISHEYE` because its side/fisheye-distorted view is not representative of normal user uploads. Superseded calibration note: Burlington is now retired for court calibration because fisheye curvature bends court lines, and readiness reports treat that as a court-calibration blocker rather than a pass; it remains in the accepted-four packet for BODY/player/ball/paddle smoke. Use Wolverine, Outdoor webcam, and Indoor doubles for active court-calibration seed work.
- 2026-06-27 Codex: TrackNetV3 checkpoints are verified on H100 and a real 2-second Burlington runtime smoke produced a schema-valid `ball_track.json`, but this does not make BALL-1 or BALL-3 `VERIFIED`; TrackNet output lacks probabilistic confidence, so the adapter records confidence semantics as official visibility mapped to `1.0/0.0`, and Phase 5 still needs contact-window generation plus labeled F1/timing gates.
- 2026-06-29 Codex: BALL gained CPU-only prep utilities for the next GPU pass: `ball_line_calls` classifies bounce locations against court/NVZ lines with an explicit `ball_bounce_location_only` scope, and `ball_model_runtime_profile` wraps future TrackNet/TOTNet/PB-MAT commands without claiming accuracy. These artifacts are not officiating, not BALL `VERIFIED`, and not H100 runtime proof until real GPU runs, reviewed overlays, labels, and phase gates pass.
- SUPERSEDED HISTORICAL - 2026-06-27 Codex: an earlier parser-count snapshot is no longer carried in this active checklist. Use the top truth snapshot only. The invariant still stands: do not promote scaffolds, smoke artifacts, or prototype label packages to `VERIFIED` until a real clip passes a real numeric acceptance gate through the fail-closed spine.
- 2026-06-27 Codex: accepted-four prototype calibration overlays now render on real review frames and were visually checked locally after H100 generation. Green homography-projected court lines align on all four clips; orange elevated net/top-post lines are only a qualitative 3D sanity layer from estimated intrinsics/extrinsics. H100 solvePnP produced non-identity extrinsics for all four, but zero four-point reprojection error is not a meaningful acceptance gate.
- 2026-06-27 Codex: historical vertical-slice note: on `wolverine_mixed_0200_mid_steep_corner`, the fail-closed orchestrator once ran calibration and precomputed-detection tracking, wrote `tracks.json`, then stopped at BODY because no BODY runner existed yet. This is now superseded by the registered `BodyStageRunner`; keep the note only as pre-runner history.
- 2026-06-27 Codex: accepted-four TrackNetV3 10-second smoke windows now exist under `runs/eval0/prototype_gate_h100_v2/*/tracknet_smoke_0000_0010/` with H.264 `ball_track_overlay_h264.mp4` review videos. Visible counts were Burlington 599/600, Wolverine 288/300, Outdoor webcam 600/600, and Indoor doubles 300/300 after correcting timestamps to each smoke input's `ffprobe avg_frame_rate`. This proves real TrackNet inference and review rendering on the accepted clips, but it is not BALL `VERIFIED` because no ball-label F1, contact-window, timing, or full-clip gate has passed.
- 2026-06-27 Codex: a real FastSAM-3D-Body probe ran on Burlington frame 0 with four tracked player boxes and returned four person outputs with mesh/keypoint/pose tensors. The probe is intentionally `probe_only_not_verified` and writes no BODY contract artifacts; BODY remains blocked on a defensible camera-to-court/world-SMPL conversion plus foot/contact/physics gates.
- SUPERSEDED HISTORICAL - 2026-06-27 Codex: a truthfulness reset superseded an earlier parser-count note. Historical counts are intentionally omitted from the active checklist; use the top truth snapshot only. Count-only gates now identify themselves as `presence_check.*`; e2e artifact completeness gates identify themselves as `artifact_check.*` and do not count toward `VERIFIED`.
- 2026-06-27 Codex: TRK-1 now has a registered default real runner (`RealYOLO26BoTSORTReIDTrackingRunner`) that refuses to use `detections.json`, verifies the manifest `yolo26m` sha256 before model load, and invokes Ultralytics BoT-SORT with `with_reid: True` through `configs/racketsport/botsort_reid.yaml`; precomputed tracking is retained only behind `--tracking-mode precomputed`. TRK-1 remains `IN-PROGRESS`, not `DONE`/`VERIFIED`, until an H100 real-clip run and the documented labeled IDF1/stability/spectator gates pass.
- SUPERSEDED HISTORICAL - 2026-06-27 Codex: old row counts after the TRK-1 real-runner registration are intentionally omitted from this active checklist; use the top truth snapshot only.
- 2026-06-27 Codex: BODY-1/BODY-2 now have a registered `BodyStageRunner` that is `real_model=true`, verifies the registry Fast SAM-3D-Body/MHR/MoGe/YOLO26m sha256s before load, passes tracked player boxes to Fast SAM-3D-Body with detector model `yolo26m`, and converts runtime tensors into court/world artifacts via `court_calibration.json`; the contract preserves `mesh_vertices_world` when the runtime returns vertices. This supersedes the earlier "no runner registered for body" note. BODY remains `SCAFFOLD`, not `DONE`/`VERIFIED`, because local-only execution lacks `/opt/fast-sam-3d-body` and `/workspace/checkpoints/body4d/...`, only a one-frame Burlington H100 spine smoke has passed, and no world-MPJPE/body accuracy gate has passed.
- 2026-06-28 Codex: the current strict no-click ball review track is `ball_track_fusion_temporal_vball100_localtraj.json` under each accepted clip's `tracknet_smoke_0000_0010/` directory. It fuses target-court TrackNet, the temporal path, and VballNet Fast/V1 verifier tracks, then applies a local trajectory-consistency filter. Human ball clicks remain held-out benchmark labels only. The four-clip prototype benchmark improved teleports from 8 to 0 and hidden false positives from 0.425 to 0.294 versus the looser fusion track, but recall dropped from 0.563 to 0.509, so this is a stricter review artifact rather than BALL `VERIFIED`.
- 2026-06-28 Codex: default calibration now writes `court_line_evidence.json` for every calibration run it can inspect. With a trusted sidecar/manual seed, it samples the upload video before still frames and requires sport-specific semantic line evidence (pickleball NVZ/centerlines; tennis service lines) plus trusted top-net evidence before a video-backed run can proceed to tracking. With only a random video and no trusted calibration seed, it still writes fail-closed `court_zones.json`, `net_plane.json`, and `court_line_evidence.json`, then stops before creating `court_calibration.json`. This is automatic evidence plumbing, not a verified no-tap calibration solve; CAL-3 remains `SCAFFOLD` until the trained/heuristic no-tap solver passes the real clip gates.
- 2026-06-28 Codex: adaptive compute planning now has a CPU-safe `frame_compute_plan.json` builder that routes frames to baseline, skeleton preview, deep mesh, or human review based on contact windows, missing expected players, low track confidence, and ball uncertainty. It also has a CPU-safe `body_compute_execution.json` manifest that records which `deep_mesh_windows` BODY will run and which frames were skipped. These are orchestration/review artifacts only; they do not promote BODY without real `smpl_motion.json`/`skeleton3d.json` outputs and accuracy gates.
- 2026-06-28 Codex: `BodyStageRunner` now consumes existing `frame_compute_plan.json` `deep_mesh_windows` before invoking Fast SAM-3D-Body. The runner writes `body_compute_execution.json` before model/checkpoint verification so failed runs still expose what would have been scheduled. Without a frame plan, the direct BODY runner keeps its historical all-track-frame behavior.
- 2026-06-28 Codex: the fail-closed orchestrator writes best-effort review helpers after partial runs when possible: `frame_compute_plan.json` and `body_compute_execution.json` if `tracks.json` exists and `virtual_world.json` if `court_calibration.json` exists. 2026-06-29 audit update: unprotected helper files are regenerated from current inputs when possible instead of reusing stale `frame_compute_plan.json`, while successful BODY-owned plan/execution/readiness artifacts are preserved; these post-run review artifacts do not change a failing/blocked pipeline status and are not acceptance gates.
- 2026-06-28 Codex: post-run `frame_compute_plan.json` generation now honors explicit `--ball-source`/`ball_source_path` when root `ball_track.json` is not present, so BODY-blocked prototype runs can still expose strict no-click ball uncertainty in review metadata before BALL has run.
- 2026-06-28 Codex: added `scripts/racketsport/render_player_track_overlay.py` for CPU/OpenCV player-track review overlays, optional H.264 transcoding, and `player_track_overlay_index.json` packet indexing. Accepted-four prototype packets now include H.264 player overlay videos, but these remain qualitative review artifacts and do not satisfy tracking accuracy gates.
- 2026-06-28 Codex: H100 read-only check found `/workspace/checkpoints/body4d/sam-3d-body-dinov3/model.ckpt`, `assets/mhr_model.pt`, `/opt/fast-sam-3d-body`, and an idle H100 available, but the remote `/workspace/pickleball` checkout is stale/dirty and lacked the local `BodyStageRunner` CLI path. The remote working tree was not overwritten; the later BODY smoke used an isolated copied workspace instead.
- 2026-06-28 Codex: racket 6DoF now has deterministic camera-space PnP-IPPE primitives, projection checks, SE(3) smoothing, rebound consistency, and camera-to-`court_Z0` pose conversion with frame/unit/source/reprojection metadata in `racket_pose.json`. RKT-1 remains `SCAFFOLD` because the trained detector/keypoint/mask runner and ArUco/GT face-angle gate are still missing.
- 2026-06-28 Codex: `virtual_world.json` now assembles the inspectable `court_Z0` state from existing court calibration, tracks, BODY joints/mesh vertices when present, ball track, and racket pose when present. The accepted-four prototype worlds show court/tracks/ball; Burlington, Wolverine, and Indoor include scheduled-frame H100 BODY mesh samples, while Outdoor still warns `missing_mesh_vertices`. All accepted clips still warn `missing_paddle_pose`; this helps human review but does not replace the GLB/USDZ replay renderer or promote RPL-1.
- 2026-06-28 Codex: when `racket_pose.json` exists, `virtual_world.json` now materializes the paddle face as `mesh_vertices_world` plus two triangle `mesh_faces` using the known paddle dimensions and pose. This gives the inspectable world a renderable paddle mesh, but RKT/RPL remain scaffold until real detector/GT gates and GLB/USDZ export exist.
- 2026-06-28 Codex: `RacketStageRunner` is now registered in the fail-closed spine for explicit `racket_candidates.json` four-corner paddle candidates. It writes `racket_pose.json` only when PnP-IPPE reprojection and ambiguity checks pass, and fails closed otherwise. This supersedes the older "no racket runner" note, but RKT-1 stays `SCAFFOLD` because detector/SAM2/keypoint/CAD generation and ArUco/AprilTag GT gates are still missing.
- 2026-06-28 Codex: `racket_candidates.json` is now a registered schema artifact (`artifact_type=racketsport_racket_candidates`) with strict four-corner, confidence, paddle-dimension, and extra-field validation before PnP-IPPE runs. This improves fail-closed paddle review hygiene, but it still does not provide detector-generated true paddle corners, SAM2/keypoint/CAD pose generation, or ArUco/AprilTag GT verification.
- 2026-06-28 Codex: an isolated Burlington BODY stage smoke ran on the H100 from `/workspace/pickleball_codex_body_smoke` without mutating the dirty remote `/workspace/pickleball` checkout. Command: `scripts/gpu-eval-run.sh /opt/conda/envs/fast_sam_3d_body/bin/python -m threed.racketsport.orchestrator --clip burlington_gold_0300_low_steep_corner --inputs runs/eval0/prototype_gate_h100_v2/burlington_gold_0300_low_steep_corner --out runs/eval0/prototype_gate_h100_v2/burlington_gold_0300_low_steep_corner --stage body --tracking-mode precomputed --max-frames 1`. Result: pipeline `status=pass`, 4 players, 1 frame, 4 BODY samples, 70 skeleton joints, and 18,439 mesh vertices per player frame in pulled-back `smpl_motion.json`; BODY remains `SCAFFOLD`, not `VERIFIED`, because no labeled world-MPJPE gate, foot/contact/physics gate, or multi-clip/full-window gate has passed.
- 2026-06-28 Codex: added a prototype `racket_candidates.json` builder from existing draft YOLO26m racket-box labels plus review-packet indexing for racket candidate and racket-stage diagnostics artifacts. It produced candidate-only artifacts for the accepted four clips (Burlington 42 frames, Wolverine 36, Outdoor webcam 397, Indoor doubles 280). The default fail-closed `RacketStageRunner` wrote `racket_stage_diagnostics.json` and rejected all generated box-corner candidates as IPPE-ambiguous, so no `racket_pose.json` or virtual-world paddle frames were claimed. This is useful paddle-lane setup and review surfacing, not RKT verification.
- 2026-06-28 Codex: accepted-four paddle candidate overlays now render into the review packet as H.264 videos with explicit 960x540 label-coordinate to 1920x1080 video-coordinate scaling. Burlington and Wolverine cover all current candidate frames from the existing 10-second smoke videos; Outdoor webcam and Indoor doubles now cover all current candidate frames after recovering 30-second source clips into each `racket_candidates/source_0000_0030.mp4`. Treat these overlays as human-review aids only; they do not convert draft box corners into valid paddle pose or RKT verification.
- 2026-06-28 Codex: ambiguous paddle poses from draft box-corner candidates may be materialized only through the explicit preview path: `racket_pose_preview.json` plus `virtual_world_paddle_preview.json`. This keeps canonical `racket_pose.json` fail-closed while giving human reviewers a paddle mesh in the court world. Preview artifacts must keep `not_gate_verified=true`/`ambiguous_paddle_pose` language until true paddle corners and GT gates exist.
- 2026-06-28 Codex: virtual-world JSON artifacts may fill missing ball `world_xyz` by inverse-projecting visible 2D ball detections onto the court plane with the calibration homography, but those points must be marked `approx=true` because no ball height is estimated.
- 2026-06-28 Codex: generated Three.js `virtual_world_paddle_preview.html` pages are human-review aids only. They are useful for visually inspecting court/player/ball/paddle alignment from existing JSON, but they do not replace GLB/USDZ export, RealityKit/Three replay integration, or replay acceptance gates.
- 2026-06-28 Codex: paddle 6DoF candidate order is refreshed to true-corner IPPE as the gate, GigaPose/FoundPose as RGB/CAD scorers, SAM2/Grounded-SAM2 as candidate generators, and FoundationPose as a depth/reference-path candidate. This is a technology-plan update only; no FoundationPose/GigaPose/FoundPose/SAM2 runner is claimed until model/runtime probes, paddle CAD/reference inputs, and GT gates exist.
- 2026-06-28 Codex: accepted-four `body_compute_execution.json` manifests now reflect promoted human contact windows from `runs/review_inputs/pickleball_cv_review_latest.json`. Burlington schedules 3 BODY frames / 9 player-frames, Wolverine 4 / 12, Indoor doubles 2 / 6, and Outdoor webcam still schedules 0 / 0 because player coverage and uncertainty keep it fail-closed. These are scheduling/review artifacts only, not BODY accuracy verification. Missing ball frames inside a present ball track count as uncertainty; sparse mesh outputs should preserve surrounding track-only frames in review worlds.
- 2026-06-28 Codex: `benchmark_person_trackers.py` can now take `--ball-track`, `--contact-windows`, and `--expected-players` so each tracker candidate writes candidate-local `frame_compute_plan.json` and `body_compute_execution.json` alongside `tracks.json`. The benchmark report includes BODY scheduled frame/player-frame counts, making adaptive frame-rating impact reviewable per tracker variant. This is scheduling/review metadata only; it does not satisfy TRK/BODY accuracy gates.
- 2026-06-28 Codex: added CPU-only replay review GLB export from `virtual_world_paddle_preview.json`. `build_replay_review_export.py` writes static court/player/body/ball/paddle review GLBs under `replay_review/` plus a root `replay_scene.json`, and the review packet indexes the GLB refs. This removes `replay_scene.json` from the accepted-four missing-artifact list but keeps RPL-1 `SCAFFOLD`: the output is not animated, compressed, USDZ-backed, or acceptance-gate verified.
- 2026-06-28 Codex: hardened replay/e2e GLB artifact gates. `inspect_glb_file()` now parses GLB 2.0 headers, declared lengths, chunks, and glTF JSON asset metadata; `replay_eval` reports `glb_files_valid`, and `e2e_eval` reports `referenced_glb_files_valid` so placeholder `.glb` bytes fail even when paths exist. This is structural validation only and does not promote RPL-1 beyond `SCAFFOLD`; tested with `.venv/bin/pytest tests/racketsport/test_replay_export.py tests/racketsport/test_eval_report_replay_gates.py tests/racketsport/test_eval_e2e_gates.py tests/racketsport/test_eval_metrics.py -q` (`48 passed`).
- 2026-06-28 Codex: accepted-four `contact_window_candidates.json` artifacts surface uncertain prototype `labels/events.json` hints as review-only windows with `not_gate_verified=true` and `trusted_for_body=false`. They are indexed in the review packet but are not read by adaptive BODY scheduling.
- 2026-06-28 Codex: the local browser review UI saved explicit human contact times to `runs/review_inputs/pickleball_cv_review_latest.json`, and canonical root `contact_windows.json` files were promoted for all four clips from those human contacts. Contact-review is no longer a user-gated next action for those clips unless the promoted files are regenerated or invalidated. Promoted events use zero machine source scores plus `sources.human_review=1.0`; they still do not make BALL verified without label F1/contact-timing gates.
- 2026-06-28 Codex: paddle-pose research was refreshed for 2026 in `docs/racketsport/archive/paddle_pose_research_2026_06_28.md`. RKT-1 should now treat SAM 3 concept detection/tracking as the preferred runtime when approved, DINO-X/Grounded-SAM-2 as fallback, and FoundationPose/GigaPose/FoundPose plus PnP-IPPE/UKF/rebound checks as the 6DoF path. Current accepted-four paddle artifacts remain preview-only because their candidates are box-derived rather than true face corners or CAD/reference pose evidence.
- 2026-06-28 Codex: accepted-four `pipeline_readiness_e2e.json` reports are packet-indexed as artifact-plus-semantic review aids. They still are not accuracy gates. After regenerating from human court/contact inputs and scheduled H100 BODY outputs, Burlington, Wolverine, and Indoor have BODY `smpl_motion.json`/`skeleton3d.json` but remain blocked on BODY accuracy promotion plus downstream canonical RKT, metrics, copy, drill, and coach outputs; Outdoor still lacks BODY output because no world-mesh frames are scheduled.
- 2026-06-28 Codex: `scripts/racketsport/review_input_server.py` now reads `prototype_gate_h100_v2_review_actions.json` and surfaces global runtime/setup blockers plus per-clip blocker/action summaries inside the local browser review UI before the editable fields. This makes top-net clicks, contact notes, paddle examples, and general correction entry line up with the generated action checklist, but it remains human-review input plumbing rather than a verification gate.
- 2026-06-28 Codex: accepted-four `ball_inflections.json` artifacts are now built from `virtual_world.json` court-plane ball trajectories and packet-indexed as review-only contact cue inputs. Time suppression reduces raw trajectory-turn candidates to Burlington 13, Wolverine 9, Outdoor webcam 22, and Indoor doubles 12. These cues remove the "missing ball cue" setup gap but are still `not_gate_verified=true`; trusted contact windows still require audio-onset plus wrist-velocity cue artifacts or explicit human-accepted contact review.
- 2026-06-28 Codex: accepted-four `audio_onsets.json` and `wrist_velocity_peaks.json` artifacts are generated and packet-indexed. Outdoor webcam has 4 review-only audio energy peaks and Indoor doubles has 1 from available 30-second audio-bearing snippets clipped to the first 10 seconds; Burlington and Wolverine are blocked by `no_audio_stream`. Wrist artifacts remain blocked: Burlington/Wolverine/Indoor now have BODY skeletons and a provisional SAM-3D-Body/MHR70 semantic map, but the wrist-cue builder has not consumed that adapter/default explicit wrist indices yet; Outdoor still lacks `skeleton3d.json`. Machine cue fusion therefore remains fail-closed; the current canonical contact windows come from explicit human review, not from complete audio+wrist+ball cue triplets.
- 2026-06-28 Codex: `scripts/racketsport/build_contact_windows_from_cues.py` exposes the BALL-3 deterministic audio+wrist+ball cue fusion as a standalone CPU command, and `prototype_gate_h100_v2_review_actions.html` lists it for cue-related blockers before the human promotion fallback. It writes only canonical `contact_windows.json` and remains fail-closed; empty/missing/untrusted machine cues do not replace the current explicit human-reviewed contact windows.
- 2026-06-28 Codex: normalized the audio-onset review CLI so generated action commands can carry `--clip`, `--analysis-sample-rate-hz`, and `--frame-rate` when sibling track/ball artifacts expose FPS. The artifact now stores optional clip/frame-rate metadata and nearest-frame indices while remaining review-only and not trusted for contact by itself; tested with `.venv/bin/pytest tests/racketsport/test_audio_onsets.py tests/racketsport/test_review_action_manifest.py -q` (`14 passed`).
- 2026-06-28 Codex: accepted-four `racket_promotion_audit.json` artifacts are now generated and packet-indexed. They explicitly check that box-derived or preview paddle pose frames have not appeared in canonical `racket_pose.json`; all four accepted clips are `safe_preview_only` with `canonical_racket_pose_present=false`, `promoted_pose_frame_count=0`, and `unsafe_promoted_frame_count=0`. This is proof of fail-closed separation, not RKT promotion.
- 2026-06-28 Codex: added global CPU-only `racket_model_runtime_readiness.json` for the premium paddle stack. It checks manifest declarations and optional checkpoint file hashes for SAM 3, DINO-X/Grounded-SAM2, FoundationPose, GigaPose, FoundPose, and required CAD/reference/GT assets without importing model runtimes, using GPU, downloading models, or claiming inference. Current status is `blocked`, `runtime_ready_count=0`, `asset_ready=false`, `may_run_gpu_smoke=false`, and `may_promote_rkt=false`; the review action dashboard surfaces this as a single `__global__` paddle-runtime action.
- 2026-06-28 Codex: added `racket_model_adapters.py` as the fail-closed adapter seam for SAM 3, DINO-X/Grounded-SAM2, FoundationPose, GigaPose, and FoundPose. It builds a CPU-only adapter plan from `racket_model_runtime_readiness.json` and raises before any future adapter can touch GPU/runtime code unless the readiness report is `ready_for_gpu_smoke`; it never imports model runtimes or claims pose output. Tested with `.venv/bin/pytest tests/racketsport/test_racket_model_adapters.py -q` (`4 passed`).
- 2026-06-28 Codex: `body_mesh_readiness.json` compares the frame-plan representation demand, BODY execution schedule, and actual BODY outputs before telling reviewers whether joints/preview, manual review, or world mesh is the correct next state. Current local audits report Burlington, Wolverine, and Indoor as `world_mesh_required_available_unverified`; Outdoor remains `no_world_mesh_requested`. The available meshes are scheduled-frame H100 smoke evidence, not proof of BODY promotion.
- 2026-06-28 Codex: virtual-world JSON summaries now include review-only uncertainty counters: approximate court-plane ball frames, mesh/joint/track-only player frames, paddle players, and ambiguous paddle frames. These counts make packet/HUD review clearer but do not change replay geometry or promote RPL/RKT.
- 2026-06-28 Codex: Phase 6 `racket_eval` is now fail-closed against presence-only RKT artifacts. It records canonical `racket_pose.json` counts as artifact context, but only reviewed `labels/racket_pose.json` reference/ArUco labels can produce measured face-angle/contact-point gates and a passing RKT result.
- 2026-06-29 Codex: audit hardening pass tightened several truth/guard edges without promoting any row to `VERIFIED`: BODY/RKT/E2E evaluators now derive required sidecar artifacts from `pipeline_contracts.py`; malformed eval metrics remain strict-by-default but can be reported with `--allow-malformed-metrics`; `BallStageRunner` no longer reads the global prototype root unless an explicit prototype fallback is enabled; post-run review helpers rebuild stale/unowned `frame_compute_plan.json`/`body_compute_execution.json` without overwriting successful BODY-owned artifacts; the web replay viewer trusts explicit label-coordinate metadata instead of guessing half-resolution from box geometry; iOS replay benchmarks count detector invocations separately from propagated frames; and mobile replay has appearance-aware stable assignment plus a `role_lock_wide_stable` linker for wide-pool bootstrap and temporal tracking. These are fail-closed/audit fixes, not capability verification.
