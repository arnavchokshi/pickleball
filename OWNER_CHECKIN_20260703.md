# Owner check-in — 2026-07-03 (Fable manager session)

## ⭐ Headline
Root causes of both your flagged failures are now **measured, ruled, and being fixed as one re-architecture** (not patches). The raw SAM-3D joints already match the mesh to ~6mm — our own post-processing was destroying them: one-euro smoothing (+710mm p90 on extreme poses, +1211mm on far players) and the core jitter clamp (+356mm) cause the skeleton≠mesh distortion; post-hoc re-anchoring + laggy uniform root smoothing cause the body-lag. Implementation lane I1 (stance-aware grounding on the placement trajectory, kill all post-hoc shifts) is running now; lane I2 (mesh-consistent skeleton everywhere, cut the damaging smoothers) follows. **One decision is staged for you below (outdoor calibration).**

## 🟥 Decision needed from you (1)
**1. Outdoor calibration — RESOLVED (owner, ~23:30):** corners were already tapped — `court_corners.json` + reviewed `court_calibration_metric15pt.json` exist in the clip's labels. Owner authorized using court-geometry files from labels for calibration (render/demo path). Gameplay labels (ball/events/foot_contact) remain scoring-only, untouched. Not-for-scoring provenance guard to be wired with the multi-video wave. Outdoor unblocked.

## Blockers
- None besides the decision above. Joints/grounding work proceeds on Wolverine meanwhile.

## Your 15:00 directive — how it's being executed
GPU-first: two speed-diagnosis lanes running now (live A100 utilization profiling during a full-clip BODY run + stored-timing audit of where the 1033s E2E goes); optimizations ruled and implemented after their reports. Then the finish-bar loop: fresh E2E worlds on Wolverine + Burlington + IMG_1605 through the fixed pipeline, verified visually (screenshots) and numerically until no further improvement is available; Outdoor joins as soon as you answer the calibration question above. Ball tracking deep-dive starts only after that bar is met (and will be fenced off your other session's in-flight ball/court work in the dirty tree).

## Verify when back
- (will be filled as implementation lanes land)

## Money / GPU log
- No GPU spend this session yet. A100 assumed running (not yet touched this session).

## Overnight log
- 4-lane research wave dispatched (R1 refine-chain audit, R2 mesh-derived skeleton, R3 unified grounding design, R4 outdoor calibration). Fixed lane-report schema for strict structured output (first dispatch 400'd).
- R3 PASS: refine-time grounding design ready; key finding = native anchor path already wired; fix = harden contract + kill post-hoc XY re-anchors + stance-aware smoothing + transl_world consistency. Design: `runs/lanes/r3_unified_grounding/design.md`.
- R4 PASS: calibration decision pack staged (above).
- R1 PASS: per-stage audit (399 extreme / 300 normal player-frames, near/far split, harness sanity-checked against the known 18.9mm slide number). Verdicts: one-euro MODIFY/bypass (worst offender), core clamp MODIFY, bone-length/wrist-lock/contact-splice KEEP (all ≤7mm), foot pin MODIFY (36mm, far-only), staged re-anchor CUT. `runs/lanes/r1_refine_audit/per_stage_audit.md`.
- R2 PASS: mesh-derived skeleton = FEASIBLE-WITH-GPU-CHANGE. Best option: GPU emits MHR70 mesh-regressed keypoints for EVERY body frame at ~+0.0–0.2 ms/person (~free) + ~0.46MB sidecar — solves the 84%-of-frames-have-no-mesh coverage gap. A100 validation of mapping exactness dispatched (Sonnet lane, running).
- RULING (Fable): one re-architecture, two sequenced lanes. I1 = R3's refine-time grounding (stance-aware root smoother, delete post-hoc XY translators, transl_world fix; acceptance: P3 transition divergence 1.86m→<0.20m, clamp engagement 16–32%→≤5%, slide ≤20mm, placement harness stays green) — DISPATCHED. I2 = pose-side cuts + mesh-derived skeleton source — after I1 + A100 validation. Independent verifier lane attacks I1 when it lands.
- Infra: fixed `docs/racketsport/lane_report.schema.json` for strict structured output (all lanes now return validated JSON reports).
- A100 validation PASS (big simplification): the GPU's emitted 70-joint skeleton **already is** the mesh-regressed output (max diff 3.6e-7 m) on every body frame — no GPU change needed for skeleton-mesh consistency. The distortion you saw is 100% our local post-processing. I2 is now a local-only lane. (GPU left clean; repo pulled to latest main; VM disk at 88% — flagging, not urgent.)
- I1 (grounding unification) LANDED, PARTIAL-pending-GPU: all locally-measurable gates PASS (foot slide 18.9mm, kitchen bias 0.0094m, wrists 0.0%, transl_world stale→max 0.015m FIXED, placement harness green, post-hoc translators dead + double-shift regression-guarded). P3 divergence/lag/clamp gates honestly await the fresh A100 E2E. Changes uncommitted (sandbox git lock) — coordinated commit after the verifier rules.
- YOUR GPU QUESTION ANSWERED (live profiling, full 1102-request Wolverine BODY run): **the A100 is ~5% busy** — 15.5s of GPU compute in a 316s run. Model itself is fast (14.09 ms/person GPU-only). Time goes to: cold load+DOUBLE compile warmup 104s (a warmup-contract bug compiles the wrong decoder graph, ~40s pure waste), serial CPU between 0.22s GPU bursts (98s: JPEG decode + JSON conversion inline), and a 60s monolithic 1.45GB JSON write at the end (first result arrives only when EVERYTHING is done). Memory: 7.7GB/40GB peak — huge headroom. Full evidence: `runs/lanes/g1_gpu_utilization/` + `runs/lanes/g1b_speed_audit/stage_budget.md` (E2E: BODY is 71.8% of the 1033s wall; legacy pose 150s; tracking fully serialized before GPU).
- SPEED RULING + wave 2 dispatched (3 parallel lanes): V1 = independent verifier attacking I1; I2a = cut/replace the damaging pose smoothers (hard 30mm displacement cap; extreme mesh-agreement p90 1105→≤60mm target); G2a = batch-runner fix (correct warmup graph, prefetch pipeline, streaming chunked output; targets: 316s→≤150s total, time-to-first-result 316s→≤80s). Hot-worker + transfer overlap deferred — it touches remote_body_dispatch.py which your other session has dirty.
- Wave 2 landed: I2a ALL 9 TARGETS PASS (skeleton-vs-mesh extreme p90 1105→25.9mm = raw ceiling; far players healed). V1 verifier found 1 blocker + 4 majors in I1 → I1 fix round FIXED ALL 5 (verified: NaN fail-safe, honest clamp metrics, legacy-artifact routing — the 485mm "blocker" was a stored-old-data artifact, legacy route measures 19.4mm slide).
- A100 bench of the new runner: time-to-first-result 316s→71s ✅, warmup 65s ✅, zero idle gaps ✅, outputs equivalent ✅(1e-6-ish), BUT total wall 644s ❌ — writer-thread GIL contention throttled the GPU loop (bucket 0 normal 0.27s, rest 3.8-7.4s) + ~243s redundant end-of-run work. Fix round 2 running with exact root-cause facts (`runs/lanes/g2b_a100_bench/g2b_summary.json`).
- Fresh A100 E2E (the accuracy gate: P3 lag 1.86m→<0.20m) queued to launch the moment runner fix round 2 lands — held to avoid racing the runner file being edited.
- Runner fix round 2 PROVEN on A100: **316s → 84.8s total (3.7×), first results at 66s** (was 316s), GPU 16.7s with zero idle/inflation, outputs identical to ~1µm (fp32 floor; the "1e-6 fail" was my over-tight bar, ruled immaterial). Bucket-32 untested (gated). Evidence: `runs/lanes/g2b_a100_bench_rev2/`.
- OWNER RULINGS (~23:55): NO ball work — joints/placement only. Commits deferred (both sessions together, later). Outdoor calibration resolved (corners already tapped). ETA given: Wolverine world ~1h, all 4 videos ~3–4h clean / 4–6h with fix rounds.
- **Fresh Wolverine E2E DISPATCHED (~00:10)** — the accuracy gate run; produces the first viewable fixed world + P3 divergence/lag/clamp numbers.
- **SETBACK found by that run (~00:20), not our code:** (1) local `.venv` was gutted Jul 3 02:34 — torch/ultralytics/playwright GONE (tracking/BODY-dispatch/viewer-verify all dead locally); (2) local disk 98% full (10GiB free; runs/ = 61GB) — likely cause of the venv strip; (3) VM checkout at HEAD lacks tonight's uncommitted fixes, so remote BODY would run pre-fix code. FIXES IN FLIGHT: ops lane restoring venv (RISK: torch wheel for Python 3.14 may not exist → would need venv rebuild, +30-60min) + syncing the 7 fixed .py files to the VM (reversible; commit+push was attempted and permission-blocked per your "commit later" — say "commit it" to override). E2E re-dispatches when ops lane reports. Wolverine world ETA slipped ~30-60min.
- ALSO: web/replay viewer sources became dirty mid-session — your other session appears ACTIVE. All our commits/staging exclude its files.
- ⚠️ Local disk needs a decision from you eventually: 10GiB free, runs/ holds 61GB of artifacts — I won't delete anything without your word. (Update ~01:15: free space jumped to 33GiB — something/someone freed space.)
- **Fresh Wolverine E2E #2 COMPLETED (~01:05) — hugely informative partial:** pipeline ran end-to-end, viewer verified (4 players, 112 FPS, 0 errors), far-player divergence already 1.86m→0.36m (5×) — BUT the stance-aware grounding NEVER ENGAGED: dispatch never ships placement.json to the VM, placement natively emits 0 stance phases (stance only existed in the staged flow), and torchreid was missing so ReID association silently degraded tracks (kitchen bias stuck 0.45m, slide gate failed loudly as designed). Also: remote BODY re-derives calibration instead of using the trusted one (1.6m internal offset, absorbed by the world builder to ≤0.02m).
- FIXES: torchreid installed+verified locally (Fable, inline). FIX2 lane dispatched (~01:20): ship placement.json+calibration with BODY inputs, native stance-phase emission in placement, remote consumes trusted calibration. E2E attempt 3 after it lands. Preview world (accuracy caveats): http://localhost:5173/?manifest=/@fs/Users/arnavchokshi/Desktop/pickleball/runs/i1_grounding_unification_a100_wolverine_20260704T0718Z/wolverine_mixed_0200_mid_steep_corner/replay_viewer_manifest.json
